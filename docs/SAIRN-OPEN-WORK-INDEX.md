# SAIRN Open Work Index

**One place to see everything that is open across the whole platform.**
Last rebuilt: **2026-08-23**. Last updated: **2026-08-25** (dead bare-named tables + the incomplete grant export).

Before this file existed, open work lived in `SAIRN-BACKLOG.md`, in
`SAIRN-ACTIVE-WORK.md`, in ~40 per-app handoffs, and in skill files — so real
items got missed and sessions sat idle while work existed. This is the index.
It does not replace those files; each row points back at the source of truth.

**Note, 2026-08-24:** `SAIRN-ACTIVE-WORK.md` was split per session to stop the
recurring merge conflicts. Its historical entries are still there (unchanged),
but new active-work entries go in `SAIRN-ACTIVE-WORK-hank.md`,
`SAIRN-ACTIVE-WORK-cc.md`, `SAIRN-ACTIVE-WORK-cody.md`, and
`SAIRN-ACTIVE-WORK-fourth.md` (one per clone). **The next rebuild of this index
must read all four, not just the shared file.**

---

## How to use this in 30 seconds

1. Filter by **Owner** — `unassigned` rows are the ones a free session can take.
2. Skip anything whose **Blocked by** is not empty unless you can clear the block.
3. Do what **Next action** says. If it says *verify*, the row's status is a claim
   that has not been re-checked; verify before building on it.

**Every row is a claim, not a fact.** Same standard as a handoff: re-verify
before you act on one. Rows marked ⚠️ are ones this rebuild could not confirm.

---

## Open items

Legend — **Sz**: S = under a session, M = one session, L = multi-session.

| App | Item | Status | Owner | Blocked by | Next action | Sz |
|---|---|---|---|---|---|---|
| **SAIRNbuild** | Zero server-side backup for any real business data | Open | unassigned | — | Scope the resource set, then add to `api/sd-data.js` allowlist + schema. Largest single item in the backlog | L |
| **Platform** | No app clears local data on a license/device re-key; storage keys are not license-scoped | Open | unassigned | — | Decide scope (all 13 apps or lead app first), then namespace keys by license hash | L |
| **Platform** | No delete capability via `api/sd-data.js` **except** the `SC_RESOURCES` (SAIRNcode) family | Open — **row corrected 2026-08-24**; **DB-side decision now MADE, see next row** | unassigned | — | The old wording said "for any resource, any app", which is wrong: `delete` IS implemented and gated for `SC_RESOURCES`, so there is a working pattern to copy rather than a design to invent. Found while trying to clean up a verification probe from `dnt_appointments`. Decide soft- vs hard-delete, then widen the gate — note the gate and the handler branch are separate, and registering one without the other yields an unreachable 400 (`api/sd-data.js` says so in its own comment). **The DB grant no longer pre-authorises hard-delete** — if this is ever built, the grant must be re-added deliberately for the specific tables chosen, which is the point | M |
| **SAIRNscape** | `scp_employee_auth_schema.sql` granted `delete` at source — **FIXED 2026-08-25** | **Done** — source now `grant select, insert, update`, matching all eight siblings | Cody | — | The sweep fixes the live grant; this fixes the file. Needed separately because the file is `create table if not exists` and safe to re-run, so re-running it after the sweep would have put DELETE straight back. Verified after the edit: **zero** `*_employee_auth` files on the platform still grant `delete` (`grep -rniE "^\s*grant[^;]*\bdelete\b[^;]*employee_auth" sql/*.sql` → no hits). Root cause was an overcorrection recorded in the file's own header — grants added explicitly on 2026-08-06 to fix a pattern of **missing** grants, and the full CRUD verb list written instead of the verbs used | S |
| **SAIRNdental** | **`dnt_referrals` is a REAL, shipping feature with no live table — migration never run** | **CLOSED 2026-08-25 — migration run, all four legs verified live** | **Michael** | Needs Supabase SQL editor | **Not abandoned — checked, not assumed.** It is a registered resource (`api/_resources/sairndental.js:47`), has a live handler (`api/sd-data.js:4852`, idCol `referral_id`), and `sairndental.html` ships both an add path (`:1609`) and a status-update path (`:1618`). But the table is absent from the live catalog — it appeared in neither the 214-row grant export nor the 134 the DELETE sweep touched. Same class as `sairncash_waitlist`, `sairnscape_org_intel` (`6776f99`) and the six SAIRNsenior tables: schema file in the repo, migration never run. **It degrades HONESTLY, which is why nobody noticed** — the UI toasts *"Saved on this device only — server sync not yet enabled for this app"* on a failed sync rather than reporting false success, so this is a provisioning gap, not a silent-failure bug. Consequence while unprovisioned: every referral lives only in one browser's localStorage with no server copy. **Its grant line was fixed pre-emptively 2026-08-25** (`delete` removed) so the table arrives clean rather than reintroducing on day one what the sweep just removed — no delete path exists (`DNT_RESOURCES` handles only read/write). **Both done 2026-08-25.** Migration run; the read leg now returns `{"ok":true,"data":[],"provisioned":true}` where it returned `provisioned:false` before — that flip is the evidence the table is live, not the absence of an error. Round-trip exercised against `https://sairn.vercel.app/api/sd-data` on licence `DNT-PINNACLE-2026`, all four legs: **read** as above; **write** 200 with `location_id` `"LOC-DEFAULT"` stamped **server-side** though it was never sent in the payload, which also confirms `dntLocation.stampLocation()` covers this path; **update** 200 driving `Pending -> Completed` through the same upsert the UI's `setReferralStatus()` uses, and the read-back returned exactly **one** row, proving `on_conflict` merged rather than inserting a duplicate — a real UPDATE on a table carrying live traffic, which independently confirms the same night's DELETE sweep did not collaterally drop UPDATE here (the 134-LOST/0-GAINED result holding outside a diff); **delete** 400 `"action must be 'read' or 'write'"` **by design** — `DNT_RESOURCES` implements only read/write (`api/sd-data.js:4854`, `:4862`). The verification wrote one probe row that the product itself cannot remove, so cleanup is hand-written in `sql/sairndental_referrals_verify_cleanup.sql` (**NOT RUN** — owner, Supabase SQL editor; keyed on `referral_id = 'RF-VERIFY-PROBE-20260825'`, not on a LIKE pattern and not on the licence, because `DNT-PINNACLE-2026` is shared demo data that must survive). See the probe-row row below — this is the tenth app to need such a file | S |
| **Platform** | Live write-path verification leaves rows the product cannot delete — **14 hand-written cleanup files across 10 apps** | Open (process) | unassigned | — | Counted 2026-08-25, real files on disk, not an estimate: 13 `sql/*_cleanup.sql` plus `sql/sairnlaw_remove_probe_rule_2026-08-25.sql`, which does the same job under a name that skips the `_cleanup` convention — **counted for what it does, not what it is named**. Apps: SAIRNgrounds and SAIRNscape (both via `rbac_test_artifact_cleanup.sql`, which spans two licences, `GRD-DEMO-2026` and `SCP-DEMO-2026` — this is why the file count and the app count do not line up one-to-one), SAIRNbuild, SAIRNcash, SAIRNcode, SAIRNdental (×2), SAIRNdesign (×2), SAIRNlaw, SAIRNlegacy, SAIRNroofing (×3). **This is a pattern, not a run of coincidences** — the `dnt_appointments` probe is what surfaced the platform-wide "no delete capability" row above, and `dnt_referrals` hit the identical wall the same night, so it has now recurred within one session on one app. Every file carries **NOT RUN**, so the probe rows are still live in production and the debt is real, not theoretical. **Next action:** decide deliberately between the two options already written into `sairndental_referrals_verify_cleanup.sql` — a disposable verification licence whose rows can be bulk-removed, or accepting a hand-written cleanup file per live write check and saying so in the verification standard. Do **not** treat this as an argument for widening the delete gate; that is the separate Platform row above and has its own decision | M |
| **Platform** | Hook commands in `.claude/settings.json` hardcode an absolute tools path — move to `${CLAUDE_PROJECT_DIR}` | Open (tooling), **deliberately deferred 2026-08-25** | unassigned | — | **Considered and postponed, not overlooked.** Tonight all four clones were repointed from `C:/Users/marsh/tools/` to `C:/SAIRN/tools/`, which fixes the immediate breakage (that directory now holds only 2 of the 5 scripts) but keeps every clone dependent on one machine-specific path outside any working tree. The durable fix makes each clone self-contained. **Deferred because the current fix is live and working across all four clones and swapping approaches again risks more confusion than it removes** — apply it in one deliberate pass, not mid-session. **The diff below was RE-DERIVED on 2026-08-25 from the current file, not recalled** — the session that first worked it out was restarted and its version is gone; treat this as a fresh derivation to be reviewed on its own merits. Two steps: (1) `git mv`/copy the five scripts from `C:/SAIRN/tools/` into each clone's `.claude/tools/` so they are versioned with the settings that call them; (2) rewrite all **six** hook commands — `PostToolUse` `html_script_check.py` and `deploy_verify_notify.py`, `PreToolUse` `git_push_master_guard.py` and `redaction_check.py`, `SessionStart` and `UserPromptSubmit` `session_lock_check.py` (`start` / `heartbeat`) — from `python C:/SAIRN/tools/X.py` to `python "${CLAUDE_PROJECT_DIR}/.claude/tools/X.py"`, quoted because the expansion contains spaces on this machine (`C:\Users\marsh\Documents\...`). **One objection checked and cleared:** `session_lock_check.py` is deliberately cross-clone, so a per-clone copy sounds like it would break the lock — it does not. The lock state lives in `~/SAIRN-SESSION-LOCKS` (`session_lock_check.py:49`), outside every clone by design, and the clone identity comes from `os.getcwd()` (`:58`), not from the script's own location. Five identical copies still share one lock directory. **Verify after applying:** `json.loads` the file, then confirm a hook actually fires — a `${CLAUDE_PROJECT_DIR}` that fails to expand degrades to a silently missing script, the same failure class as tonight's stale path | M |
| **Platform** | Explicit `delete` grants removed from every non-`sc_*` schema file | **CLOSED 2026-08-25** | — | — | 68 grant lines across 16 files, 68 distinct tables. Confirmation grep returns **only** the two rows above, both deliberately excluded. `sc_*` untouched — 29 lines across 15 files, the platform's only reachable delete path. Each edited file carries a 6-line note recording why `delete` is absent, so a future session fixing a *missing* grant does not re-add it — which is exactly the 2026-08-06 mistake that created the SAIRNscape overcorrection | — |
| **StoneDesk** | `sd_employee_auth_schema.sql` had **no `grant` statement** — **RESOLVED 2026-08-25, it was a doc gap, not a break** | **Done** — origin found, grant backfilled into the file | Cody | — | Confirmed live: `service_role` holds INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE — **no DELETE** — matching every sibling exactly. **Origin found, not guessed:** the grant was applied **by hand in the Supabase SQL editor and never backfilled**. Recorded contemporaneously in a *different* file — `sql/sd_render_usage_schema.sql` (2026-08-04) says *"an earlier real incident on sd_employee_auth … where the fix was applied manually in the Supabase SQL editor and never backfilled into that migration file — a gap worth knowing about if that table is ever re-provisioned from scratch."* **That note predicted this exact confusion**, but lived in the wrong file to prevent it, which is why two sessions had to rediscover it. Same 42501 `permission denied` class that hit `sd_render_usage` on its first live run: on this project, `CREATE TABLE` alone grants `service_role` nothing. Only SELECT/INSERT/UPDATE came from the manual grant — REFERENCES/TRIGGER/TRUNCATE are the platform default ACL baseline, fixed in `sql/append_only_grant_audit.sql`, not here. **Grant now folded into the file** so a fresh run is self-sufficient, per the convention `sd_render_usage_schema.sql` set. **Also recorded there so nobody "fixes" it later:** `sql/stonedesk_audit_license_credential_reset.sql` really does `delete from public.sd_employee_auth`, but runs as the **owner** role like every other cleanup script — it does **not** need a DELETE grant | S |
| **Platform** | Cross-file notes: a warning written in the file that *hit* the bug, not the file that *has* it | Open (process, small) | unassigned | — | Surfaced 2026-08-25 by the `sd_employee_auth` resolution above. The manual-grant gap was correctly documented on 2026-08-04 — in `sd_render_usage_schema.sql`, the file that suffered the *same class* of incident, not in `sd_employee_auth_schema.sql`, the file that actually carried the gap. Nobody reading the affected file could see the warning, so it was rediscovered twice in one session. **Next action:** when a note explains a defect in *another* file, put a pointer in that other file too. Cheap habit, no tooling needed. Worth a line in `sairn-guardian-v2` if it recurs — **not** worth a new check yet on one instance | S |
| **Platform** | **`license_keys` grants need their own dedicated review — excluded from every sweep until then** | Open — **excluded on Michael's decision 2026-08-25**, not deferred by drift | unassigned | Needs Supabase SQL editor **and** a decision on where its schema lives | **Why it is excluded, not just postponed:** `license_keys` has **no tracked `CREATE TABLE` anywhere in this repo**, so any sweep's output cannot be checked against an expected schema — there is nothing to diff against. `api/_lib/license.js`'s own header documents this: the real table is *"owned by a separate, not-yet-built generation system"*, and `sql/demo_license_keys_seed.sql` works around the absence with `WHERE NOT EXISTS` precisely because no committed schema means no constraint to check. **Blast radius is every license check on the platform** — all 13 apps gate on it. Both sweeps now exclude it by name: `sql/full_crud_truncate_sweep_2026-08-24.sql` (TRUNCATE) and `sql/unused_delete_grant_revoke_2026-08-24.sql` (DELETE, Section 2). **Consequence to remember:** that DELETE sweep's Section 3 therefore expects **exactly one row — `license_keys`** — not zero; a future session must not read that as a failed revoke and "fix" it. **Next action:** review its live grants by hand against what `api/_lib/license.js` actually calls, decide whether a real schema file should be committed here or the table genuinely belongs to the separate system, then narrow its grants deliberately in a pass of its own. **Do not fold it back into a bulk sweep** | S |
| **SAIRNlaw** | AI Chain of Custody gap 2: `matter_id` is an unvalidated localStorage id | Open (gap 1 closed) | unassigned | `law_matters` must be server-backed first | Blocked — do the SAIRNlaw server-sync item first | M |
| **SAIRNlaw** | `law_check_and_insert_disbursement` can return a null row on a cross-client `trusttx_id` collision | Open ⚠️ | unassigned | — | The idempotency lookup keys on `(license_hash, trusttx_id)` and does **not** filter by client. Verify a cross-client collision reproduces, then add the client predicate | S |
| **SAIRNlaw** | `ai_list` derived-status window can go stale at high license-wide volume | Open (accepted nit) | unassigned | — | Per-license row-count or per-entry status query. Fails safe toward `Unreviewed`, never fabricates a status | S |
| **SAIRNlaw** | **"Which holidays actually count" — ONE bundled lawyer's question across four jurisdictions, not four separate ones** | Open — legal judgment | **Michael** | Needs a lawyer's read | **Bundled on Michael's direction 2026-08-25: do this as one research/decision pass, not one state at a time as each is seeded.** Every jurisdiction hits some version of it and the answers are likely to share reasoning. **(1) ILLINOIS** — statute (205 ILCS 630/17) vs court-observed (M.R. 5272) disagree **both ways**; M.R. 5272 omits Pulaski Day and Good Friday and adds the day after Thanksgiving. Confirm the statutory list is the right basis for 5 ILCS 70/1.11. Encoded that way; changes real dates. **(2) KENTUCKY — the blocker that stops the state being seeded at all.** RAP 6(A) and KRS 446.030 both say "legal holiday" and neither defines it; KRS 2.110 is titled "Public holidays", never uses the phrase, lists four days KY courts do not close for (Robert E. Lee Day, F.D. Roosevelt Day, Lincoln's Birthday, Confederate Memorial Day) and omits Thanksgiving. Encoding it fails **LATE**. **(3) TEXAS** — Tex. R. Civ. P. 4 and Tex. R. App. P. 4.1 both say "legal holiday" without defining it or citing Tex. Gov't Code 662.021, which is what the `tx` calendars encode; the link is the settled reading, not an express cross-reference. **(4) WEST VIRGINIA** — R. Civ. P. 6(a)(6) and R. App. P. 39(a) define it **differently within the same state** (Juneteenth), and one calendar per jurisdiction cannot serve both. **(5) ARIZONA** (added 2026-08-25) — Ariz. R. Civ. P. 6(a) says "legal holiday" and, on the text read, names no statute; the candidate A.R.S. 1-301 was not read. Same shape as Texas. **TWO JURISDICTIONS HAVE NOW ANSWERED IT THEMSELVES AND THEY ANSWER IT DIFFERENTLY, which is the strongest argument for asking this once rather than per state: WASHINGTON's CR 6(a) says outright "Legal holidays are prescribed in RCW 1.16.050" — an express cross-reference — and that statute then excludes its own decoys in subsection (7) ("may not be considered legal holidays for any purpose"); NORTH CAROLINA's Rule 6(a) instead asks whether "the courthouse is closed for transactions", so its statute is the wrong source entirely.** So the answer is not uniform and the question is really: which source does THIS rule's own words point at, and what do you do when they point at nothing? The common question: **when a court rule says "legal holiday" and points at nothing, does the general holiday statute supply the list, even where that statute is broader or narrower than what the courts actually observe?** Add the two Texas secondary-source rows to the same set while a lawyer is engaged | S |
| **SAIRNlaw** | Deadline engine coverage: 14 jurisdictions (federal + 13 states), 167 rules | **Batch 4 in progress** | **Hank** | — | **Batch 1: IL ✅ FL ✅ CA ✅. Batch 2: TX ✅ (`cc6899b`), NY ✅ (`18e41a4`), GA ✅ (`c656135`).** Then, on Michael's direction 2026-08-24: external claim doc rewritten after a decision-gate re-run (`e9db252`) ✅, `resolve_periods` built (`cd5744f`) ✅. **Batch 3: West Virginia ✅ (`ad45643`) — 17 rules, 123 → 140, first jurisdiction with TWO computation standards. Kentucky BLOCKED, not skipped (`fc14895`) — see its own row. Batch 4: North Carolina ✅ (`eb548f3`) — 13 rules, 140 → 153, civil only. WV and NC are both LIVE-LOADED and live-verified against the deployed endpoint under `LAW-PINNACLE-2026` (8/8 and 9/9 real computes).** **Batch 4: North Carolina ✅ (`eb548f3`) — 13 rules. Washington ✅ — 14 rules, 153 → 167. Arizona GATED OUT, banked not skipped — see its own row.** All four of WV, NC, WA (and the 10 earlier jurisdictions) are LIVE-LOADED under `LAW-PINNACLE-2026` and live-verified against the deployed endpoint. Next: gate the next state in line (New Jersey and Virginia are the largest unseeded, both need the gate run first), or NC/WA appellate (each needs its own source check — both states' appellate rules are court rules, not statutes), or a third domain (family/probate/criminal) | L |
| **SAIRNlaw** | **Arizona gated out — a NEAR MISS, not a dead end like Kentucky** | **Blocked 2026-08-25** ⚠️ | **Michael** (one decision unblocks AZ and KY together) | Westlaw terms | Full write-up with every verbatim text already obtained: `docs/sairnlaw-arizona-deadline-seed-blockers.md`. **azcourts.gov's own rules page links out to `azrules.westgroup.com` → `govt.westlaw.com`** for the maintained text — the same structure that blocked Kentucky, and not scraped for the same reason. What IS free on azcourts.gov is the **2017 restyled rules** (complete, official, but an undated petition attachment) plus a **maintained per-rule amendment index**. That index proves the snapshot is stale for **four of the six rules a seed needs**: `R-17-0010` amended Rules 33–37 and `R-20-0028` amended Rule 12 (eff. 2021-01-01). **The index itself stops at 2024**, so even reconstruction carries an unknown 2025–2026 gap. **Why this is a near miss:** Kentucky had no free base text at all; Arizona has a complete base plus a free official amendment index, and the remaining work is bounded — apply two named orders and close the 2025–26 gap. It was still failed because a reconstruction cannot prove its own completeness, and a missed amendment yields wrong dates with no refusal. **Already banked verbatim:** Rule 6's short-period exclusion is **ELEVEN days**, higher than any state seeded (all the others are 7) — encoding 7 by analogy would be wrong on every 7–10 day Arizona period; Rule 6(a)(4) **defines the backward direction expressly**, so AZ backward rows would be seedable where NC's and WA's were not; and Rule 6(c) adds **FIVE** days on the **federal after-expiry** order, matching only Florida. Arizona's Rule 6 also says "legal holiday" without naming a statute — **add it to the bundled lawyer's question**. **Next action: read Westlaw's terms once — it unblocks Arizona and Kentucky together** | L |
| **SAIRNlaw** | Canonical deadline license is `LAW-PINNACLE-2026`; all 153 rules backfilled there | **CLOSED 2026-08-25** | — | — | Rules are stored per `license_hash`, and they had drifted across two: `LAW-TEST-2026` held 8 jurisdictions / 87 rules (every earlier "live-loaded" claim went there), `LAW-PINNACLE-2026` held nothing until WV and NC were loaded into it, and **neither had Texas, New York or Georgia — Batch 2 had never been loaded anywhere**. Michael's call 2026-08-25: `LAW-PINNACLE-2026` is canonical, matching the naming convention every other app uses. Backfilled in one pass with `python tools/load_deadline_seed.py --file ...` over all 30 seed/calendar files: **79 calendars + 153 rules, zero failures**. **Verified independently of the loader**, not assumed from its exit code: live `rules_status` counted per jurisdiction and diffed against the repo JSON — all 13 match exactly (ca 10, fl 7, ga 12, il 6, in 7, mi 18, nc 13, ny 11, oh 8, pa 11, tx 11, us-federal 22, wv 17) — and a real `compute` was run in **all 13 jurisdictions, 13 returning a real date, 0 refusing**. `LAW-TEST-2026` still holds its stale 87 and is now genuinely a test tenant; nothing was deleted from it |  |
| **SAIRNlaw** | Junk `zz` / `__probe__` rule removed from `law_deadline_rules` | **CLOSED 2026-08-25** | — | — | `sql/sairnlaw_remove_probe_rule_2026-08-25.sql` run by Michael: exactly one match before, none after. **Verified through the API, not just the table** — live `rules_status` now returns **13 jurisdictions / 153 rules with no `zz`**, the per-jurisdiction counts are identical to the repo JSON, and a real `compute` in all 13 still returns a date with 0 refusals. **How it got there and the lesson that outlives it:** I checked whether a new computation standard had deployed by POSTing `add_rule` — a **write** endpoint — and the dummy validated, so it was stored. It could never change a computed date (no caller asks for `zz`) but it inflated the coverage figure `rules_status` reports, which is what the UI shows. It needed SQL because `api/legal-deadlines.js` has compute, rules_status, add_rule and add_holidays and **no delete** — the platform-wide gap tracked separately above, still open. **Never probe a deployment with a write endpoint** — use `rules_status`, or a `compute` you expect to refuse. Recorded in `tools/load_deadline_seed.py`'s docstring so the next person meets it there |  |
| **SAIRNlaw** | Batch 4 pick: **North Carolina — source-availability gate PASSED, ready to seed** | Open — **Hank, next up** | **Hank** | — | **The gate is new and exists because Kentucky did not have one:** confirm the primary sources are readable and permitted BEFORE investing in a jurisdiction. NC picked as the largest unseeded state (~11M) whose civil rules are **statutes** — N.C. Gen. Stat. § 1A-1 — published free and in full by the NC General Assembly on `ncleg.gov`, which is precisely the failure mode Kentucky hit. Rules 5, 6, 12, 33, 34, 36 and G.S. 103-4 already downloaded and read verbatim. **THREE FINDINGS THAT CHANGE HOW IT MUST BE SEEDED. (1) NC RESOLVES THE BUNDLED HOLIDAY QUESTION IN ITS OWN RULE TEXT** — Rule 6(a) rolls off "a Saturday, Sunday or a legal holiday **when the courthouse is closed for transactions**". It does not point at G.S. 103-4, and that matters enormously: 103-4 lists Robert E. Lee's Birthday, Greek Independence Day, the Halifax Resolves anniversary, Confederate Memorial Day, the Mecklenburg Declaration anniversary, Columbus Day, First Responders Day, Yom Kippur, Washington's Birthday and general election day, and the NC Judicial Branch closes for **none of them**. Encoding 103-4 would add ~9 spurious holidays and push deadlines **LATE**. The right basis is the NCAOC's published closure schedule — same treatment already given to Michigan's locally-modifiable MCR schedule, and the **inverse** of the standing Pennsylvania guidance in the engine header, which says to encode the statute; NC is the counter-example where the rule itself keys on closure, so that guidance must be read as conditional on what the rule's own test is. **(2) COVERAGE WILL BE 2026–2027 ONLY, not the 2026–2031 every other jurisdiction has.** NCAOC publishes dated schedules two years forward (read 2026-08-25 from `nccourts.gov/holiday-schedule`, which is JS-rendered and 403s to plain fetches — it needs a real browser). 2028+ must refuse NOT_PROVISIONED honestly, and the calendar needs re-running each year as NCAOC publishes. **(3) THE CHRISTMAS AND THANKSGIVING BLOCKS ARE ADMINISTRATIVE, NOT DERIVABLE** — Christmas is THREE days (2026: Dec 24, 25, 28; 2027: Dec 23, 24, 27) and Thanksgiving TWO, allocated by decision, not by any rule. Proof that a computed calendar would be wrong. Also already read: Rule 6(e) adds three days **"to the prescribed period"** (period-lengthening, NOT the federal order) and **mail only**; Rule 6(f) adds **five** days for Address Confidentiality Program participants but **only on periods of 10 days or less** — a per-method amount conditioned on the BASE PERIOD LENGTH, which `amount(method)` cannot express today and is likely a small engine addition | L |
| **SAIRNlaw** | **Kentucky: blocker 1 may be DEAD — the free Westlaw government edition carries the KY civil rules on the same host whose terms were just cleared** | **Needs Michael's call, 2026-08-25** ⚠️ | **Michael** | One factual check | **Correcting the record rather than acting on it.** Blocker 1 was recorded as "no permitted primary source", and Michael's 2026-08-25 decision kept Kentucky blocked on the stated basis that kycourts.gov "points straight to Westlaw's paid product with no free mirror — there's no page to read". **That last part appears to be wrong.** The Arizona link he cleared, `azrules.westgroup.com`, redirects to **`govt.westlaw.com/azrules`**; kycourts.gov's link goes to **`govt.westlaw.com/kyrules`** — the *same free government-edition host*, listed on that host's own "State Government Sites" index alongside Arizona. Its Kentucky **Rules of Civil Procedure** browse page loads without a subscription (verified 2026-08-25: Parts I–XIII plus the 1952 Civil Code Disposition Table). If the free-access terms he read cover that host rather than the `/azrules` path specifically, **Kentucky's blocker 1 is resolved by the same reading** and CR 6.01, 6.05, 5.02, 12.01, 33.01, 34.02 and 36.01 become readable. **Blocker 2 is untouched either way** — "legal holiday" is still undefined for Kentucky courts and KRS 2.110 still fails LATE, and that alone stops the state being seeded. So this changes Kentucky from *two* blockers to *one*, not from blocked to buildable. **Not acted on:** re-reading the terms is Michael's call, and the earlier decision was explicit | S |
| **SAIRNlaw** | Kentucky blocker 2 (the fatal one): "legal holiday" undefined, KRS 2.110 fails LATE | **Blocked 2026-08-25** ⚠️ | **Michael** | Bundled lawyer's question | Full write-up: `docs/sairnlaw-kentucky-deadline-seed-blockers.md`. RAP 6(A) and KRS 446.030 both say "legal holiday" and neither defines it; KRS 2.110 is titled "Public holidays", never uses the phrase, **lists four days KY courts do not close for** (Robert E. Lee Day, F.D. Roosevelt Day, Lincoln's Birthday, Confederate Memorial Day) and **omits Thanksgiving**, which reaches Kentucky only by annual presidential proclamation. Encoding it rolls deadlines LATER than any real court. **Fatal on its own: no calendar means every KY row refuses.** Belongs to the bundled five-jurisdiction holiday question. Banked and reusable regardless: KY replaced its appellate rules 2023-01-01 (Order 2022-49) and **CR 73.01–73.08 are REPEALED**, so every summary still citing CR 73.02 for the notice of appeal cites a dead rule; RAP 3, RAP 6 and amended CR 77.04 read verbatim; KY **adds 3 days for electronic service** (eFiling Rules 13(6)); and **KRS 446.030(1)(b) rolls a BACKWARD period FORWARD**, which `rollOff` cannot express today | L |
| **SAIRNlaw** | `us-federal` answer-to-complaint row grants an FRCP 6(d) extension for service of PROCESS | Open ⚠️ — **found 2026-08-25 while seeding West Virginia** | unassigned | — | `frcp-12a1Ai-answer-after-service` carries `service_extension: frcp_6d`, but FRCP 6(d) extends only where "service is made under **Rule 5(b)(2)**", and a summons and complaint go out under **Rule 4**. Georgia's equivalent row deliberately carries none (9-11-6(e) says "other than process"), Texas's and New York's carry none by inference, and West Virginia's carries none by Rule 5(a)(1)(B)'s scope — the federal row is the odd one out among five states checked. **The error direction is LATE: mail service adds 3 days to a 21-day answer deadline that should not move, and late is how a filing is missed.** Not fixed in the WV commit because it is an unrelated jurisdiction's data. Next action: confirm the reading, then drop `service_extension` from that one row and check `frcp-12a1B` and `frcp-12a2` (those two look correct — both run from service of a paper under Rule 5) | S |
| **SAIRNlaw** | One holiday calendar per jurisdiction cannot serve West Virginia, which has TWO official holiday lists | Open — **structural limit, disclosed not fixed** | unassigned | Needs a schema decision, not a patch | Found seeding WV 2026-08-25. `W. Va. R. Civ. P. 6(a)(6)` and `W. Va. R. App. P. 39(a)` each define "legal holiday" for their own rules and **the lists differ**: 39(a) expressly names Juneteenth, 6(a)(6) does not and cannot reach it — 6(a)(6)(C) counts days declared by the Governor, the President or the WV Legislature and **omits Congress**, which is what created Juneteenth National Independence Day. `holidayFor()` keys a calendar by jurisdiction + year only, so one `wv` calendar serves both. **It encodes the CIVIL list, so a WV appellate deadline landing on 19 June computes one day EARLY.** Early was chosen deliberately: the opposite would roll civil deadlines LATE, and the file's own Pennsylvania note already states that rule. First jurisdiction in the engine where the two lists genuinely conflict; us-federal has no conflict because FRCP 6(a)(6) and FRAP 26(a)(6) agree. Next action: decide whether calendars gain a per-standard key, or whether the per-row disclosure is sufficient | M |
| **SAIRNlaw** | `W. Va. R. Civ. P. 6(e)`'s subparagraph pointer contradicts its own parenthetical | Open (refused in code, disclosed on every row) ⚠️ | unassigned | Needs a lawyer's read, like the Illinois holiday row | Found 2026-08-25. 6(e) extends for service under "Rule 5(b)(2)(C) (mail), (D) (leaving with the clerk), or **(F) (other means consented to)**" — but WV inserted a state-specific 5(b)(2)(E) (WV E-Filing / facsimile) that pushed the federal lettering down one, so WV's **(F) is "other ELECTRONIC means ... consented in writing"** and **(G) is "any other means ... consented to in writing"**. The pointer and the parenthetical name different subparagraphs; the cause is visibly an unrenumbered carry-over from FRCP 6(d). Read by the pointer WV extends for consented electronic service, which the federal rule stopped doing in 2016; read by the parenthetical it does not. **The engine refuses both contested methods visibly (new `refused_contested_standard` state) rather than resolving them** — the usual "prefer the earlier date" tie-break would mean adding nothing, and a silent no-extension is indistinguishable from "none requested", which is the defect this engine already fixed once. `mail` and `left_with_clerk` are unaffected. Next action: add to the same question set as the Illinois holiday row if a lawyer's read is ever commissioned | S |
| **SAIRNlaw** | `resolve_periods` — "later of two COMPUTED PERIODS" | **CLOSED 2026-08-24** (`cd5744f`) | — | — | Built as a real engine capability, not a patch. `computeBasePeriod` was extracted from `computeDeadline` **verbatim** so limb computation reuses the tested code rather than duplicating the short-period exclusion, Florida's shifted start, month clamping and the missing-calendar refusal — proven inert by diffing an 8,076-line behavioural signature captured before and after the extraction (byte-identical). Each limb is computed in full and the RESULTS compared; comparing unrolled limb ends is sound because `rollOff` is monotonic forward, and the comment proves it rather than asserting it. Forward limbs only. A `resolve_periods` rule carries no top-level `count` and the validator enforces its absence. **All five refusals now resolve**, each from verbatim text: O.C.G.A. 9-11-36(a)(2) (re-encoded, retiring the interim two-row decomposition), Ohio App.R. 4(B)(1), CPLR 5513(c), Tex. R. App. P. 26.1(d) (ordinary appeal only — see below) and FRCP 15(a)(3). 119 → 123 rules, zero pre-existing dates changed | — |
| **SAIRNlaw** | Three `resolve_periods` rows are scoped to one variant of a rule that has several | Open (disclosed limits on new rows) | unassigned | — | Each is an explicit decision recorded in the row's own note, not an oversight. **(1) Tex. R. App. P. 26.1(d)** — limb one is "the applicable period stated above" and Rule 26.1 states FOUR (30 ordinary, 90 post-judgment motion, 20 accelerated, six months restricted). Only the ordinary 30-day variant is seeded and the rule id says so; using it where the underlying appeal is accelerated or restricted gives a wrong date. **(2) Ohio App.R. 4(B)(1)** — limb one assumes App.R. 4(A)(1); no variant is seeded for 4(A)(2) or for 4(A)(3), where late clerk service moves the trigger, even though Ohio already carries a standalone 4(A)(3) row. **(3) N.Y. CPLR 5513(c)** — limb two is the subdivision (a) period in its ordinary form; no variant for subdivision (b) permission-to-appeal, nor for 5513(a)'s own "except that" limb where the appellant served notice of entry. Next action: decide whether the missing variants are worth seeding or whether the per-row disclosure is sufficient | S |
| **SAIRNlaw** | FRCP 15(a)(3) needs a supplied date that is itself a COMPUTED DEADLINE — the sharpest case of the trigger-derivation gap | Open (linked to the trigger-derivation row above) | unassigned | — | Its first limb is "the time remaining to respond to the original pleading", which is not a period from an event but the original deadline itself; it is encoded as a **zero-day period** from that deadline, which is the faithful reading. Consequence: unlike every other rule in the engine, this row asks the caller for something that never happened in the world — a date the engine computed elsewhere and does not re-derive or validate here. Compute the original deadline first (for an ordinarily served complaint, the FRCP 12(a)(1)(A)(i) row) and feed the result in. **Whatever fix lands for the trigger-derivation row should cover this one explicitly**, because a warning is the only thing standing between a user and feeding it an event date | S |
| **SAIRNlaw** | Georgia holiday calendar is a FROZEN 2022 federal snapshot — must not be regenerated from a live federal list | Open (standing caution on the `ga` calendars) | unassigned | — | O.C.G.A. 1-4-1(a)(1) adopts "All days which have been designated **as of January 1, 2022**, as public and legal holidays by the federal government". It is a snapshot, not a live reference: a federal holiday created after that date does **not** reach Georgia through this subsection. The `ga` calendars are the eleven days in force on that date (Juneteenth included — designated June 2021). **Anyone regenerating them from a current federal list will silently add days Georgia has not adopted, pushing real deadlines LATER.** Disclosed in `sql/sairnlaw_deadline_calendars_georgia.json`. Related and not modelled: 1-4-1(a)(2) gubernatorial proclamations, which unlike the equivalent limbs in IL/IN/FL/CA/TX/NY are **guaranteed to exist every year** — the subsection requires the Governor to designate at least one of Jan 19, Apr 26 or Jun 3 or a date in lieu, so a Georgia date is correct only absent a proclamation and at least one such day always exists | S |
| **SAIRNlaw** | Service-extension SEQUENCING must be read per rule — two of three states checked did not follow the federal order | Open (pattern, watch on every new jurisdiction) | **Hank** | — | Found 2026-08-24 by a failing New York test, then fixed in Texas (`04455a5`) where it had already shipped wrong. **Georgia (`c656135`) confirmed the pattern a third time** — O.C.G.A. 9-11-6(e) also says "added to the prescribed period", and it was read up front rather than caught by a test. Final tally for the batch: **3 of 4 states lengthen the period; only Florida follows the FRCP after-expiry order.** FRCP 6(d) and Fla. 2.514(b) say days are added *"after the period would otherwise expire"* — roll, add, roll again. CPLR 2103(b)(2) and Tex. R. Civ. P. 21a(c) instead say the days are added *"to the prescribed period"* — one period, one rollover at its end. The difference was **3 days and always in the LATE direction**, which is the direction that misses a filing. Every standard now declares `sequence` explicitly and none is left on the default. **Next action is a habit, not a task:** when seeding any new jurisdiction, read the service-extension rule's own words for expiry-vs-period language before assuming the federal order — and re-read `ca_ccp_1013_1010_6`, which is declared `roll_then_add_then_roll` to preserve shipped behaviour but was **not** re-verified against CCP 1013's actual wording during the New York work. **WEST VIRGINIA (2026-08-25) SHOWED THE HABIT IS NOT ENOUGH ON ITS OWN, TWICE.** (1) Its sequencing **changed over time**: the former R. Civ. P. 6(e) said three days "shall be added **to the prescribed period**", the restyled 2025 rule says three days "are added **after the period would otherwise expire** under Rule 6(a)". First jurisdiction here where the wording flipped, so it has to be read per **date**, not only per state. (2) The same state sequences its two extensions **opposite ways at once** — civil 6(e) is after-expiry, appellate R. App. P. 39(c) says "shall be added to the prescribed period". So it also has to be read per **body of rules**. Running tally across states checked: 3 of 5 lengthen the period; only Florida and post-2025 West Virginia civil follow the FRCP after-expiry order | S |
| **SAIRNlaw** | Deadline engine cannot version a rule by the date the CASE WAS FILED, only by the date the period runs from | Open — **structural limit, disclosed not fixed** | unassigned | Needs a schema decision, not a patch | Found seeding Texas 2026-08-24. Tex. Sup. Ct. Misc. Docket No. 20-9153 amended TRCP 47, 99, 169, 190, 192–198 effective 2021-01-01 but keys applicability to filing: *"The amendments apply to cases filed on or after January 1, 2021 ... The rules amended by this Order continue to govern procedures and limitations in cases filed before January 1, 2021."* `computeDeadline()` selects a version with `effective_from <= <date the period runs from>`, and there is no `case_filed_on` input. **So a Texas case filed in late 2020 whose service or discovery happened in 2021 silently gets the post-2021 row — the wrong version, with no refusal.** Not Texas-specific: any jurisdiction whose amendment order keys to filing rather than to the trigger has the same hole, and none of the other 8 were checked for it. **West Virginia is a confirmed second instance, found 2026-08-25 and this time read up front rather than discovered:** R. Civ. P. 86 says the rules "govern all proceedings in cases thereafter commenced and insofar as just and practicable, all proceedings then pending", and R. App. P. 1(e) keys the appellate rules to orders **entered** after 2022-06-30. So a WV case commenced in 2024 whose trigger falls in 2025 silently gets the restyled row — and the restyled rules changed both the short-period exclusion and the service-extension sequencing, so the wrong version is not a cosmetic difference there. Next action: audit the other 8 jurisdictions' amendment orders for filing-date keying, then decide whether to add an optional `case_filed_on` that narrows rule selection and refuses when it is absent but a filing-keyed rule is in scope. Disclosed in `sql/sairnlaw_deadline_seed_texas.json` readme | M |
| **SAIRNlaw** | Deadline engine accepts a trigger date as given even where the law makes that date itself a COMPUTED one — no validation, no warning in the returned date | Open — **highest-consequence input gap in the engine** ⚠️ | unassigned | — | Found seeding New York 2026-08-24; flagged in the seed readmes but it is not a New York problem. `computeDeadline()` never validates what a trigger date means — it counts from whatever it is handed. **Sharpest instance: CPLR 308.** Both `ny-cplr-320a-appearance-30-service-complete` and `ny-cplr-3012c-answer-30-service-complete` run 30 days from when *"service is complete"*, and for CPLR 308(2) leave-and-mail and 308(4) affix-and-mail that is a computed date — a fixed number of days after proof of service is **filed**, often more than a week after delivery. CPLR 308 was deliberately not read verbatim for that seed, so no completion arithmetic is encoded and none is guessed. **A user supplying the delivery date gets a deadline that is too early; supplying the filing date gets one that is too early by a different amount. Neither draws a refusal.** The same class already spans the engine: Florida's `rendition` (a term of art, not the signature or mailing date), California's Rule 8.104 service-of-notice-of-entry limbs, Texas's `signing of the judgment`, New York's `service ... with written notice of its entry`. Four jurisdictions now start an appeal clock four different ways and nothing stops a caller conflating them. **West Virginia makes a fifth (2026-08-25):** R. App. P. 5(b) runs from `entry of the final order OR MANDATE`, and 5(f) from `the date the judgment ... was entered` — so within one state the notice-of-appeal clock and the perfect-the-appeal clock can start from two different documents, and neither is derived. Next action: decide between (a) encoding CPLR 308's completion arithmetic as a real computation so the engine derives it, and (b) a `trigger_requires_derivation` flag on such rows that makes the endpoint surface a named warning alongside the date rather than returning a bare one. (b) is cheaper and covers all four jurisdictions; (a) only fixes New York. Do **not** close this by adding prose to the seed notes — those are already there and are invisible at the point the date is read | M |
| **SAIRNlaw** | Two deadline-rule authorities rest on secondary sources, recorded rather than silently trusted | Open (accepted, documented) | unassigned | — | Both found seeding Texas 2026-08-24 and both are **disclosed in the seed/calendar readmes**; neither is load-bearing, which is why they are accepted rather than blocking. **(1) Proctor v. Green, 673 S.W.2d 390 (Tex. App.—Houston [1st Dist.] 1984)** — CourtListener confirms case, court, docket `01-84-73-CV`, date and citation but serves no accessible full text, and Leagle/vLex refused automated access; the holding and its worked dates come from a legal database's rendering, not a primary source read this session. The rule row cites **Tex. R. Civ. P. 99(b)**, not the case, and the same result follows from the rule's own words, so nothing computes off it. **(2) Tex. R. Civ. P. 4 and Tex. R. App. P. 4.1 both say "legal holiday" without defining it and without citing Tex. Gov't Code 662.021** — the link to 662.021 (which is what the `tx` calendars encode) is the settled reading, not an express cross-reference, and was not found stated in either rule's text. Next action: if a lawyer's read is ever commissioned for the Illinois holiday row above, add both of these to the same question set | S |
| **SAIRNlaw** | Court e-filing: readiness check built, transmission not possible | Closed as scoped | — | EFSP certification is a contract, not code | Nothing to build. Revisit only if an EFSP relationship is pursued | — |
| **StoneDesk** | ~28 `sdDemoCleared() ? [] : SEED` fallback sites unaudited (Slabs fixed in `501d15b`) | Open ⚠️ | unassigned | — | Count is `grep -c` measured, not authoritative — Guardian's note says 29 constants / 56 sites. **Re-count precisely first**, then apply the two-part test per site | L |
| **StoneDesk** | Procedural stone texture likely inflates `canvas.toDataURL()` PNG snapshots against an existing quota risk | Open | unassigned | — | Measure a real textured snapshot's byte size before changing anything | M |
| **StoneDesk** | Saved-quote Load: overwrite confirm fires on a merely-opened Drawing Tool tab | Open | unassigned | — | Gate the confirm on actual unsaved drawing state | S |
| **StoneDesk** | `dcMode` doesn't reset to `preset` on a same-session Custom-Draw-then-preset quote Load | Open | unassigned | — | Reset `dcMode` in the Load path | S |
| **StoneDesk** | Stone-texture visualization — accepted cosmetic nits | Open (accepted) | unassigned | — | None unless a client raises one | S |
| **SAIRNbiz** | AP "Pay" button doesn't mark anything paid | Open | unassigned | — | Real status write + server sync | M |
| **SAIRNbiz** | No way to update a training cert's status at all | Open | unassigned | — | Add status transition + persistence | M |
| **SAIRNbiz** | Budget "actual" spend never syncs with recorded expenses | Open | unassigned | — | Derive actuals from the expense store rather than a separate field | M |
| **SAIRNbiz** | Payroll runs are never recorded anywhere | Open | unassigned | — | Persist a payroll-run record | M |
| **SAIRNbiz** | Benefits panel has no way to enroll anyone | Open | unassigned | — | Enrollment write path | M |
| **SAIRNdental** | Real-sync sweep can silently exhaust localStorage once photo-bearing bookings accumulate | Open | unassigned | — | Quota guard + honest failure. Silent-failure class — treat as higher priority than its size suggests | M |
| **SAIRNdental** | `public-book.js` returns a generic 502 on a real slot race instead of 409 `SLOT_TAKEN`, and orphans a patient record | Open | unassigned | — | Distinguish the race, return 409, clean up the orphan | M |
| **SAIRNdental** | Pediatric guardian fields: `onPtDobChange()` not called at page init | Open | unassigned | — | Call it on init | S |
| **SAIRNdental** | Per-patient photo history panel | Open (feature) | unassigned | — | Scope before building | M |
| **SAIRNdental** | Vendor/supply ordering — deferred whole-branch-review items | Open | unassigned | — | Re-read the review list, triage | M |
| **Tooling** | Rebuild graphify's knowledge graph, properly scoped | Open | unassigned | — | Decide scope first; the last attempt was unscoped | M |
| **Platform** | `C:\Users\marsh\` is itself a working-tree checkout of the repo, 33 commits behind | **Open — PLAN WRITTEN 2026-08-25, awaiting approval** | **Michael** | One decision in Step 4 (delete the 428 orphaned files or keep them) | **Read `docs/PROPOSAL-retire-home-checkout-2026-08-25.md`** — concrete 8-step plan, nothing run. **It is not a stray folder inside the home directory; it *is* the home directory.** Measured: **451 tracked files** (the whole repo — `stonedesk.html`, every app HTML, `api/`, `sql/`, `docs/`), **477 untracked entries** (i.e. `Documents\` with all four real clones, `Downloads\`, `Desktop\`, `AppData\`), **0 unpushed commits** so nothing would be lost, 18 MB `.git`, 428 orphaned files (~11 MB) left loose if `.git` alone is removed. **Three reasons it is worth doing:** (1) **`git clean -xfd` run there deletes all four working clones** — everything under `Documents\` is untracked-and-not-ignored inside this tree; (2) it is the root cause of the skill-store exposure — `~/.claude/skills/` *is* this checkout's tracked `.claude/skills/`, holding 20 `SKILL.md` files at a 33-commit-old revision, so a `git restore` there overwrites the 21 symlinks; (3) **it is already causing wrong behaviour** — `C:\Users\marsh\CLAUDE.md` loads as project instructions for every session under the home dir, is 33 commits stale, still says *"all 26 sairn-guardian-v2 checks"* against the repo's 30, and **this session read it**. A spot-check of 8 tracked files there found 5 behind current `main`. **Also added 2026-08-25:** the second skill store `~/.agents/skills/` (17 skills, 11 symlinked into `~/.claude/skills/`) is inside this same tree — retiring the checkout fixes that as a side effect, but those 17 still live in one place on one machine | M |
| **Tooling** | **Skills backup: 29 global-only skills now archived in-repo at `docs/skill-backups/`** | **Done 2026-08-25** — byte-verified | Cody | — | Found by the skills ground-truth audit: 29 skills existed **only** on this machine, in no repo, with no backup. Now committed at **`docs/skill-backups/`** — deliberately *not* under `.claude/skills/`, so it reads as an archive and is never loaded. **Verified: 29 skills, 190 files, 3,447,054 bytes, identical on both sides, zero mismatches.** The tracked `sairn-*` set is **excluded on purpose** — it is already version-controlled, and copying it would create the second silently-diverging copy this audit existed to find. **Read `docs/skill-backups/README.md` before restoring:** 11 of the 29 are **symlinks** into `~/.agents/skills/` and are archived as dereferenced content, so a naive `cp -r` restore detaches them from that root. **Verification requires `find -L`** — plain `find` does not follow those symlinks and reports 0 bytes, which looks exactly like a failed backup (a real false alarm hit during this audit). **It is a snapshot, not a sync**, and nothing detects drift automatically | S |
| **Tooling** | **`sairn-grant-sweep` skill written — the grant-sweep discipline now has a home** | **Done 2026-08-25** | Cody | — | Closes the gap the skills audit found: no skill encoded *excess*-privilege work, and `sairn-infra-debugger` actively taught the overcorrection that caused it (now fixed). New skill at `.claude/skills/sairn-grant-sweep/`, 223 lines, built directly on the three real files as worked examples (`append_only_grant_audit.sql`, `full_crud_truncate_sweep_2026-08-24.sql`, `unused_delete_grant_revoke_2026-08-24.sql`). Encodes: the four-section shape (baseline → discover → fix → verify); **list-free discovery** from `information_schema`, never a hand-built list; the **revoke-then-grant** idiom that preserves exactly what each table held; the **real baseline table, not a temp one** (the Supabase editor does not guarantee two Run clicks share a session, and a vanishing temp table takes the only proof with it); **full-outer-join verification labelling `LOST` and `GAINED`**, not merely "the target is gone", plus the lower-bound guard that stops a vacuous pass; the full **R1–R6** checklist with the `aclexplode` correction to R4; the **sequencing rule** (one sweep per window — a second sweep's intended changes appear in the first's verify as `LOST` rows); the "grant only the verbs the code calls" rule with the `grep` that proves it; and the counting-discipline traps (truncated exports, repo lines ≠ live rows, 227-vs-214, arithmetic fit is not evidence). Verified live: registered and loadable through the new symlink | S |
| **Tooling** | Skill-store drift has no detector — the stale copy is the one that loads | Open ⚠️ — **root cause of two findings on 2026-08-25** | unassigned | — | `sairn-guardian-v2` sat at **28 checks** in `~/.claude/skills/` while the committed copy had **30**, and `sairn-app-scaffold` at **1** required component vs **2** committed. Both stale copies were the ones actually loaded — the session's own skill listing advertised "28 numbered checks", so checks 29–30 (including the `RESEND_FROM_ADDRESS` env-drift check) had never been read in a session since being committed 2026-08-24. A leftover `SKILL.md.pre-sync.bak` showed a partial sync that ran and was never finished. **RESOLVED 2026-08-25 — durable fix applied, drift is now structurally impossible.** All 20 (now 21) `sairn-*` entries in `~/.claude/skills/` are **native symlinks** to `.claude/skills/` in `Documents\SAIRN-cody`, matching the pattern `~/.agents/skills/` already used for 11 others. There is now one copy, so there is nothing to diverge. Pre-flight confirmed all 20 global copies were content-identical (CRLF-normalised) before replacement, and the repo copies matched `origin/main`; a safety copy was taken at `~/.claude/skills-pre-symlink-2026-08-25` and can be deleted once this has run a few sessions. Global store is now 50 entries: 32 symlinks (21 `sairn-*` + 11 `~/.agents/`), 18 real dirs. **MOVED OFF THE WORKING TREE 2026-08-25, same day.** The first version pointed the symlinks at `Documents\SAIRN-cody`, which made every skill depend on a live git checkout. Canonical content now lives at **`C:\SAIRN\skills\sairn\`**, verified outside every git working tree, and the 21 `~/.claude/skills/sairn-*` symlinks point there. Registration proven end-to-end through the new chain with a temporary probe skill, which the harness picked up and which has been removed from all three locations. **`~/.agents/skills/sairn/` was tried first and rejected on evidence:** the *entire home directory* is inside the stale `C:\Users\marsh\` checkout — `git -C ~/.agents/skills rev-parse --show-toplevel` prints `C:/Users/marsh`, and `git clean -xdn` there prints *"Would remove .agents/skills/sairn/"*. Using the obvious precedent would have reproduced the exact anti-pattern the move existed to escape. **The repo copies were deliberately NOT symlinked**, against the literal instruction, because it would have been destructive: git stores a symlink as mode `120000` whose blob is the absolute target path, and this machine has `core.symlinks=false`, so a fresh checkout writes a *one-line text file* instead of a directory — deleting 21 skills' tracked content, hardcoding a machine-local path into the repo, and breaking `.claude/skills/` in all four clones. Verified in a scratch repo before choosing. The repo copy therefore stays real and tracked, and the two-copy drift it re-introduces is covered by **`scripts/verify-skill-store.sh`** (read-only, negative-tested, exit 1 on drift). **STILL EXPOSED and not fixable from here:** `~/.claude/skills/` *is* the stale checkout's tracked `.claude/skills/` — the user-level skill store and that checkout are the same path, so a `git restore` there would overwrite the symlinks with 33-commit-old content. That is loud and cheap to rebuild; canonical content is not, which is why it moved out. **Only retiring that checkout closes it** — see the row above | M |
| **Platform** | `api/sd-data.js` returns all 171 resource names to any caller with a junk bearer token | Open — minor disclosure, **deliberately not fixed** | unassigned | Needs an auth-ordering decision, not a patch | **Verified live 2026-08-24**, no credential used: `POST /api/sd-data` with `Authorization: Bearer not-a-real-key` and an unknown resource returns 400 listing every registered resource, because the resource gate (`sd-data.js:190`) runs *before* license validation (`:221`). Low severity — names only, no data, and every path past the gate still 401s. Moving license validation above the gate changes the first-failure response for **every app**, so it needs a deliberate call and the same baseline-replay proof as the verb-gate change, not a quick reorder. Note it also makes that baseline capturable without credentials, which is a real testing benefit to weigh against fixing it | M |
| **Platform** | `service_role` holds TRUNCATE/REFERENCES/TRIGGER/MAINTAIN (Postgres default ACL, confirmed live via `pg_default_acl`) on full-CRUD tables **beyond** the append-only-by-design set already fixed in `sql/append_only_grant_audit.sql` | **Diagnosed, draft fix written, not run** | unassigned | — | `sql/full_crud_truncate_sweep_2026-08-24.sql`: Section 1 is the real list-free discovery query (`information_schema`, not a hand-built table list); Section 2 is a DO-block draft fix that reads each table's own current SELECT/INSERT/UPDATE/DELETE grants and re-asserts exactly that set, so it cannot add or remove functional capability, only strip the unused TRUNCATE/REFERENCES/TRIGGER/MAINTAIN baseline. 151 of 158 granted tables (counted, not estimated) lack the sound `revoke all` pattern and are code-level candidates; Section 1 confirms which ones actually carry it live. StoneDesk slabs (`sd_slabs`/`sd_blocks`/`sd_bundles`/`sd_slab_history`) and sampled SAIRNgrounds/MSB tables checked directly in `api/sd-data.js`, not assumed from this row's own earlier wording: all genuinely use UPDATE (real `on_conflict` upserts), none ever get a DELETE (platform-wide, exactly one `method: 'DELETE'` call exists anywhere in that file, and it's the SC_RESOURCES/SAIRNcode branch). Separate finding, deliberately not bundled into the fix: most full-CRUD tables are granted DELETE at the DB level with no live code path that ever uses it — narrowing that would be a real functional-capability decision, not a strip-the-unused-default fix, and needs its own review | L |
| **Platform** | `service_role` is granted DELETE at the DB level on StoneDesk's slab tables (`sd_slabs`/`sd_blocks`/`sd_bundles`/`sd_slab_history`) and on SAIRNgrounds/MSB's tables, with **zero live code path that ever issues one** | Open — **found 2026-08-24, deliberately its own row** | unassigned | — | Confirmed by direct grep, not inferred: exactly one `method: 'DELETE'` call exists anywhere in `api/sd-data.js`, and it's the SC_RESOURCES (SAIRNcode) Compliance-Admin-gated branch — every other DELETE-granted table, including these, has no caller. Surfaced while diagnosing the broader TRUNCATE sweep (see the row above) but is a **different kind of excess**: narrowing an unused DELETE grant changes real functional capability (a future code path could no longer delete a row even if someone wanted one), unlike stripping TRUNCATE/REFERENCES/TRIGGER/MAINTAIN, which no schema file ever intentionally granted anywhere. Decide first whether DELETE should ever be reachable on these tables at all before writing any revoke | M |
| **Platform** | 19 bare-named tables carrying the excess grant baseline are reachable by **no code path at all** — candidates to drop, not just to narrow | Open — **found 2026-08-25, code-verified** | unassigned | — | `conversations`, `demo_calls`, `gl_entries`, `licenses`, `messages`, `parts`, `payments`, `profiles`, `projects`, `shop_users`, `shops`, `subscriptions`, `usage_logs`, `user_storage`, `webhook_events` appear nowhere in `api/` and have no `create table` in `sql/`. `customers`, `invoices`, `jobs`, `slabs` are the trap: they **are** registered resource keys, but every branch maps them to a **prefixed** table (`scp_customers` `sd-data.js:1356`, `scp_invoices` `:1440`, `grd_jobs` `:690`, `sd_slabs` `:508`) and `sd-data.js:1355` says the prefixed names were chosen "specifically to avoid the collision" — the bare tables are never addressed. Enumerated exhaustively, not sampled, across **four** channels — 107 `rest('literal')` constants, 21 `TABLE` consts, 12 `rest(resource + …)` sites (every one keyed on a map of prefixed names), **and the browser going straight to Supabase on the anon key**. That fourth channel was a real gap in the first pass, caught by cross-checking against SAIRN-cc's independent sweep; re-running with it included returns exactly two client-side tables platform-wide (`employees`, `intake_submissions`) and leaves the 19 intact. **Reached independently by SAIRN-cc (`dd5327b`) by a different route, same 19 names.** Two corrections to that block, both evidenced and both recorded in the SQL file rather than fixed in place: `network_insights` is **not** grant-broken (three live 200s), and `intake_submissions` is **not** unreferenced (`stonedesk.html:31943`/`:31982`). Very likely the abandoned Fabricor-era schema. Next action: **confirm each is empty and unreferenced before proposing any drop** — dropping a table is a schema owner's call, not a session's, exactly as the `canlii_rate_limit_log` routing worked | M |
| **SAIRNsenior** | **Six migrations were never run against production, and the app has been failing quietly since 2026-08-20** | Open — **found 2026-08-25, live-proven** | **Michael** | Needs Supabase SQL editor + a provisioned `SEN-` license | `sen_caregivers`, `sen_claims`, `sen_clients`, `sen_portal_links`, `sen_visits`, `sairnsenior_employee_auth` all have real schema files dated 2026-08-20 and **do not exist in the database**. Proven live, no credential used and nothing written — `api/sen-portal.js`'s `view` action is deliberately auth-free (`:92-95`) and a bogus token cannot reach the `last_accessed_at` write at `:107`: `POST /api/sen-portal {"action":"view","token":"aaaa…"}` → **503 `NOT_PROVISIONED`**, which is the 42P01/PGRST205 branch at `:100`; a table that exists returns 404 `INVALID_LINK` at `:104`. Corroborated by the build sessions' own logs at the time — `SAIRN-ACTIVE-WORK.md:153` "both new migrations still queued, not run", `:155` "a third migration queued, none run yet", never reversed. Same failure mode as `sairncash_waitlist` / `sairnscape_org_intel` (`6776f99`): schema file in the repo, migration never run. Every endpoint degrades honestly rather than crashing, which is why nobody noticed for five days. **Next action:** run the six migrations and provision a `SEN-` demo license, then the owner-bootstrap → caregiver-scoped-visibility round trip Hank's log lists as blocked. Deliberately **not** done as part of the grant sweep — running another app's migrations as a side effect of a privilege audit is the scope creep `6776f99` refused | M |
| **Platform** | ~~The 227-table grant export was truncated a second time~~ | **RETRACTED 2026-08-25 — the inference was wrong, the export was right** | — | — | Claimed 11 tables were missing because they were code-reachable, had schema files, and appeared to use the unsound grant pattern. **Method error:** each table's *own* schema file was read and the check stopped there; no check was made for a *later* file having already fixed it. One had — `sql/append_only_grant_audit.sql` explicitly `revoke all … from service_role` on `dnt_credentials` (`:118`), `dnt_cred_rules` (`:120`), `alf_staff_credentials` (`:124`), `alf_signals` (`:126`), `alf_claim_routes` (`:128`), and it was run live (`6776f99` reports a real 42P01 from it). **Corroborated by the export itself:** that file revokes on **nine** tables — those five plus `sairnlaw_audit_log`, `rf_photos`, `rf_jobs`, `sairnroofing_employee_auth` — and **all nine are absent, 9 for 9**. A truncation does not select exactly the nine rows a known REVOKE touched. The other six are the SAIRNsenior row above. **Still unresolved and minor:** the pasted list enumerates to 215 against a 227 label, but the raw `jsonb_agg` result was offered and the message carried the placeholder `[same JSON dataset as above]` instead of the data, so it could not be recounted; likeliest reading is a shorthand family size off by a few. Do not treat 215 as an export defect on the strength of this row | — |
| **SAIRNlaw** | `CANLII_API_KEY` | **Closed** | — | — | No longer a blocker — Canada is out of scope and the code is deleted | — |

### Added by the handoff-reading pass, 2026-08-24

Everything below came out of the ~40 per-app handoffs and was **re-verified
against current code before being listed** — several handoff claims turned out
to be stale, and those are in *Contradictions* rather than here.

| App | Item | Status | Owner | Blocked by | Next action | Sz |
|---|---|---|---|---|---|---|
| **SAIRNdental** | `dnt_appointments` still carries a DB-level 64 KB `CHECK` (`dntap_data_size`) while photo capture ships ~900 KB per booking | **Open — live risk** ⚠️ | unassigned | — | **Verified still present** at `sql/sairndental_data_schema.sql:80`, `octet_length(data::text) <= 65536`. SAIRN-PLATFORM-SESSION3 called this "the single most important thing to pick up next, before this reaches a real practice" and the migration was never written. Confirm empirically whether the constraint bites on the live table, then raise it — do not degrade photo quality to fit it | M |
| **SAIRNcare** | ~~Resend sending domain `sairn.com` is not verified — alert emails are refused `403`~~ | **CLOSED 2026-08-24 — confirmed delivering** | — | — | `RESEND_FROM_EMAIL` moved to `alerts@notifications.sairntech.com` and redeployed. Proven by a provider-issued id in the production log, not by absence of the error: `alf-alerts: Resend send OK … resend_id 69b4138e-58e9-4105-9496-c853e91f7e0a` (11:00 UTC), repeated from `c6103691-…` (10:01 UTC). **Root cause was not a config oversight** — `sairn.com` is registered to a third party (Porkbun, NS `ns1/ns2.atom.com`, an Atom.com marketplace listing) and could never have been verified; `sairntech.com` is the owned domain (NameCheap, exp. 2028-06-09). SAIRNdental's identical `RESEND_FROM_ADDRESS` bug closed the same day (`d2a3382`), also with a real `resend_id` | S |
| **SAIRNcare** | PA ALR: "max 2 residents per living unit" and the cognitive pre-admission screening window unverified | Open | unassigned | Needs a primary Ch. 2800 source | Encode only from a positive source. **Do not carry the PCH equivalents across** — those are confirmed, ALR's are not, and substituting is the exact error the handoff flags | S |
| **SAIRNcare** | PCH "no RN or dietitian required" recorded as unverified, deliberately | Open (accepted) | unassigned | — | Asserting a negative from an absence in one chapter is a compliance claim. Needs a positive source or it stays unencoded | S |
| **SAIRNcare** | `ALF_PHARMACY_SECRET` is set and `api/alf-pharmacy.js` is live, but no real pharmacy is connected | Open | unassigned | Needs a pharmacy partner | Nothing to build until there is one to integrate with | M |
| **SAIRNvet** | 20 panels never audited | Open | unassigned | — | Panels confirmed to exist. Start with `panel-staff`. Money-touching panels are all done; these are clinical/operational, so watch for fake success toasts and non-functional buttons rather than dollar fabrication | L |
| **StoneDesk** | 163 missing DOM targets, never triaged | Open ⚠️ | unassigned | — | **Re-measured: 163 of 1,425 distinct targets, not the 184 the handoff recorded.** Pre-existing baseline, never individually traced. Triage by hand | L |
| **StoneDesk** | `panel-remnant` double-render: `sdRemnantRender()` and `remRender()` both live, two containers | Open | unassigned | — | **Verified still real.** Search/filter inputs call only `remRender()`, so typing updates the grid and leaves the table stale | M |
| **StoneDesk** | Two parallel template modules with different storage keys | Open | unassigned | — | **Verified still real** — `sd_templates` and `sd_template_records` both present. Check which one `panel-templates` actually renders from **before** merging; the wrong direction silently orphans real user data | M |
| **StoneDesk** | 2 `stonedesk-demo` license fallback sites, each its own expression | Open | unassigned | — | Was 3, now 2. Collapse into one shared helper so they cannot diverge from `sdData()`'s identity resolution | S |
| **StoneDesk** | Chamfer setback input does not live-refresh when a run is shrunk after typing | Open (display only) | unassigned | — | Pricing and the cut sheet read the live-clamped value, so this is staleness, not a money bug | S |
| **StoneDesk** | Three drawing-tool feature gaps: no 45° corner angle-snap; raised bar cannot combine with L/U-shape; canvas fixed at 480px | Open | unassigned | — | All three re-confirmed in Session 79 and untouched since. Missing precision features, not paths to a bad cut | M |
| **StoneDesk** | Worktree branch `worktree-stonedesk-chamfered-corners` is five features past its name | Open | unassigned | — | Carried unactioned from Sessions 81–84. Rename or retire | S |
| **StoneDesk** | Dormant `custAddNew()`/`custSave()` form missing the `rating` field | Open (quarantined) | unassigned | — | Quarantined by explicit direction 2026-07-28. If anyone wires a button to it, it creates records missing `rating` | S |
| **StoneDesk** | Four `safety*` render functions have no containers and no callers | Open (accepted gap) | unassigned | — | Real feature-completion work (3–4 UI sections + nav), not a mechanical fix. Accepted by explicit direction 2026-07-28 | M |
| **StoneDesk** | `#rm-causes` permanently empty since the old IIFE render was removed | Open (cosmetic) | unassigned | — | No crash, no data risk | S |
| **StoneDesk** | ~40 lower-frequency `sd_*`/`sh_*` storage keys never traced for the two-system collision pattern | Open | unassigned | — | The pattern was real for `sd_customers`, `sd_inventory`, `sd_remakes`. Trace the rest | L |
| **SAIRNbiz** | `syncEmps()` — payload reshaped (`16fbb31`), but blockers 2 and 3 not confirmed closed | Open ⚠️ | unassigned | Needs live Supabase | **Could not verify from code** — needs a real round-trip against the live project. Re-read SAIRNBIZ-SESSION1 §4 before assuming the fix landed | M |
| **SAIRNbuild** | AI Budget Early Warning: `f072765` fixed 5 review findings and was never independently re-reviewed | Open | unassigned | — | Self-review only. A genuinely separate pass before calling it done | S |
| **SAIRNbuild** | Never live-verified against `sairn.vercel.app/sairnbuild` | Open | unassigned | — | `git push` succeeded; no curl or browser check was ever run for that feature | S |
| **SAIRNdental** | Vendor ordering never live-browser verified | Open | unassigned | — | Every check was a hand-traced code walkthrough. The handoff calls a real browser session the single highest-value next step | M |
| **SAIRNdental** | SDD workspace and `worktree-sairndental-vendor-ordering` never cleaned up | Open | unassigned | — | Commits are already fast-forwarded onto `main`; only the branch and workspace remain | S |
| **StoneDesk** | Several SDD workspaces under `.superpowers/sdd/` never cleaned up | Open | unassigned | — | Carried across Sessions 80, 83, 84 | S |
| **Tooling** | Handoff reminder before context loss — a `PreCompact` hook, dropped tonight and worth rebuilding later | Open (low priority) | unassigned | — | Found 2026-08-25 in all four stashes on the retiring `C:\Users\marsh\` checkout, and **not** present in the live `~/.claude/settings.json`. The hook fired on `PreCompact` and injected a reminder to write a handoff before compaction erased working context. **Dropped rather than restored** because its text names `SAIRN-SESSION-N-HANDOFF.md`, the counter-based convention CLAUDE.md corrected on 2026-08-23 — restoring it would reintroduce guidance that already failed in production when two real `SAIRNLAW-SESSION6-HANDOFF.md` files existed at once. Patch preserved at `C:\SAIRN\backup-2026-08-25\salvage\stash-{0..3}.patch`. **Not urgent, and there is evidence it may not be needed:** tonight's session died to a terminal crash with no handoff written, and nothing was lost — the work was recoverable from staged changes plus `docs/SAIRN-OPEN-WORK-INDEX.md`, which is the design working without the hook. **Next action if rebuilt:** use the `APP-YYYY-MM-DD-subject-handoff.md` naming and the commit-in-the-same-action rule from `sairn-session-handoff`, not the old text | S |
| **Platform** | No redaction check before writing a handoff or any file | Open | unassigned | — | The `sairn-session-handoff` skill calls for one; no hook exists. A real GitHub PAT was involved in a prior session | S |
| **Platform** | `core.autocrlf` produces false-positive deploy-mismatch hook fires | Open | unassigned | — | Set `core.autocrlf=false` or `input` for this repo, so a real stuck-webhook incident is not misdiagnosed as line endings — or vice versa | S |
| **Platform** | **No `.gitattributes` — nothing enforces a line-ending convention, and blobs are silently flipping** | **Open — ready to apply, needs a quiet window** | unassigned | **Every active session must be between tasks, not mid-edit** | **Confirmed 2026-08-24: no `.gitattributes` exists anywhere in the repo**, and `core.autocrlf=true` locally, so each contributor's toolchain decides the stored convention. This is not theoretical — `api/sd-data.js` flipped from pure CRLF (4653 lines) to pure LF (4828) in `4886658`, which read as **+4828/−4653** while `git diff -w` read **+175/−0**; a later 24-line change rebased into a ~9,000-line whole-file conflict because of it. **Decision needed before applying:** `* text=auto` (one rule, git normalises everything to LF in the blob) **vs** explicit per-type rules (`*.js text eol=lf`, `*.html text eol=lf`, `*.sql text eol=lf`, `*.md text eol=lf`, binaries `-text`). Prefer explicit per-type — `text=auto` guesses on unknown extensions and this repo has `.esx`-adjacent and generated artifacts. **Applying it WILL rewrite blobs repo-wide** (one large normalising commit, plus `git add --renormalize .`), so it must land when no session has uncommitted work — otherwise it recreates exactly the conflict it prevents. Pairs with the `core.autocrlf` row directly above; do both in the same window | M |
| **Platform** | `.agents/` untracked pile at repo root, never examined | Open | unassigned | — | Very likely tool/plugin cache, not SAIRN content. Confirm what wrote it before `.gitignore`-ing or deleting | S |
| **SAIRNcash** | Stripe not configured — `api/sairncash/checkout.js` returns `{"error":"Stripe not configured"}` | Open | **Michael** | Needs `STRIPE_SECRET_KEY` / `STRIPE_PRICE_ID` in Vercel | Expected, not a bug — Stripe setup was planned separately. Blocks the estimator panel's final live-subscriber regression test | S |
| **SAIRNroofing** | Contingency rescission: only Ohio is seeded, and SAIRNlaw's real holiday calendar is not wired in | Open | unassigned | — | Two separate gaps, both disclosed rather than hidden. (1) CO/FL/TX are researched and recorded in `sql/sairnroofing_contingency_seed_ohio.json` under `_states_researched_but_NOT_seeded` but not loaded — each needs its own verification pass against current statute text before it renders as a date. (2) A rule on the generic `mon_fri` basis applies NO holiday calendar and says so on every result; SAIRNlaw already owns a real per-jurisdiction calendar (`sql/sairnlaw_deadline_calendars_*.json`) and wiring it in would remove that disclosure. Ohio needs neither — its statute enumerates its own eleven holidays and they are applied exactly | M |
| **SAIRNroofing** | The claim assignment gate is now written four ways across `api/sd-data.js` | Open | unassigned | — | Phase 5 factored it into one `rfClaimGate` helper and used it for the three new branches, but deliberately did NOT retrofit the live 3b/3c branches (rf_claim_photos read/write, `reconcile`) — rewriting verified live code inside a feature commit is how a regression gets in unnoticed. `api/rf-auth.js`'s own header names duplicated role logic as SAIRNsenior's root cause, so this should not sit indefinitely. Retrofit + re-run the 3b/3c live round trips together | S |
| **SAIRNroofing** | Two sibling resources return their primary key under different field names | Open | unassigned | — | Found live during the Phase 3c round trip, not from reading code: `rf_claims` read returns `claim_id`, but `rf_jobs` read returns the job id as **`id`**, not `job_id` — even though the stored column is `job_id` and every write payload sends `id`. Not a bug today (nothing reads the wrong one) and **not** folded into 3c, which is why it is a row and not a commit. Decide one convention before a third resource copies whichever one it happened to see first | S |

---

## Contradictions found by the handoff-reading pass

Flagged, **not silently resolved**, per instruction. Each was re-verified
against current code or git before being called stale.

| Claim | Where | What is actually true now |
|---|---|---|
| "`main` is 47 commits behind `master`" — carried as an unresolved gap across SAIRNvet Sessions 60, 61 and 62 | SAIRNVET-SESSION60/61/62 | **Inverted.** `origin/main` is **1,142 commits AHEAD** of `origin/master`; master is 1 ahead of main. `master` is the stale branch, which is what `CLAUDE.md` says. The handoff series repeated a wrong claim three times. Nothing to merge or re-point |
| "Guardian v2 skill file found at 25 checks despite being logged as 33" | SAIRNVET-SESSION59, carried to 60/61/62 | **Resolved, then drifted AGAIN — updated 2026-08-25.** This row said "28 numbered checks", which was true of the copy that was loading and false of the committed one (**30**, since 2026-08-24). `CLAUDE.md` separately said **26**. Three live numbers at once, exactly the pattern this row was closed on. Now synced to 30 and `CLAUDE.md` corrected — but **stop recording the count here.** Read `## The N Checks` in the skill itself; it is the only source that moves when a check is added |
| "`div_balance_check.py`'s pre-existing `DIFF:-4` / line 5092 underflow on StoneDesk" | STONEDESK-SESSION78 | **Resolved.** `stonedesk.html` now measures 4,747 open / 4,747 close, `DIFF:0`, PASS |
| "184 missing DOM targets on StoneDesk" | STONEDESK-SESSION78 | **Still open but the number is wrong** — re-measured at **163** of 1,425 distinct targets |
| "3 `stonedesk-demo` fallback sites" | STONEDESK-SESSION77 §4.2 | **Still open, now 2.** Reduced but not eliminated |
| "Resend vars not reaching Production — Michael action item, not a code fix" | SAIRNCARE-SESSION2 §5.1 | **Diagnosis was wrong**, and the later SAIRNcare handoff says so itself: the real cause was a code-side name mismatch (`RESEND_FROM_ADDRESS` vs `RESEND_FROM_EMAIL`), now fixed for SAIRNcare. A *second*, separate problem — the unverified sending domain — is what remains, and that one genuinely is Michael-only |
| "Two colour collisions: SAIRNhr/SAIRNvet `#7C3AED`, SAIRNcare/SAIRNacc `#0D9488`" | SAIRN-SESSION68 §4.4 | **Moot.** Guardian's App File Map removed SAIRNhr and SAIRNacc on 2026-08-13 as speculative entries that were never real apps, which resolves both collisions by elimination |
| "No delete capability via `api/sd-data.js` for **any** resource, any app" | This index's own row, carried from `SAIRN-BACKLOG.md` | **Overstated.** `delete` is implemented and gated for the 28-resource `SC_RESOURCES` family (`sc_denial`, `sc_claims`, `sc_settings`, …). It is genuinely absent everywhere else — `dnt_appointments` included, which is how this surfaced. The row is corrected above rather than left to imply the work starts from nothing |

---

## Recently closed (kept briefly so nobody re-opens them)

| App | Item | Closed | Evidence |
|---|---|---|---|
| Platform | Explicit `delete` grants removed platform-wide — **zero remaining outside `sc_*`** | 2026-08-25 | Final confirmation grep returns nothing. 68 lines across 16 files in the main pass, plus SAIRNscape's 13 across 3 files, plus `dnt_referrals` fixed pre-emptively before its migration runs. `sc_*` untouched at 29 lines across 15 files — the platform's only reachable delete path. Every edited file carries a note ending *"do NOT re-add `delete` here when fixing a missing grant"*, because that fix is precisely what caused the 2026-08-06 overcorrection. |
| SAIRNlaw | `canlii_rate_limit_log` table definition removed from `sairnlaw_wex_intl_schema.sql` | 2026-08-25 | Descoped properly rather than left as a cosmetic grant fix. The `create table if not exists`, index, RLS enable, policy, both grants, the `revoke`, and the verification line are all gone — a routine re-run of that schema would otherwise have **resurrected a table Michael deliberately dropped on 2026-08-24** for a feature deleted in `b747ecb`. **Dependencies checked first:** the file's three rate-limit tables are independent (zero FK/`references`), and the only CanLII references left anywhere on the platform are explanatory comments in `api/_lib/intl-caselaw.js`, `api/legal-citator.js` and `api/legal-reference.js` — no live code path. The header now records that Canada was a **scope** decision, never a terms problem, and says to re-read CanLII's current terms if it is ever restored. |
| **Platform** | `execute format(...)` grant loops re-added DELETE on 48 tables — invisible to the literal-grep sweep | 2026-08-25 | **Found by asking whether a stale finding was stale, and it was — but the check turned up two real ones.** The platform-wide pass (68 lines / 16 files) matched `grant ... delete` statements carrying real table names. `sql/sairndental_data_schema.sql:140` and `sql/sairnlegacy_data_schema.sql:346` built theirs as `execute format('grant select, insert, update, delete on public.%I to service_role', t)` inside a `foreach t in array` loop, so the table name never appeared on the grant line and **all 48 tables (12 `dnt_*`, 36 `leg_*`) were invisible to the grep**. Both files are `create table if not exists` and safe to re-run, so either one would have silently restored DELETE on its whole list — undoing part of a verified sweep with no error and no signal, the exact durability failure the SAIRNscape source fix existed to close. **Fixed in the format string, not the table lists**, so neither can reintroduce `delete` on tables added to the arrays later. **Sweep run before calling it closed, not after:** every `execute`/`EXECUTE` line touching `grant`/`revoke` across `*.sql`, `*.js`, `*.html` — the only two executable dynamic sites on the platform are these, both now delete-free; the four other hits are commented-out code inside the two sweep files. `grant`-inside-a-quoted-string checked separately across the same file types: every remaining hit is application prose ("Only an existing Owner can grant Owner access"), no SQL. Confirmation greps now return **zero** `grant ... delete` outside `sc_*` in any form, literal or dynamic, and `sc_*` is untouched at **29 lines / 15 files** — unchanged, re-counted after the edit rather than carried forward from the earlier row |
| Platform | Unused `DELETE` grants revoked on all non-`sc_*` tables (134) | 2026-08-25 | Section 2 run by Michael after the R1/R4/R6 hardening pass. **3a:** one row, `license_keys`, the deliberate exclusion. **3b:** one row, `LOST | DELETE | 134` — exactly the inverted assertion: 134 LOST all DELETE, **zero GAINED, zero LOST of any other privilege type**, which is what proves no SELECT/INSERT/UPDATE was collaterally dropped and is invisible to 3a. **3c:** baseline 785/213, live 651/213 — 785−134=651 exactly, table count unchanged, confirming no table was emptied. **3d:** 26 `sc_*` tables, DELETE intact on all. **The 26-vs-29 mismatch inside that file is not a loss** — 26 is the live `sc_*` count; `SC_RESOURCES` registers 28, of which `sc_specialty_checks`, `sc_anesthesia_base_units` and `sc_pctc` have schema files but no live table (already logged "QUEUED, NOT RUN"). ~~**NOT DURABLE YET — see the SAIRNscape row:** `sql/sairnscape_data_schema.sql:147-152` still grants DELETE on six `scp_*` tables~~ — **DURABLE AS OF 2026-08-25.** SAIRNscape landed in `46c8bd9`; lines 165-170 now read `grant select, insert, update` and :147-152 is the comment block explaining the removal, which is what kept regenerating the stale "still grants DELETE" claim on a `delete` grep. Two **dynamic** grant sites then had to be fixed before this was true — see the row above. |
| Platform | `service_role` TRUNCATE/REFERENCES/TRIGGER/MAINTAIN sweep, 214 tables | 2026-08-25 | `923a31c`. Section 2 run by Michael. **3a:** one row, `license_keys` — the deliberate exclusion, nothing else. **3b: five rows, all `GAINED`, ZERO `LOST`** across 774 baseline rows / 209 tables; the five are the Phase 5 `rf_*` tables created after Section 0 captured, and Section 2 is structurally incapable of producing a `GAINED` row (its GRANT list is filtered from the table's own existing grants, so it can only restore a subset). **3c:** 774/209 unchanged, so the diff ran against the real before-state. Baseline table dropped. Source already fixed by `append_only_grant_audit.sql:192-193`'s `ALTER DEFAULT PRIVILEGES` revoke, so the excess cannot return. |
| SAIRNlaw | Trust disbursement cross-device atomic check | 2026-08-17/18 | `law_check_and_insert_disbursement` live in `api/sd-data.js` + `sql/sairnlaw_trust_disbursement_atomic_check.sql`; 4 `law_` resources in the allowlist. **`SAIRN-BACKLOG.md` still lists this as open — that entry is stale.** |
| SAIRNlaw | Void-of-deposit could retroactively negative a client balance | 2026-08-17/18 | `law_check_and_void_deposit`, browser-verified |
| SAIRNlaw | CanLII / Canada coverage | 2026-08-23 | `b747ecb` — deleted, live-verified |
| SAIRNlaw | Orphaned `public.canlii_rate_limit_log` table | 2026-08-24 | Dropped by Michael. Was flagged for the schema owner because dropping a table on a shared DB is not a session's call — that routing worked as intended |
| SAIRNlaw | Deadline engine: Florida | 2026-08-23 | `78af333` — 47/47 isolation + live-loaded. Needed a real engine change (`shifted_start`), contrary to the plan |
| SAIRNlaw | Deadline engine: California | 2026-08-24 | 52/52 isolation + live-loaded. Two computation standards, statutory vs rules-of-court |
| SAIRNlaw | California service extensions (CCP 1013, 1010.6) | 2026-08-24 | Read verbatim and encoded. Per-method amounts: 5/10/12/20 calendar days for mail by location, 2 **court** days for overnight, fax and electronic. Appellate rows carry none — both sections exclude a notice of appeal in terms. 84/84 |
| Index | Handoff-reading pass over all 46 handoff files | 2026-08-24 | 28 new rows added, 7 stale claims caught and listed under Contradictions |
| SAIRNdental | `send-reminder.js` read the non-existent `RESEND_FROM_ADDRESS` | 2026-08-24 | **Fixed by another session after this index flagged it** — now reads `RESEND_FROM_EMAIL` at lines 46 and 98, with a regression test asserting the name. The index row saying "verified still broken at lines 37 and 75" was true when written and went stale within hours. Guardian Check 30 now catches this class mechanically |
| SAIRNlaw | Court e-filing (Ohio-first) | 2026-08-23 | `a183a07` — readiness check; transmission scoped out with reasons |
| SAIRNlaw | Deadline engine: Illinois | 2026-08-23 | `b8250a2`, 39/39 isolation + live-loaded |
| SAIRNdesign | Invoicing server-side uniqueness constraint | 2026-08-10 | Backlog marked resolved |
| SAIRNlegacy | Merchandise reservation server-side lock | 2026-08-10 | Backlog marked resolved |
| StoneDesk | Saved quote history didn't capture drawing-tool state | 2026-08-13 | Backlog marked resolved |
| StoneDesk | `SD-AUDIT-2026` credential blocker | 2026-08-23 | CC — fixed and verified |
| Guardian | `vercel_config_check` false 404s, `nav_panel_check` conventions, dead Check 23 | 2026-08-23 | `158999f` — 12/12 apps pass |
| SAIRNroofing | Phase 4d — manufacturer programmes at company level | 2026-08-25 | 51/51 **live**; 30 engine + 16 endpoint in isolation. Second of the 4a/4d/4b/4c split. **Nothing is seeded and that is the design** — the real programme terms sit behind manufacturer contractor portals, so the contractor enters their own thresholds citing their own agreement and a requirement naming NO SOURCE evaluates 'unusable' rather than met. Same computed/attested split as 3c: the roster-credential share is real arithmetic over the Phase 3a `rf_certifications` store (proven live with genuine records — an expired card and a same-named credential of the wrong `record_type` were both correctly excluded), everything else is self-reported and says so in the same string. The verdict vocabulary avoids the word "eligible" (asserted, not assumed), zero-over-zero is 'unknown' not 100%, and the not-regulatory disclosure rides on every result including empty ones. Gated harder than 3a's `evaluate` — management/broad-read only, because a roster share is an aggregate over colleagues — and the caller cannot substitute the roster or the certifications |
| SAIRNroofing | Phase 4a — multi-location attribution + crew scheduling | 2026-08-25 | 51/51 **live**; 17 engine + 29 endpoint in isolation. First of the 4a/4d/4b/4c split. **Backfill confirmed against real data**: all 5 pre-existing jobs carry `LOC-DEFAULT`, zero null or empty. `location_id` is ATTRIBUTION only — the gate is provably unchanged, verified live: fmA sees their branch job and not fmB's, fmB the reverse, and neither gained or lost anything because of which branch a job sits in. The carry-forward trap is closed and tested both ways: an edit that omits a location does **not** move the job to the default, while an explicit change is honoured. Scheduling: staffing a crew is management-only (an assignment decision, matching `rf_jobs`), a day inherits the **job's** branch not the caller's, and a narrow-tier employee sees a day they are on the crew for **even on someone else's job** — proven live, and the clause without which a crew member would be scheduled to work a day they cannot see. `set_status` proven status-only against production: date, job, crew and branch all unmoved by a payload that tried to change all four |
| SAIRNroofing | Phase 5 contingency agreement — tracked document, signature state, rescission clock | 2026-08-25 | 51/51 **live** against the deployed app; 26 engine + 25 endpoint in isolation. Closes §5.3 and with it Phase 5. Rule is per-state data with an enforced citation, seeded Ohio-only (R.C. 1345.21-.28); CO/FL/TX researched and recorded but deliberately NOT seeded. **A real bug was caught by reading the definitions section**: R.C. 1345.21 defines a business day as any day except Sunday plus eleven named holidays, so **Saturday counts in Ohio** — the first draft's Mon-Fri assumption was a full day wrong on a Thursday signing. Append-only proven live (409 on id reuse, original survived, no update verb); rescission is a second row that must name a real target; `recorded_by` server-stamped; the caller cannot substitute the governing state; `agreement_status` writes nothing. Three empty states kept distinct: `rule_not_applicable`, `no_rule`, and **`indefinite`** — R.C. 1345.23, where a missing cancellation form means the right to cancel never expires |
| SAIRNroofing | Phase 3c supplement reconciliation engine — **live** round trip | 2026-08-25 | 39/39 against the deployed `sairn.vercel.app` with real signed tokens, not isolation. Arithmetic hand-checked end to end on a real measured scope (44 SQ): quantity shortfall $1300, omitted item $432, asserted-with-photo $624, total $2356; adjuster-over produced $0 rather than a negative that could net away a real shortfall; the asserted line with no photo stayed at $0. Also proven live: a caller **cannot** substitute the measured scope (`payload.measured` ignored, still 44 from the job), `reconcile` writes nothing (claim + jobs rows identical across repeat calls), and the assignment gate holds (assigned foreman 200 / other foreman 403 with no worksheet in the body / no session 401). Cleanup pending in `sql/sairnroofing_verify_3b_cleanup.sql` |

---

## What this index does NOT cover, stated so the gap is known

- **Per-app handoffs were not read line by line.** Rows come from
  `SAIRN-BACKLOG.md` (section-by-section), `SAIRN-ACTIVE-WORK.md`, and work
  done in-session on 2026-08-23. There are ~40 handoff files; an item that
  exists **only** inside one and never reached the backlog is not here yet.
  That is the first thing to improve on the next rebuild.
- **Sizing is judgment, not estimation.** S/M/L is a rough shape, not a
  commitment.
- **Owners are observed, not assigned.** `unassigned` means no session has
  claimed it in the active-work log (`SAIRN-ACTIVE-WORK.md` at rebuild time;
  now the three per-session files) — not that it is unimportant. Rows
  owned by **Michael** need a decision or an access level a session does not
  have.
- **⚠️ rows carry a number or claim this rebuild could not confirm.** They say
  so in Next action.

---

## Keeping it alive

This file rots the moment it stops being updated, and a stale index is worse
than none — it makes work look handled when it is not.

1. **When you close something, move the row** to *Recently closed* with the
   commit SHA. Do not delete it silently.
2. **When you find something new, add a row** in the same action as logging it
   in `SAIRN-BACKLOG.md`. Two places, one action, or they diverge.
3. **Re-derive, don't trust.** Before a planning decision, spot-check the rows
   you are about to rely on. This rebuild found one stale backlog entry
   (SAIRNlaw trust disbursement, listed open, actually shipped) out of 26 —
   assume a similar rate next time.
4. **Do not renumber or restructure the backlog to match this file.** The
   backlog is the detailed record; this is the index over it.
