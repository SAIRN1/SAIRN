# SAIRN Active Work — Fourth

Per-session active-work log for the **Fourth** session (`Documents\SAIRN-fourth`).
Split out of the shared `SAIRN-ACTIVE-WORK.md` on 2026-08-24 because four
concurrent sessions appending to one file produced repeated merge conflicts
in a single night. Each session now appends only to its own file, so two
sessions can never collide on the same lines.

**Append your entries here, not to `SAIRN-ACTIVE-WORK.md`.**

The four sessions and their files — one row each, all equal, no special case:

| Session | Clone | File |
|---|---|---|
| Hank | `Documents\SAIRN-hank` | `SAIRN-ACTIVE-WORK-hank.md` |
| CC | `Documents\SAIRN-cc` | `SAIRN-ACTIVE-WORK-cc.md` |
| Cody | `Documents\SAIRN-cody` | `SAIRN-ACTIVE-WORK-cody.md` |
| Fourth | `Documents\SAIRN-fourth` | `SAIRN-ACTIVE-WORK-fourth.md` ← this file |

Still read the other three (`SAIRN-ACTIVE-WORK-hank.md`, `SAIRN-ACTIVE-WORK-cc.md`, `SAIRN-ACTIVE-WORK-cody.md`) before starting anything —
the point of the shared file was to avoid two sessions touching the same app
file at once, and that reason has not gone away. Splitting the file removes
the write collision, not the need to check.

One line per task: app/file(s) touched, a one-line task description,
timestamp. Add a line before starting anything new.

This file starts empty: the Fourth clone had no logged entries in the shared
file at split time (2026-08-24), so there was nothing to move. Cross-session
context that predates the split — including the `cc-work` worktree
correction — stays in `SAIRN-ACTIVE-WORK.md`.

---


- sql/full_crud_truncate_sweep_2026-08-24.sql, docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) — resumed the dead-table check the crashed session lost, then reviewed Section 2. 19 of 20 bare-named tables confirmed reachable by no code path (four channels enumerated, incl. the browser's direct anon-key Supabase calls — a gap my first pass had). Section 2's "cannot change capability" claim holds for the logic; 6 findings raised on coverage/verification/blast radius, none applied. Two evidenced corrections to SAIRN-cc's dd5327b block: network_insights has SELECT (3 live 200s, INSERT still unverified), intake_submissions is read by stonedesk.html:31982. Export as pasted here enumerates to 215, not 227 — second truncation. NOTHING RUN against the live DB; Section 1 remains the only executable statement. 2026-08-25
- sql/full_crud_truncate_sweep_2026-08-24.sql, docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) — CLOSED. Platform-wide service_role TRUNCATE/REFERENCES/TRIGGER sweep run by Michael and verified: 3a one row (license_keys, the deliberate exclusion), 3b five rows all GAINED and ZERO LOST across 774 baseline rows / 209 tables, 3c 774/209 unchanged. Baseline table dropped. Preconditions R1/R2/R4/R6 all closed by real queries first; R4a had to be rewritten because `role_column_grants` echoes table-level grants per column and could never return zero. Two of my own claims corrected along the way: the "export truncated twice" inference (wrong — 5 tables already revoked by append_only_grant_audit.sql, 6 never migrated) and P2's mechanism for the Phase 5 tables (right verdict, wrong route). NOT MINE, QUEUED NEXT: Cody's sql/unused_delete_grant_revoke_2026-08-24.sql — needs its OWN fresh baseline, must not reuse _grant_baseline_2026_08_25, and must run in its own window. 2026-08-25
- sql/unused_delete_grant_revoke_2026-08-24.sql, docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) — CLOSED. Hardened Cody's DELETE revoke sweep (R1/R4/R6 added — it had NO baseline at all, only Section 1 was executable in 358 lines) and Michael ran it: 3a one row (license_keys), 3b one row LOST|DELETE|134 with zero GAINED and zero non-DELETE LOST, 3c 785/213 -> 651/213 table count unchanged, 3d 26 sc_* intact. Verification shape deliberately INVERTED vs the TRUNCATE sweep — LOST is the objective here, so "zero rows both sides" would have failed 134 times on success. My predicted baseline 779/211 was short by 6/2; cause verified in the repo (Phase 4a 68668ec created rf_locations + rf_schedule, 3 privs each, no delete), not accepted from the arithmetic fit. OPEN AND NOT MINE TO CLOSE: sql/sairnscape_data_schema.sql:147-152 still grants delete on six scp_* tables — the sweep is not durable until that lands. 2026-08-25
- .claude/settings.json, sql/sairndental_referrals_verify_cleanup.sql, docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) — Repaired settings.json: the working tree copy was TRUNCATED mid-string at 2272 bytes (unterminated string, line 89, inside the PreCompact additionalContext), so the whole file failed to parse and NONE of it was in effect — not the hooks, not the permission allow/deny/ask lists, not outputStyle silent, not the superpowers plugin. The C:/SAIRN/tools/ repoint was therefore never live in this clone; HEAD still pointed at C:/Users/marsh/tools/, which now holds only 2 of the 5 scripts. Rebuilt from HEAD programmatically: 6 path rewrites (4 already attempted + the 2 session_lock_check.py lines the truncation had eaten along with the entire SessionStart and UserPromptSubmit blocks) and 1 PreCompact payload replacement with the corrected APP-YYYY-MM-DD-subject wording already applied in Hank/CC/Cody. json.loads validated before write, after serialize, and re-read from disk; asserted all 5 hook events present, zero stale paths, silent style and plugin intact. Diff vs HEAD is 7 insertions / 7 deletions — exactly the 7 changed lines, nothing else. Index: dnt_referrals row moved to CLOSED (migration run, provisioned flipped false->true, all four legs verified live incl. server-side location stamp and a real on_conflict UPDATE); new Platform row for the probe-row pattern at 14 files across 10 apps — Michael's 14/10 confirmed correct, and my own earlier "13 across 9" grouping was arrived at sloppily and happened to land right: rbac_test_artifact_cleanup.sql spans TWO apps (GRD-DEMO-2026 and SCP-DEMO-2026), which is the only reason file count and app count reconcile. Settings.json IS committed (amended into 411ee97 on Michael's instruction) so the repair propagates to all four clones via git rather than staying local to this one. The PreCompact clone list shipped three clones and omitted SAIRN-fourth — the same stale three-clone list CLAUDE.md records as corrected to four on 2026-08-24; Michael passed it through verbatim without catching it, I flagged it, and it was corrected to all four before the amend. 2026-08-25
- sql/sairndental_data_schema.sql, sql/sairnlegacy_data_schema.sql, sql/unused_delete_grant_revoke_2026-08-24.sql, docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) — DELETE-sweep durability CLOSED for real. Asked whether the sairnscape_data_schema.sql:147-152 finding was fresh before reporting it: it was STALE — SAIRNscape landed in 46c8bd9, lines 165-170 read `grant select, insert, update`, and :147-152 is now the COMMENT BLOCK explaining the removal, which is exactly what keeps regenerating the false claim on a `delete` grep. But checking it turned up two REAL survivors the literal-grep pass could not see: sairndental_data_schema.sql:140 and sairnlegacy_data_schema.sql:346 built their grants as `execute format('grant select, insert, update, delete on public.%I to service_role', t)` inside a foreach loop, so the table name never appeared on the grant line and 48 tables (12 dnt_*, 36 leg_*) were invisible. Both files are `create table if not exists` and safe to re-run, so either would have silently restored DELETE across its whole list — the same no-error no-signal failure the SAIRNscape source fix existed to close. Fixed IN THE FORMAT STRING per Michael's instruction, not by editing table lists, so neither can reintroduce delete on tables added to the arrays later. Sweep run BEFORE claiming closure, not after: every execute/EXECUTE line touching grant/revoke across *.sql/*.js/*.html (only two executable dynamic sites platform-wide, both now clean; four other hits are commented-out sweep code), plus a separate grant-inside-a-quoted-string pass (every remaining hit is app prose like "Only an existing Owner can grant Owner access", no SQL). Confirmation greps: ZERO grant...delete outside sc_* in any form; sc_* re-counted after the edit at 29 lines / 15 files, unchanged. Also corrected the stale warning block in unused_delete_grant_revoke_2026-08-24.sql:37-46 that generated this false finding — kept rather than deleted, with a note telling the next reader to read the executable grant lines and not the comment. 2026-08-25
- .claude/settings.json, docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) — Logged the ${CLAUDE_PROJECT_DIR} hook-path fix as a deliberately deferred follow-up with a full diff. RE-DERIVED, NOT RECALLED — the session that first worked it out was restarted and its version is gone, so it is labelled as a fresh derivation in the index row rather than passed off as the earlier work. One objection checked and cleared rather than assumed: per-clone copies of session_lock_check.py do NOT break the cross-session lock, because lock state lives in ~/SAIRN-SESSION-LOCKS (:49) outside every clone and clone identity comes from os.getcwd() (:58), not the script's own path. NOT APPLIED tonight, by Michael's call — the current fix is live across all four clones and swapping approaches again risks more confusion than it removes. 2026-08-25
- api/_resources/index.js (read-only), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- LIVE PROVISIONING SWEEP RUN, and three corrections including one of my own numbers. METHOD: api/sd-data.js maps a PostgREST 404/400 to {"ok":true,"data":[],"provisioned":false} instead of an error, so a READ is a live table-existence check needing no DB credentials and writing nothing -- no probe row, which matters given the 14-across-10 pattern. Verified licence-independent first (leg_memorials returns provisioned:true under both LEG- and DNT- keys). RESULT: 150 of 186 registered resources confirmed live (146 by flag + 4 by returned rows on flagless handlers), ZERO unprovisioned, 36 unmeasurable -- they need an employee session token and answer 401 NO_SESSION/403 FORBIDDEN, leaving SAIRNcare, SAIRNroofing, SAIRNsenior and SAIRNbuild ENTIRELY unmeasured. Disclosed as a limit, not a pass; SAIRNroofing matters most since rf_proposals/rf_invoices shipped tonight and are exactly the unprovisioned shape. My first pass logged 21 false 'unparseable' results from my own 300-char truncation, not an upstream fault; re-probed in full, all 21 live. COUNT RECONCILED, CODY WAS RIGHT AND I WAS WRONG: 224, not my 231. My grep counted `create table` inside COMMENTS, inventing five phantom tables named alone/anywhere/for/if/to from prose in file headers. Comment-stripped with a quote-aware state machine = 226; minus the two transient sweep baselines = 224. File count was never a disagreement: 128 at 0edc23d, 129 after 389cdb1, 130 only because my own referrals cleanup file landed tonight, and it declares no table. Only 89 of 130 files contain a CREATE TABLE. LOGGED SEPARATELY, NOT FOLDED INTO COVERAGE: 41 declared tables have no registered resource at all and are structurally invisible to this method -- mostly *_employee_auth across 11 apps, rate-limit logs, audit logs and the cl_* CourtListener cache. Needs one information_schema query from Michael; that single query also closes the 36 session-gated ones. The 12 logical-name mappings (customers->scp_customers, render_usage->sd_render_usage at :1651, etc.) were each read out of the handler, not inferred from the name. RF-VERIFY-PROBE-20260825 CHASED AND THE INDEX WAS STALE: the row is GONE. Read under the licence it was written with returns data:[], and the handler filters on license_hash only, so nothing could hide it. Control run before concluding -- dnt_patients returns 8 rows and dnt_appointments 11 under the same licence and code path, so it is absence, not a bad hash. Cannot have gone through the API (no delete path for DNT_RESOURCES); most likely the cleanup was run in the SQL editor and the file never re-labelled. Corrected the index claim that all 14 probe rows are still live -- the FILE count is solid, the ROW claim was not. 2026-08-25
- sql/provisioning_gap_check_2026-08-25.sql (NEW), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- NOT-RUN LABEL DRIFT IS SYSTEMIC, NOT A ONE-OFF, and the DB-side query is written. Michael confirmed he has no record of a targeted cleanup for RF-VERIFY-PROBE-20260825, so the row's disappearance stays unexplained -- but the evidence for its absence holds independently, and he asked me to spot-check the other labels. Did so: 8 named probe rows across 5 more cleanup files, every measurable one GONE. sairndesign_qa_test_cleanup (sdn_vendors), sairndesign_synctest_cleanup (sdn_vendors, sdn_roomdims), sairnlegacy_audit_test_cleanup (leg_cases), sairnlegacy_synctest_cleanup (leg_cases, leg_vehicles, leg_monuments), sairndental_credentials_verify_cleanup (dnt_credentials). CONTROL RUN EVERY TIME before concluding, same discipline as the referrals row -- an empty table under a licence proves nothing on its own: sdn_specitems 1 / sdn_invoices 2 under SDN-PINNACLE-2026, leg_dispatches 1 / leg_merch_units 3 under LEG-PINNACLE-2026, four dnt_* tables with rows under DNT-PINNACLE-2026. Each file probed under the licence IT names. TWO LIMITS STATED RATHER THAN SMOOTHED: sdn_clients is session-gated and unmeasurable, and sairndental_credentials_verify_cleanup names NO licence so its result rests on a weaker assumption. And GONE does not mean THE FILE WAS RUN -- a re-provisioned table or manual delete reads identically. The durable finding is narrower and firmer: NOT RUN is a claim about the file that no longer describes the database, in 6 of 8 measurable cases. SAIRNROOFING CLOSED BY OTHERS, NOT BY ME: rf_proposals/rf_invoices were live the whole time -- CC ran a real production round trip, Cody verified panel rendering in a browser. My sweep flagged them as the sharpest unmeasured risk and was structurally incapable of answering; keeping it in the index as the worked example of why the 36 session-gated resources are reported unmeasured rather than folded into a pass. QUERY WRITTEN: sql/provisioning_gap_check_2026-08-25.sql, read-only and mechanically verified (0 insert/update/delete/drop/create/grant/revoke/truncate/alter outside comments, 6 selects). Section 1 declared-but-not-live, section 2 live-but-not-declared (drift the other way, invisible to any code-side method), section 3 totals so a truncated paste is DETECTABLE -- if declared_total != 224 the first two sections must not be read. The 224 names are generated from the repo with comments stripped, not hand-typed, and the file says so, because the naive extraction is exactly what produced my phantom 231. Michael's message specifying the query was itself truncated mid-sentence; flagged rather than guessed at, and this file written to stand on its own. 2026-08-25
- .claude/skills/sairn-guardian-v2/SKILL.md (+ user-level copy), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- PROVISIONING SWEEP CLOSED PLATFORM-WIDE, and Guardian Check 23's retirement wording corrected. Michael ran sql/provisioning_gap_check_2026-08-25.sql split at the three section boundaries (not pasted whole -- the paste length WAS the failure mode). Section 3 first as instructed: declared_total 224, matching the generated list exactly, so 1-2 are trustworthy. Section 1: exactly ONE declared table not live, sairnscape_org_intel, already a known suspect on this index -- so zero NEW unprovisioned migrations platform-wide. matched 223/224. Section 2: live_total 248, live_but_not_declared 25, all previously known. That also closes the 36 session-gated resources my read probe could not measure: their tables are inside the 224, and with only sairnscape_org_intel missing, all 36 are provisioned. Net: 150 by read probe, remaining 74 by catalog query, one real gap and it was already known. Kept the sweep row rather than deleting it -- it is the record that a method disclosed its own blind spot and the blind spot turned out EMPTY, which is exactly the outcome that makes skipping the disclosure tempting next time. NO DIRECT DB PATH EXISTS FROM THIS SESSION, confirmed rather than assumed before answering: no psql, no supabase CLI, no pg_dump, no pg/postgres driver in node_modules, no ~/.pgpass. Vercel CLI 56.5.0 IS installed and authed as sairn1, so I listed production env NAMES (read-only, no values) -- there is no POSTGRES_URL, DATABASE_URL or any connection string, only SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY. Installing pg would not have helped; nothing to connect with. Offered a service-role-key + PostgREST-OpenAPI route with its two real limitations stated (includes views, and a table with no service_role grant would read as absent -- a false positive the catalog query cannot produce); Michael declined it since the exact answer was already in hand, correctly. CHECK 23 WORDING FIXED: it claimed 'the mechanism this check describes does not exist anywhere.' False. SAIRN_INTERNAL_KEY is real and provisioned on Vercel Production and Preview (~67d), confirmed via vercel env ls --project sairn. The retirement still stands on the accurate narrower claim -- ZERO CODE READS IT -- and the correction is recorded in place rather than overwritten, per this file's own convention. The general lesson written into the skill: the grep was over api/, and a grep of code can only ever prove something about code. Both copies of SKILL.md were byte-identical (59100) before the edit and are byte-identical (60135) after -- checked by sha256, not assumed, since this skill's own 'Eliminate Duplication at the Source' section warns about exactly this user-level/project-level pair. 2026-08-25
- sql/introspect_undeclared_tables_2026-08-26.sql (NEW), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- MIGRATIONS FOR THE THREE UNDECLARED TABLES ARE BLOCKED ON REAL SCHEMA, AND I DID NOT GUESS THEM. Michael asked for migration files for business_profiles, ai_memories and employees matching the live schema exactly, pulling structure from information_schema first. I have NO information_schema access -- established last turn and unchanged: no psql, no supabase CLI, no pg driver, and no connection string of any kind in the Vercel project (only SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY). So the correct move was to write the introspection query, not the migrations. WHAT I DID INSTEAD OF GUESSING: read every call site and recorded the code-side expectations IN the query file as an explicit CROSS-CHECK, not as the answer -- business_profiles license_hash/app_id/data/shop_id/updated_at with UNIQUE(license_hash,app_id) proven by ?on_conflict= at api/sd-data.js:288; employees customer_email/employee_id/source_app/status/data/updated_at with UNIQUE(customer_email,employee_id) at :432; ai_memories license_hash/app_id/shop_id/data/created_at, plain INSERT with no on_conflict so a surrogate key is likely and created_at must default server-side (writer never sends it, reader orders by it, :303/:320). The file says in terms that if the DB disagrees, the DB wins and the disagreement gets its own index line. Code shows COLUMNS; it cannot show types, defaults, PKs, indexes, RLS policies or grants, and business_profiles backs every StoneDesk profile read/write, so a near-miss there is worse than the gap. QUERY: six independent read-only SELECTs, no shared state, run one at a time -- columns, constraints via pg_get_constraintdef() so the text is character-identical rather than reconstructed, indexes, RLS + every policy in full, grants, and a sanity section naming all three so a PARTIAL answer is visible instead of silent. Section 6 is to be run FIRST. Mechanically verified read-only: 0 insert/update/delete/drop/create/grant/revoke/truncate/alter outside comments, 6 selects, 6 statements. TWO THINGS FLAGGED FOR DECISION RATHER THAN BURIED: employees is scoped by customer_email, NOT license_hash -- the only table on the platform keyed that way, and a migration would enshrine it. And section 5 doubles as a grant check: expect select/insert/update and NO delete; if DELETE appears, these three were missed by the 2026-08-25 sweep exactly as the two execute format(...) loops were, and tonight's durability claim needs revisiting. File named 2026-08-26 rather than -25 because that is when it was actually written; the finding it addresses is from the 25th and the header says so. 2026-08-26
- sql/business_profiles_schema.sql, sql/ai_memories_schema.sql, sql/employees_schema.sql (all NEW), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- THREE UNDECLARED LIVE TABLES NOW DECLARED, written from the real introspected structure rather than from the code. Michael ran all six sections of introspect_undeclared_tables_2026-08-26.sql and returned the full structure; every column, nullability, default, constraint and index in the three files matches what came back, verified column-by-column by script rather than by eye. Declared tables 224 -> 227, live_but_not_declared 25 -> 22. THE CODE-SIDE PREDICTIONS HELD ON SHAPE AND FAILED ON SURFACE, which is the whole justification for not writing these from the code: both UNIQUE constraints were correctly derived from ?on_conflict= (business_profiles license_hash/app_id at :288, employees customer_email/employee_id at :432), and the ai_memories call -- surrogate key likely, created_at must default server-side -- was right. But nothing in the code could see id, created_at, the defaults, or the 64KB CHECK on all three. Two nullable columns would have been easy to get wrong in the other direction and are correct here: ai_memories.shop_id (writer stamps it inside a try/catch commented 'non-fatal -- memory still saves unlinked') and employees.status (defaulted in JS at :423, not in the column). GRANT CHECK CAME BACK CLEAN: service_role holds INSERT/SELECT/UPDATE and NO DELETE on all three, so they were NOT missed by the 2026-08-25 sweep and that durability claim stands. TWO FINDINGS DISCLOSED IN THE HEADERS, NOT SILENTLY FIXED, both now their own index rows because both are design decisions above a migration file's pay grade. (1) ai_memories has NO unique constraint -- primary key on id and nothing else, and the code does not want one: plain INSERT with no on_conflict, read is order=created_at.desc&limit=10, live index idx_mem_scope(license_hash, created_at DESC). Append-only is the actual behaviour; the open question is whether that is intended, since the NAME suggests a per-licence record. Adding a constraint would change behaviour and could fail on existing duplicates, so the file adds none and says why. (2) anon and authenticated still hold TRUNCATE/REFERENCES/TRIGGER on all three -- the default-ACL baseline removed everywhere else, which never covered these because a sweep driven off sql/ inherits sql/'s omissions. Same blind spot one level down. Each file carries the revoke-anon/authenticated line as the house idiom so a re-provision does not inherit it, but every header flags it as the ONE non-inert statement in the file. employees is the one to weigh first: ten apps share that roster. Worth checking the other 22 live_but_not_declared tables for the same baseline. 2026-08-26
- sql/undeclared_table_acl_check_2026-08-26.sql (NEW), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- THREE MIGRATIONS RUN BY MICHAEL INCLUDING THE REVOKE LINE; POST-RUN CHECK DONE TO THE LIMIT OF WHAT I CAN REACH, AND THE LIMIT SAID OUT LOUD. I have no DB access, so the footer checks in each migration -- row counts, constraint sets, index counts, grant listings -- were NOT run by me and are NOT claimed. What I could verify is the API, and it is a real check rather than a formality: after a `revoke all` + re-grant, all three read paths still answer correctly through api/sd-data.js. profile -> 200 with a real populated row incl. its shop_id; memory -> 200 with a real row dated 2026-07-22; employees -> 403 FORBIDDEN from the ROLE gate, i.e. the request reached the role check instead of dying on a Postgres permission error. That is precisely the failure this sequence could have caused -- a revoke landing without its re-grant shows up as 42501/500 on every one of the three -- and it did not happen. AI_MEMORIES LEFT UNCONSTRAINED on Michael's instruction, reasoning recorded rather than encoded: the shape (plain INSERT, order-by-created_at read, an index built for exactly that) reads as intentional design, but 'reads as intentional' is an inference, and inferring a product decision then silently enshrining it in a migration is how an accident becomes a specification. Index row now owned by Michael as a product decision, not left unassigned. 22-TABLE ACL CHECK WRITTEN, NOT ASSUMED: sql/undeclared_table_acl_check_2026-08-26.sql, read-only (4 statements, 7 selects, zero write verbs, 681 VALUES entries = 227 x 3). Section 2 COMPUTES the live-but-not-declared set from the 227 rather than taking a pasted list, so it cannot go stale between the gap check and this one -- which matters because I never had the list of 25, only the count. Section 1 re-checks the three as a control. Section 3 runs the same check across DECLARED tables, and that control is the point: if BOTH sets come back dirty the earlier sweeps did not finish and this is far larger than three tables; if only the undeclared set is dirty, the sql/-blind-spot mechanism holds. Section 4 totals so a truncated paste is detectable. The prediction is written into the file AHEAD of the answer deliberately -- if the other 22 come back clean, my reasoning is wrong and that is worth more than being right. 2026-08-26
- docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- MY HYPOTHESIS WAS WRONG AND THE CONTROL CAUGHT IT. I predicted the anon/authenticated TRUNCATE baseline would sit only on live-but-not-declared tables (sweep driven off sql/ inherits sql/'s omissions). Section 3 -- the control over DECLARED tables, written specifically so the prediction could be falsified -- came back dirty too: 158 tables each for anon and authenticated, 316 grant-rows, including heavily-used declared tables like dnt_appointments and grd_jobs. Declaration status is irrelevant. REAL ROOT CAUSE: every grant-hardening pass tonight targeted service_role ONLY. Verified at append_only_grant_audit.sql:192-193, which reads `revoke truncate, references, trigger, maintain on tables from service_role` and names no other role. The three tables were never a special case. Writing the control in is what made this findable; had I only run section 2 I would have 'confirmed' a wrong mechanism with real data. SEVERITY ASSESSED, and the first question came back the OPPOSITE of what the proxy-only model predicted. anon is NOT unused: browsers ship a publishable key (sb_publishable_... at stonedesk.html:24668, :34319, sairnbiz.html:835) and hit intake_submissions directly with select/update/DELETE (stonedesk.html:31984, :32119, :32130) -- already recorded at full_crud_truncate_sweep_2026-08-24.sql:351,502. But the excess verbs have no invocation path: PostgREST exposes no TRUNCATE and no DDL verb, there are ZERO .rpc() calls in any browser file, and all five SQL functions are SECURITY INVOKER with revoke-all-from-public + grant-execute-to-service_role only. Two precision points that change how this should be read: RLS is NOT a control (TRUNCATE bypasses row-level security entirely, so the svc-only policies do nothing against it), and a publishable key is public by design, so whatever anon holds is effectively held by anyone. VERDICT: latent not active, Medium -- unreachable because no verb exposes it, not because anything guards it; High the moment any SECURITY DEFINER function is granted to anon/public or any RPC runs dynamic SQL. SCOPE PROPOSED, NOTHING WRITTEN YET, awaiting Michael: roles anon+authenticated (verify separately whether authenticated has ANY legitimate use -- no code path signs a user into Supabase auth); verbs TRUNCATE/REFERENCES/TRIGGER ONLY, explicitly not S/I/U/D since anon legitimately needs three of those on intake_submissions; NAMED CARVE-OUT and the sweep's sharpest risk -- intake_submissions must keep anon's SELECT/UPDATE/DELETE or StoneDesk's intake panel breaks silently, asserted BEFORE and after; fix the SOURCE too by extending ALTER DEFAULT PRIVILEGES to both roles or every new table re-acquires the baseline; same shape as the two sweeps that worked (R6 -> R4 -> Section 0 baseline -> 1 discover -> 2 revoke commented -> 3 verify -> 4 cleanup); verification INVERTED as in the DELETE sweep since LOST is the objective -- assert the expected LOST rows all in {TRUNCATE,REFERENCES,TRIGGER}, zero GAINED, zero LOST of any other verb, table count unchanged, carve-out intact. 2026-08-26
- sql/anon_authenticated_grant_revoke_2026-08-26.sql (NEW), docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- ANON/AUTHENTICATED SWEEP BUILT TO APPROVED SCOPE, SECTION 2 COMMENTED, AWAITING MICHAEL'S SECTION 0-1 OUTPUT. Six-part shape matching the two service_role sweeps: R6 owner gate -> R4 pre-flight -> Section 0 baseline (+0b carve-out asserted BEFORE) -> Section 1 list-free discover (+1b what is NOT touched) -> Section 2 revoke, commented -> Section 3 3a/3b/3c/3d -> Section 4 cleanup, commented. Mechanically verified: the ONLY executable write in the entire file is the Section 0 baseline table; the revoke, the ALTER DEFAULT PRIVILEGES and the Section 4 drop are all commented out. I CANNOT run Section 0-1 myself -- no DB access -- so that report has to come from Michael. AUTHENTICATED IS UNREACHABLE, NOT MERELY UNUSED, and the whole-codebase check is what got the right answer. Zero Supabase Auth anywhere: no supabase.auth, signInWithPassword, signUp(, onAuthStateChange, setSession, /auth/v1/, gotrue, admin.createUser, or @supabase import in any .html/.js/.json including api/. THE ONLY AUTH ON THE PLATFORM IS FIREBASE -- sairncash.html:402 imports getAuth/signInWithCustomToken from firebasejs/10.12.0/firebase-auth.js for RTDB on sarintype-6e070; it mints a Firebase auth.uid, never a Supabase JWT, so it can never yield the authenticated role. A naive grep for 'auth' hits it and reads exactly like Supabase Auth in use -- that is precisely the trap Michael's 'check the whole codebase, not the obvious paths' instruction was for, and a narrower grep would have produced a confident wrong answer either way. SAIRN's own *-auth.js files are its own per-employee credential endpoints verified by verifySessionToken(), unrelated. Corroborated from the DB side: zero RLS policies reference auth.uid()/auth.jwt()/auth.email(); all 186 are auth.role()='service_role'. SCOPE STILL HELD TO THREE VERBS despite that: stripping an unreachable role to zero is a separate change with its own blast radius, and mixing it in would leave Section 3 unable to distinguish an intended removal from a collateral one. Logged as its own open question instead of folded in. TWO THINGS ADAPTED RATHER THAN COPIED FROM THE PRIOR SWEEPS. R4: this file issues no REVOKE ALL so the column-ACL cascade risk mostly does not apply -- but REFERENCES has a column-level form while TRUNCATE and TRIGGER do not, so R4a is run for REFERENCES specifically and the file says why, rather than carrying the check as ceremony. CARVE-OUT: intake_submissions is protected STRUCTURALLY -- Section 2 revokes three named verbs, so anon keeps S/U/D because nothing in the file touches them; a revoke-all + selective re-grant would have put it at the mercy of a correctly-written list. Asserted before (0b) and after (3a) anyway, because structurally-safe is a claim about the code and the assertion is a claim about the database, and those two have diverged repeatedly tonight. The failure mode is silent: the browser intake calls sit inside try{}catch(e){} at stonedesk.html:32119/:32130, so a permission error shows as an empty panel, not an error. BASELINE DELIBERATELY CAPTURES ALL PRIVILEGES for both roles, not just the three being revoked -- that is what lets 3b catch collateral loss of anon's SELECT/UPDATE/DELETE on intake_submissions, which is outside the verb-set. No expected row counts hardcoded: the DELETE sweep predicted 779/211 against a real 785/213 and the arithmetic fit of the wrong number was itself convincing, so 3c compares against Section 0's recorded output rather than a literal. 2026-08-26
- sql/anon_authenticated_grant_revoke_2026-08-26.sql, docs/SAIRN-OPEN-WORK-INDEX.md, stonedesk.html (read-only) (Fourth) -- SECTION 0b FAILED ITS OWN ASSERTION, WHICH IS THE ASSERTION WORKING, AND I HAD TO CORRECT MY OWN SEVERITY CLAIM. 0b demanded anon hold SELECT/UPDATE/DELETE on intake_submissions before the sweep and said that if it did not, the panel was already broken for some other reason. It returned anon | REFERENCES, TRIGGER, TRUNCATE -- the default-ACL baseline and nothing else. MY EARLIER CLAIM WAS RIGHT ABOUT THE CODE AND WRONG ABOUT THE DATABASE. I reported 'anon is NOT unused' on the strength of stonedesk.html:31984/:32119/:32130 calling PostgREST directly, and let that imply the path WORKS. It does not. Verified live with the real shipped publishable key: GET /rest/v1/intake_submissions -> 401 42501 permission denied, with PostgREST's own hint naming the missing grant. A 42501 is an AUTHORIZATION failure, so the key authenticated fine and resolved to anon; the role holds nothing. Same 401 on business_profiles, employees, sd_slabs and the PostgREST root. Repo side corroborates: NO `grant ... to anon` statement exists anywhere in sql/, on any table, ever. CONSEQUENCES, both good for the sweep: severity drops again (no legitimate anon usage to protect, so no carve-out to get wrong), and the carve-out is RETIRED. Kept the 0b/3a queries with expectations INVERTED rather than deleting them -- a CHANGED answer there between now and Section 2 would mean someone granted anon something in the window, and deleting the pair would remove the only check aimed at the one table anyone had reason to watch. SEPARATE REAL BUG FOUND, NOT CAUSED BY ANY SWEEP: StoneDesk's intake panel is broken in production and fails SILENTLY. The read is inside try{}catch(e){}; the catch swallows the 42501 and falls back to localStorage 'sd_intake', rendering device-local data with no error -- while the line directly above comments 'Supabase is the real source of truth here', which is exactly backwards. Same class as the Slabs SEED fallback in Guardian's verification-discipline lesson 6. :32119 and :32130 (accept/delete) are the same shape and also dead. intake_submissions is declared in no schema file and was never granted to anon by anything in the repo; whether it EVER worked (hand-grant later removed, as happened to sd_employee_auth) or never worked is not answerable from the repo. Logged with an explicit warning NOT to fix it by granting anon SELECT -- that would make the table world-readable since the publishable key is public by design; routing it through api/sd-data.js like every other table is the consistent fix, and that decision comes first. Also caught and fixed one of my own index rows re-introducing the literal-pipe-inside-backticks rendering bug I fixed hours ago ('anon | REFERENCES, ...'), found by the column-count check rather than by eye. 2026-08-26
- docs/SAIRN-OPEN-WORK-INDEX.md (Fourth) -- ANON CAN SELECT NOTHING, MEASURED NOT ASSUMED; INTAKE BUG RAISED TO HIGH. I still cannot run Sections 0/1/1b -- no DB access, unchanged, and saying so again rather than quietly substituting. What I could do instead is the strongest available substitute for 1b's central question: a GET-only probe as anon with the real shipped publishable key across all 228 tables (227 declared + intake_submissions). Result: 227 x 401, 1 x 404, ZERO 200s -- anon can SELECT nothing on this platform. USEFUL CROSS-CHECK FELL OUT OF IT: the single 404 is sairnscape_org_intel. 404 means the relation does not exist; 401 means it exists but the role lacks privilege. So this probe independently reproduces the catalog gap check's one genuine miss by a completely different method -- two unrelated techniques landing on the same single table is real corroboration for both. SCOPE STATED SO IT IS NOT OVER-READ: SELECT only. I deliberately did NOT probe INSERT/UPDATE/DELETE, because probing them means writing to production, and that is not an acceptable way to answer a question about grants -- a probe row is exactly the debt this index already tracks 15 cleanup files for. Section 1b remains the only way to settle those three verbs. On evidence so far anon looks to be in the same position as authenticated: authenticated by the key, authorised for nothing. INTAKE_SUBMISSIONS BUG RAISED TO HIGH PRIORITY per Michael, and explicitly NOT fixed here -- routing it through api/sd-data.js is a real feature change deserving its own pass, not a drive-by inside a grant sweep. The row now says both that and 'do not fix it by granting anon SELECT' (the publishable key is public by design, so that makes the table world-readable). Noted in the row why this class is expensive: nobody reported it, and it surfaced only because an unrelated pre-assertion happened to fail. 2026-08-26
