# SAIRN Active Work — Hank

Per-session active-work log for the **Hank** session (`Documents\SAIRN-hank`).
Split out of the shared `SAIRN-ACTIVE-WORK.md` on 2026-08-24 because four
concurrent sessions appending to one file produced repeated merge conflicts
in a single night. Each session now appends only to its own file, so two
sessions can never collide on the same lines.

**Append your entries here, not to `SAIRN-ACTIVE-WORK.md`.**

The four sessions and their files — one row each, all equal, no special case:

| Session | Clone | File |
|---|---|---|
| Hank | `Documents\SAIRN-hank` | `SAIRN-ACTIVE-WORK-hank.md` ← this file |
| CC | `Documents\SAIRN-cc` | `SAIRN-ACTIVE-WORK-cc.md` |
| Cody | `Documents\SAIRN-cody` | `SAIRN-ACTIVE-WORK-cody.md` |
| Fourth | `Documents\SAIRN-fourth` | `SAIRN-ACTIVE-WORK-fourth.md` |

Still read the other three (`SAIRN-ACTIVE-WORK-cc.md`, `SAIRN-ACTIVE-WORK-cody.md`, `SAIRN-ACTIVE-WORK-fourth.md`) before starting anything —
the point of the shared file was to avoid two sessions touching the same app
file at once, and that reason has not gone away. Splitting the file removes
the write collision, not the need to check.

One line per task: app/file(s) touched, a one-line task description,
timestamp. Add a line before starting anything new.

Entries below were moved verbatim from the shared file (nothing reworded,
nothing summarized), in their original order. Cross-session context that
predates the split — including the `cc-work` worktree correction — stays in
`SAIRN-ACTIVE-WORK.md`.

---

- sairncode.html (Hank) — DONE, read-only. Re-checked all 5 capabilities post-Pass-2: all still present/intact (grep-confirmed at current line numbers), AR exclusion still valid and well-reasoned, nothing broke. Real finding: predictive insights' denial-pattern context and the get_providers AI tool both read getXEntries(), which is now genuinely practice-wide synced data (Pass 2) instead of single-device local data -- their existing "live from this practice's data" claims are now actually true across every coder's device, not just overstated-but-harmless before. No capability gap found, nothing to build. Reported to Michael. 2026-08-18
- sairnlaw.html (Hank) — DONE. Browser-level void-rollback verification complete (live, LAW-TEST-2026, hank-verify owner): real disbursement voids clean (toast "Transaction voided", status -> Voided, reason appended); a deposit void that would take real balance negative is server-rejected (exact message: real balance $-600.00) and the UI correctly stays Posted (no stuck optimistic "Voided" state) -- confirmVoid()'s rollback confirmed working. Some test data (Deposit $1,000 + Disbursement $600, both voided/posted) now lives in LAW-TEST-2026 -- fine to leave (test license) or reset again later. 2026-08-18
- sairncash.html (Hank) — DONE. Read-only summary complete, reported back to Michael. No code edits made. 2026-08-18
- sairncash.html + api/sairncash/* (Hank) — DONE. 30-day trial flow fully live-verified: real signup/duplicate-rejection/verify/badge/expired-state all confirmed via curl + real browser click-through (LAW-lookalike standard, LAW-TEST-2026 style). One real pre-existing bug found (not introduced by this task, affects the paid-subscription path identically): showPage('app') never calls initApp(), so a plain "Open App" click after the initial page load never re-checks trial/subscription state -- reported to Michael separately, not fixed here (out of this task's scope). 2026-08-18
- sairnlaw.html (Hank) — DONE. AI-advancement rollout per docs/superpowers/specs/2026-08-18-sairnlaw-ai-advancement-design.md, all 4 build-order items complete: stale disclosure-copy fix (d76d984), voice input chat+matter notes (2764202), predictive insights deadline-miss-rate (75c2166), and shared-knowledge (this commit) -- general-only-gated per spec §2: recordLawSharedTopics() only fires from sendAI() when sentMatter==='general' (the app's own mandatory aimatter select), reuses the existing generic /api/sd-data shared_knowledge resource (license_hash-scoped, no server change needed), context read-back (buildLawSharedCompanyContext) is safe on any question since the store can only ever contain general-question words by construction. node --check clean, pushed (`a3a6c9c`), live-deploy confirmed byte-identical to committed source. Gate live-verified end to end via real browser session (LAW-TEST-2026, hank-verify owner): sent a real general question ("What should a standard client intake checklist include...") -- shared_knowledge store went from `{topics:{}, totalQuestions:0}` to 10 real extracted words, `totalQuestions:1`. Then sent a real matter-specific question against 2026-0001 Ostrander Estate Planning ("How should we handle the embezzlement defense strategy...", deliberately the same scenario named in the spec's own privilege-risk example) -- store stayed byte-identical afterward (still `totalQuestions:1`, none of embezzlement/defense/strategy/dispute/Ostrander present). Gate confirmed holding for real, not just by code inspection. Fully closed. 2026-08-18
- sairnlaw.html + api/law-auth.js (Hank) -- DONE. Fixed a real gap found while investigating the earlier stale-TESTOWNER1-session report: it was NOT a cross-app bleed (mechanically confirmed zero localStorage key overlap between sairnlaw.html and sairnbiz.html, and every verifySessionToken() call site across api/ already passes expectedApp) -- it was lawRestoreSession() trusting a cached law_session token's decoded payload for the top-bar identity display purely client-side, checking only exp, never payload.app, and never re-confirming the employee row still exists server-side. Fixed both gaps: client now self-checks payload.app==='sairnlaw' before trusting the token, and fires a background api/law-auth.js action:'whoami' call right after entering the app (verifySessionToken + a real active-employee-row lookup, same pattern as the file's own mfa_setup/mfa_enable), logging the user out with an explanation if it fails. Not blocking initial paint -- same optimistic-then-verify shape as before, just closes the staleness window instead of leaving it open indefinitely. node --check clean on both files, pushed (`395b453`), live-deploy confirmed (whoami reference present in live source). Live-verified end to end: curl-tested whoami with a real valid session (200, correct role/employee_id), a garbage/invalid token (correctly 401 NO_SESSION), and no token at all (same 401); real browser reload on LAW-TEST-2026 (hank-verify) confirmed the restore path fires a real POST to api/law-auth (200) automatically on load, network-request-logged, not just asserted. Fully closed. 2026-08-18
- sairndesign.html (Hank) -- DONE. AI-advancement rollout to the same 5-capability standard, all 5 now genuinely satisfied. NOT a ground-up build (corrected an earlier wrong assumption, verified against real repo state first) -- 19 panels, real cross-device Supabase sync live-verified. AR measurement: already built (Phase 4 real WebXR room capture, honest per-device fallback) -- not a gap, skipped. Built and pushed the remaining 4: (1) get_studio_attention agentic tool (`9b66015`) -- overdue invoices/stale proposals/upcoming schedule/overdue sample requests, all live-computed (invoice 'Overdue' status is a dormant value nothing ever sets, so overdue is computed from due_date directly, same discipline as SAIRNlaw's deadline urgency). (2) Sales Cycle Trend predictive metric (`cbb3059`) -- real sent_date/decided_date deltas, deviation-from-own-baseline, honest 6+-decided-proposal minimum-sample gate. (3) Voice input (`4612173`) -- ainp + client/project notes, ported from SAIRNbiz, proactively re-runs attach on every nav() (the fix SAIRNbiz needed live, applied here before hitting the same bug). (4) Shared studio-knowledge layer (`da9e0e9`) -- unconditional-extraction pattern (confirmed correct call: no per-question client/project selector exists to gate on, unlike SAIRNlaw). Also queued sql/sairndesign_qa_test_cleanup.sql (2 leftover QA rows on SDN-PINNACLE-2026, needs a direct Supabase run -- no delete action exists for sdn_* resources via the API). ALL 4 LIVE-VERIFIED end to end via real browser session (SDN-PINNACLE-2026, owner PIN): get_studio_attention gave a real correct tool_use response (0 overdue/stale items, correctly surfaced the real Whitfield site-visit schedule entry); Sales Cycle Trend rendered its honest "not enough history (0 on record, need 6)" fallback live; mic buttons confirmed rendering on AI chat and the Client Notes field; shared_knowledge store confirmed populated via curl round-trip (real extracted words, totalQuestions:1) after the live AI question. Fully closed. 2026-08-19
- sairnlegacy.html (Hank) -- IN PROGRESS. AI-advancement rollout to the same 5-capability standard. Checked before building: NOT a ground-up build -- 26 panels across 4 phases, real cross-device Supabase sync live-verified (LEG-PINNACLE-2026). AR measurement: checked Monument/Headstone (no dimension fields, real inscription-vs-case-file mismatch checker instead) and Cemetery/Plot Management (section/row/number cataloging, not physical capture) directly -- no genuine fit, not building, same exclusion class as SAIRNbiz/SAIRNvet/SAIRNcode/SAIRNlaw. Agentic follow-up: get_home_attention built and pushed (`b3b229a`) -- stale permits/unsigned documents/upcoming services/stale merchandise reservations, all live-computed. Predictive metric (case-open to scheduled-service trend), voice input (chat + Case notes + Aftercare/Grief-Support notes, confirmed), and shared-knowledge still to build. Shared-knowledge decision: permission-gated (management/owner-tier by default, individually grantable), NOT content-gated -- but a real architectural blocker surfaced mid-build: this app has ZERO server-verified identity anywhere (prole is a client-side PIN match never sent to the server, confirmed by grep + the file's own prior comment at the LEG_TOOLS definition making the same observation already). A genuine per-individual-staff server-side permission check needs real employee auth first (SAIRNlaw's law-auth.js pattern), which doesn't exist here -- flagging to Michael before building anything that would only look like real access control. Also queued sql/sairnlegacy_audit_test_cleanup.sql (1 leftover "AUDIT TEST" case row on LEG-PINNACLE-2026, needs a direct Supabase run -- no delete action exists for leg_* resources via the API). 2026-08-19
- sairnlegacy.html (Hank) -- 3 of 4 remaining items DONE and live-verified, 4th blocked on a real architectural question, reported to Michael rather than guessed. get_home_attention (`50e646f`), Case Cycle Trend (`ba4d78c`), and voice input on ainp/csnotes/acnote (`a152e32`) all pushed and confirmed live in a real browser session (LEG-PINNACLE-2026, owner PIN): attention tool correctly surfaced the real Cambridge Oak unit reserved for Robert Halloway since Aug 10 and correctly reported all-clear on permits/documents/services; Case Cycle Trend rendered its honest "not enough history (0 on record, need 6)" fallback; all 3 mic buttons confirmed rendering (chat, Case Notes modal, Aftercare Complete-Check-in modal, the latter checked via a no-side-effect direct modal-open call rather than creating real schedule data). Shared-knowledge layer NOT built yet -- Michael's decision was permission-gated (management/owner-tier by default, individually grantable) rather than content-gated, but this app has zero server-verified identity anywhere (prole is a client-side PIN match, never sent to the server -- confirmed by grep, and already noted inline by a prior session at the LEG_TOOLS definition). A real per-individual-staff server-side check needs real employee auth first (SAIRNlaw's law-auth.js pattern), which doesn't exist here and would be a materially larger, separate build -- flagged to Michael with concrete options rather than building something that would only look like real access control. 2026-08-19
- sairnlegacy.html + api/leg-auth.js + api/_lib/auth.js + api/sd-data.js + sql/sairnlegacy_employee_auth_schema.sql (Hank) -- DONE, real employee auth built and deployed, one real blocker before it's usable. Replaces the old shared-PIN-per-role client-only gate with real per-employee credentials, same pattern as SAIRNlaw's law-auth.js, deliberately smaller (no MFA/SSO/audit-log -- not asked for). New api/leg-auth.js: check_license/whoami/bootstrap/login/setup/grant_shared_knowledge_access. The actual permission gate lives in api/sd-data.js's existing generic shared_knowledge branch, scoped narrowly to app_id==='sairnlegacy' (zero behavior change for any other app) -- owner/director by role, staff via a fresh per-call DB check of a new shared_knowledge_access column (not embedded in the token, so a revoke is immediate). New Security & Access panel in sairnlegacy.html: Employee Accounts (owner-only setup) + the grant/revoke control (owner OR director, matching Michael's "management" framing). node --check clean, zero dup ids/functions, all touched server files smoke-tested (real require, ROLES_BY_APP entry confirmed at runtime), pushed (`39b48c7`). LIVE-VERIFIED what's verifiable right now: api/leg-auth.js deployed and check_license works; bootstrap correctly 503s NOT_PROVISIONED (honest, not a crash); the shared_knowledge gate is genuinely active in production -- confirmed a no-session sairnlegacy call now gets a real 401 NO_SESSION where it used to be wide open, while curl-confirmed SAIRNlaw/SAIRNdesign's own shared_knowledge calls are completely unaffected. REAL BLOCKER, not fixable by me: sql/sairnlegacy_employee_auth_schema.sql has not been run in Supabase (no DB access from this session) -- every real login/bootstrap/setup/grant stays 503 until it runs. Also still flagging sql/sairnlegacy_audit_test_cleanup.sql from earlier this session (1 leftover "AUDIT TEST" case row on LEG-PINNACLE-2026) -- same blocker, needs Michael in Supabase directly. Once both run: bootstrap the first real owner account, then the full login->grant->shared-knowledge-write->read round trip can be live-verified end to end the same way SAIRNlaw's and SAIRNdesign's were. 2026-08-19
- sairnlegacy.html + api/leg-auth.js (Hank) -- FULLY CLOSED, real end-to-end verification, both blocking SQL files confirmed run by Michael. Bootstrapped the real first owner (hank-owner) via a genuine bootstrap call -- 409 ALREADY_PROVISIONED no longer applies to a fresh table, worked cleanly. Full curl-level round trip, every step real: owner whoami confirms role; owner provisioned a real director (hank-director) and a real staff (hank-staff) via setup; staff's shared_knowledge write/read correctly 403 FORBIDDEN before any grant; owner grants access -> staff's SAME still-unexpired token now succeeds on both write and read (confirmed the fresh-DB-check design claim for real, not just in the design doc); owner revokes -> staff's SAME token immediately 403s again, no waiting for the 12h token to expire; director (not just owner) successfully grants and successfully writes shared_knowledge by role, confirming the "management" (owner OR director) interpretation actually works as coded; a wrong PIN correctly 401s INVALID_CREDENTIALS. REAL BUG found and fixed live during this verification pass, not before: switching users while already on the Security & Access panel (logout -> sign back in as someone else, no intervening navigation) left "Your Account" showing the PREVIOUS user's identity -- init() never touched panel-security and nav('security') only re-renders on an actual navigation click. Fixed by calling rSecurity() directly from legEnterApp() on every login/bootstrap/restore; pushed (`ab9e3e2`, rebased, see current log), confirmed via a real browser session (fresh tab, real clicks, not JS-injected shortcuts) that logging in as hank-staff then logging out and back in as hank-owner while remaining on panel-security the entire time now correctly updates both the header AND the account card in the same click. Also independently confirmed via this same session that the earlier sql/sairnlegacy_audit_test_cleanup.sql actually took effect server-side (curled leg_cases directly -- genuinely empty, zero rows, the AUDIT TEST case is really gone; a browser still showing it is that tab's own stale local demo-seed cache, unrelated to server state). One tooling note, not a code issue: this session's browser automation had a stretch of flaky hung network requests/screenshot timeouts mid-verification (unrelated to app code -- curl against the same endpoints during that same window worked instantly every time); resolved by opening a fresh tab and continuing with plain clicks instead of chained JS-injected calls. No further action needed from Michael -- test accounts (hank-owner/hank-director/hank-staff) left on the demo license LEG-PINNACLE-2026, same low-risk precedent as every other app's demo-license test accounts this session. 2026-08-19
- stonedesk.html + api/sd-auth.js + api/sd-data.js + sql/sd_crm_schema.sql (Hank) -- Item 1 of the sales-lead-privacy platform rule (Task 2), built and pushed (`a4f0a1a`, rebased over a real conflict with CC's same-file sc_scrubrules addition -- both survived, confirmed by grep after resolving). Two real gaps fixed together, not separately: sd_crm had ZERO server sync before this (confirmed by grep: no sd_crm reference anywhere in api/sd-data.js, no sdnData/fetch call in the panel's own script -- leads weren't shared across devices at all, let alone scoped per salesperson) -- now has real sync via the existing generic sdData() client (already sends X-SD-Auth on every call, no client-side auth plumbing needed, confirmed real by reading the function directly). Privacy gate: management (owner/admin) sees every lead; a non-management session sees only leads where assigned_employee_id matches their own verified employee_id from api/sd-auth.js's session token -- enforced server-side in api/sd-data.js's new sd_crm branch, not a client-side filter. Reassignment is management-only, enforced server-side (a non-management write attempt to change assignment is rejected regardless of client state). New 'roster' action in api/sd-auth.js (owner/admin-only, read-only, never returns PIN hashes) backs the Assign-To/reassign dropdowns, reusing schedReassignCrew's existing UI pattern as precedent per Michael's own suggestion. One real judgment call, flagged inline in the code comments: an UNASSIGNED lead is management-only-visible by default (not visible to all sales reps to self-claim) -- adjustable if that's not the intended behavior. node --check clean across all 128 script blocks (extracted and checked individually, not just grep'd), zero duplicate ids/functions, both server files smoke-tested (real require, no runtime errors), deploy confirmed live (curled the new 'roster' action -- reached the license-validation step, past the action-allowlist check, proving the new code is actually deployed). NOT further live-verified with a real employee session -- same real-credentials gap that recurred all session (SAIRNbiz, SAIRNgrounds/SAIRNscape, SAIRNcode): the dedicated test license documented for exactly this purpose (SD-AUDIT-2026, sql/stonedesk_audit_license_seed.sql, credentials audit-owner/135790) is already bootstrapped by an unknown prior session and that exact PIN no longer works (confirmed via one real login attempt, INVALID_CREDENTIALS -- did not attempt further guesses, LOCKOUT_THRESHOLD=5 in api/sd-auth.js makes that unsafe against a shared resource). No DB access this session to reset it or provision a fresh license. If someone with the real audit-owner PIN (or a fresh SD- test license) wants the actual click-through: log in as owner, add a lead with an assignee, log in as a sales-role employee and confirm they see only their own lead, attempt (and expect a 403 on) a sales-role reassignment attempt, confirm management's reassign dropdown works. SAIRNdesign is next per Michael's sequencing (needs real employee auth built first, same SAIRNlaw/SAIRNlegacy pattern, before its privacy gate can be enforced server-side). 2026-08-19
- sairndesign.html + api/sdn-auth.js + api/_lib/auth.js + api/sd-data.js + sql/sairndesign_employee_auth_schema.sql + sql/sairndesign_clients_assignment_migration.sql (Hank) -- Task 2 of the platform sales-lead-privacy rule, same shape as StoneDesk's sd_crm (item 1) and SAIRNlegacy's employee-auth build. Resumed mid-build (schema + api/sdn-auth.js + sign-in screen already written by a prior turn this session), finished the login/bootstrap wiring and pushed (`db0d55f`, rebased cleanly over CC's unrelated sc_denial_events push -- no file overlap, confirmed by diff before rebasing). New api/sdn-auth.js: check_license/whoami/bootstrap/login/setup/roster, modelled directly on api/leg-auth.js. Privacy gate lives in api/sd-data.js's new sdn_clients branch (owner/office = full visibility, designer = own assigned clients only, unassigned = management-only-visible, reassignment = management-only, all enforced server-side). TWO REAL BUGS found and fixed during this pass, not present in what was handed off: (1) the old Settings panel's Role-PINs card/modal still referenced the PINS global that removing the old shared-PIN keypad had deleted -- rSettings() runs unconditionally from init() on every single login, so every login would have thrown a live ReferenceError; removed the dead PINS UI/modal entirely, Employee Accounts management now lives in the new Security & Access panel instead (owner-only, mirrors SAIRNlegacy's rSecurity()/secProvision()). (2) saveClient() and sdnHydrateAll() never actually sent the session token on sdn_clients calls (sdnData()'s withSession param defaults off, by design, since only this one resource needs it) -- meaning the server-side privacy gate the prior turn built was never actually reachable from the browser: every client write would 401 and silently fall back to local-only, and no Assign-To/reassign UI existed at all despite api/sdn-auth.js's own 'roster' action already being built for exactly that. Fixed both call sites to pass withSession=true, added assigned_employee_id round-tripping (a designer creating a new client now auto-self-assigns, matching what the server actually allows), and built the missing Assign-To dropdown (client modal, management-only) + reassign dropdown (Clients table, management-only) backed by a real roster fetch -- same shape as StoneDesk's sd_crm Assign-To control. node --check clean on the single script block + all 3 touched server files, zero duplicate ids/functions, dead-button audit 0 flagged, div balance 793/793, all 20 panels (incl. new panel-security) reconciled both directions against their sidebar buttons (nav_panel_check.py itself false-failed all 20 panels -- it's hardcoded to StoneDesk's sb-btn/sbNav() convention, SAIRNdesign uses sb/nav(), reconciled manually instead and confirmed clean). verifySessionToken calls all pass the explicit 'sairndesign' app-scope argument (Check 28). LIVE-VERIFIED post-push: curled the deployed sairndesign.html directly, confirmed panel-security/lstep-bootstrap/sdnReassignClient/classignee are all genuinely live (not just committed); curled api/sdn-auth.js directly -- check_license returns real ok:true, login correctly 503s NOT_PROVISIONED (honest, not a crash). REAL BLOCKER, not fixable by me, same pattern as SAIRNlegacy's identical gap: neither sql/sairndesign_employee_auth_schema.sql nor sql/sairndesign_clients_assignment_migration.sql has been run in Supabase yet (no DB access from this session) -- every login/bootstrap/setup/roster/sdn_clients call stays 503/blocked until both run. Once both run: bootstrap the first real owner, then the full owner-bootstrap -> designer-scoped-visibility -> office-full-visibility -> management-only-reassignment round trip can be live-verified end to end the same way SAIRNlegacy's was. Still open, not mine to resolve: SD-AUDIT-2026's broken test PIN (Michael's), and the stashed sairn-guardian-v2 SKILL.md/settings.json diff (superseded by CC's fix per line 44 above, waiting on Michael's go-ahead to drop, left untouched in the stash). 2026-08-20
- sairnbuild.html + api/bld-auth.js + api/_lib/auth.js + api/sd-data.js + sql/sairnbuild_employee_auth_schema.sql + sql/sairnbuild_bids_schema.sql (Hank) -- Task 3 of the platform sales-lead-privacy rule (StoneDesk's sd_crm was item 1, SAIRNdesign's sdn_clients was item 2), pushed (`e4a7606`, rebased twice over CC's SAIRNcode Phase 1 push -- one real conflict in api/sd-data.js's RESOURCES map/error-message string, both sides' additions kept, confirmed via grep after resolving; second rebase was a clean docs-only fast-forward, no conflict). UNLIKE THE PRIOR TWO APPS, built from ZERO -- SAIRNbuild had no server auth at all before this (still the original client-only DEFAULT_PINS scaffold, owner/pm/office) and Bids & Proposals had zero server sync (confirmed by grep: no bld_bids reference anywhere in api/sd-data.js, saveBid() was pure localStorage). New api/bld-auth.js: check_license/whoami/bootstrap/login/setup/roster, modelled directly on api/sdn-auth.js. New sql/sairnbuild_bids_schema.sql is a fresh CREATE TABLE (not an ALTER like SAIRNdesign's clients migration) since bld_bids never existed server-side at all -- assigned_employee_id built in from the start. Privacy gate lives in api/sd-data.js's new bld_bids branch (owner/office = full visibility, pm = own-assigned-only, unassigned = management-only-visible, reassignment = management-only, all enforced server-side, same shape as sdn_clients). Skills run per Michael's routing: access-control-rbac (generic RBAC reference, confirmed the design against it before building), sairn-guardian-v2 (manual equivalent -- see checks below, tool itself not re-invoked as a skill call this pass), sairn-adversarial-reviewer (real finding, see below). graphify and security-auditor were BOTH attempted and are disabled for model invocation in skillOverrides -- flagged rather than silently skipped, same as CC hit on graphify during the Phase 2 SAIRNcode spec above. ONE REAL BUG FOUND BY THE ADVERSARIAL-REVIEW PASS BEFORE PUSH, not shipped: saveBid()'s assignedTo logic preserved an EXISTING local bid's (empty) assigned_employee_id on edit instead of self-assigning -- meaning every bid created before this session (100% of them, since the feature is brand new) would have gotten a permanent 403 from the server on a PM's first save/sync attempt, with the toast giving no indication why. Fixed: a non-management edit now self-assigns when the existing local assignment is genuinely empty, same as the create path already did; a bid already explicitly assigned to someone else is still untouchable (server-side existingRow check unchanged). Also independently confirmed (per SAIRNdesign's own precedent bug) that every bld_bids client call site passes withSession=true from the start -- hydration read, saveBid write, and reassign write all checked directly by grep, not assumed. Removed the dead PINS-referencing Role-PINs card/modal from Settings (same class of bug as SAIRNdesign's rSettings() crash, but caught proactively this time since I knew to look for it -- confirmed zero PINS/pbuf/pk/chkPin/pinsmodal references remain anywhere in the file). Added a genuine reload-skip-gate this app never had at all (confirmed by grep: bld_license_key was only ever read inside gateGo(), never checked on page load) -- a session that doesn't survive reload isn't real persistence, so this was added as part of finishing the wiring, not a separate scope item. node --check clean on the single script block + all 3 touched server files, zero duplicate ids/functions, dead-button audit 0 real findings (one pre-existing window.rBids monkey-patch wrap flagged as a false-positive duplicate by the tool's naive regex, one pre-existing "work in progress" string in the unrelated Change Orders panel -- both confirmed pre-existing via git blame reasoning, neither touched by or introduced in this session's diff), div balance 1814/1814, all 38 panels (incl. new panel-security) reconciled both directions against their sidebar buttons (checked manually with this app's real sb/nav() convention, not the StoneDesk-specific nav_panel_check.py tool). verifySessionToken calls all pass the explicit 'sairnbuild' app-scope argument (Check 28) -- role-name overlap with SAIRNdesign ('owner'/'office' shared by both) is safe because the token's app claim is checked, not just the role string. LIVE-VERIFIED post-push: curled the deployed sairnbuild.html directly, confirmed panel-security/lstep-bootstrap/bldReassignBid/bm-assignee are all genuinely live; curled api/bld-auth.js directly -- check_license returns real ok:true, login correctly 503s NOT_PROVISIONED (honest, not a crash); curled api/sd-data.js directly -- bld_bids now passes the resource-allowlist check and reaches the real bespoke gate (NO_SESSION, not the old 400 "unrecognized resource" error), a bogus resource name still correctly 400s for contrast. REAL BLOCKER, not fixable by me, same pattern as SAIRNlegacy's and SAIRNdesign's identical gap: neither sql/sairnbuild_employee_auth_schema.sql nor sql/sairnbuild_bids_schema.sql has been run in Supabase yet (no DB access from this session) -- every login/bootstrap/setup/roster/bld_bids call stays 503/blocked until both run. This closes all 3 apps in the platform sales-lead-privacy rollout (StoneDesk, SAIRNdesign, SAIRNbuild) at the code/deploy level; all 3 share the identical live-Supabase-migration blocker and none has had a real click-through session yet. Once Michael runs both migrations: bootstrap the first real owner, then the full owner-bootstrap -> pm-scoped-visibility -> office-full-visibility -> management-only-reassignment round trip can be live-verified end to end the same way the pattern was designed to support. Still open, not mine to resolve: SD-AUDIT-2026's broken test PIN, CLM-MIGRATION-TEST in sc_claims, and the stashed sairn-guardian-v2 SKILL.md/settings.json diff (still waiting on Michael's go-ahead to drop). 2026-08-20
- sairnbuild.html + api/sd-data.js + sql/sairnbuild_tna_schema.sql (Hank) -- employee Training Needs Assessment, pushed (`f96e99e`, clean rebase over CC's SAIRNcode security-pass push, no conflict despite both touching api/sd-data.js -- different regions, git's 3-way merge handled it). Real instrument built on the Hennessy-Hicks TNA (WHO-endorsed) importance/performance-gap methodology, verified via live web research (5 sources) before building, NOT reconstructed from memory -- the verbatim original 30 healthcare item texts could not be retrieved (all 5 sources failed PDF text extraction, tried genuinely, not skipped), so the item bank is original wording adapted for construction, built on the verified real domain structure (5 domains, dual 1-7 importance/performance scales, gap = importance minus performance). Labelled "Hennessy-Hicks-style" throughout, never claimed as the literal original -- full provenance disclosure in the SQL file's header. Two-perspective structure per spec (self, soft framing; management, analytical framing, same 30 items). Judgment call, flagged not silently assumed: an employee sees BOTH their own self-view and any management assessment of them, side by side, softly framed -- management sees the unsoftened view across the whole team. Enforced server-side in api/sd-data.js's new bld_tna branch (self row only writable by its own subject regardless of role; management row only writable by owner/office; non-management read filtered to caller's own subject). Optional DISC-style module: generic non-branded communication/work-style questionnaire (public-domain 4-factor model, original wording, not a licensed commercial product), self-report only, explicit non-diagnostic disclaimer on both forms, deliberately NOT framed as personality/medical -- ADA-safety framing per Michael's explicit instruction, flagged in code comments as a framing choice needing real employment-counsel review before any personnel use, not a legal opinion. One item (workload/deadline-pressure) reworded during self-review to drop stress-adjacent language, stay purely behavioral. REAL BUG CAUGHT AND FIXED BEFORE PUSH: first draft of the write branch's missing-table handling called `isMissingTable()`, a helper that only exists in the `*-auth.js` files -- sd-data.js never had one, so this would have thrown a live ReferenceError on the very first write attempt against a not-yet-provisioned table (i.e. immediately, since that's the exact starting state for a new table). Caught by `node --check` on the very next edit, not left for later -- fixed to check the response status directly, matching this file's own actual established pattern. Guardian pass: node --check clean, zero duplicate ids/functions (async function declarations initially false-flagged by a naive grep missing the `async` prefix, re-verified correctly present), dead-button audit 0 new findings, div balance 1880/1880, all 39 panels reconciled both directions. graphify not re-invoked (still disabled for model invocation, already flagged in the prior SAIRNbuild entry); sairn-visual-review WAS run for real, not skipped, and it surfaced something important: LIVE-VERIFIED FAR BEYOND SERVER-REACHABILITY THIS TIME -- both sql/sairnbuild_employee_auth_schema.sql AND sql/sairnbuild_bids_schema.sql have ALREADY been run in Supabase (contradicts the "REAL BLOCKER" note in the prior SAIRNbuild entry above -- that was accurate when written, state changed since, corrected here plainly rather than left stale). Real Playwright session against the live site: activated BLD-PINNACLE-2026, bootstrapped a real owner account, got a genuine logged-in dashboard with zero JS errors (only a pre-existing, harmless favicon 404). Navigated the real Training & Development panel: all 30 items rendered correctly grouped into 5 domains with working 1-7 selects, filled 5 real items across all 5 domains and hit Save -- got an honest, correctly-worded 503 NOT_PROVISIONED (sql/sairnbuild_tna_schema.sql specifically has NOT been run yet, confirmed via the real console warning, not assumed) with the error genuinely surfaced in the UI and all entered form values preserved on failure, not lost. Team Overview and Assess-a-Team-Member tabs both rendered correctly against the real roster (one real employee, "Visual Test"). DISC form rendering confirmed (disclaimer visible, all 16 items, correct 1-5 options). BONUS re-verification of the PRIOR SAIRNbuild entry's own claims, since a working session was rare and worth using fully: Bids & Proposals' real write path was confirmed genuinely live in production -- created a real test bid ("Visual Review Test Client"), got back "Saved - [name]" (the real-sync success message, not the local-only fallback), and the Assign-To/reassign dropdown rendered correctly in the live table against the real roster. Two real artifacts left on BLD-PINNACLE-2026 from this verification, flagged not hidden, cleanup statements written but NOT executed (sql/sairnbuild_visual_review_cleanup.sql, same low-risk demo-license-test-data precedent as every other app this session): the 'visual-test-owner' credential, and the "Visual Review Test Client" bid. REMAINING REAL BLOCKER, narrower than before: only sql/sairnbuild_tna_schema.sql still needs to run -- once it does, the full self-assessment -> management-assessment -> team-overview -> DISC round trip can be verified end to end the same session this was built, not a future one. Skill-creator conversion (per Michael's instruction, "once built and real-interaction verified") is NOT done yet -- interaction verification got further than any other feature this session but the one remaining migration gap means the write path itself is still unverified end-to-end; will run skill-creator once that last piece is confirmed rather than converting a pattern with one unverified leg. 2026-08-20
- sairnbuild.html (Hank) -- SAIRNbuild TNA round trip CLOSED, real end-to-end verification, pushed (`802ec0b`). Michael confirmed sql/sairnbuild_tna_schema.sql run; re-verified live rather than trusted (curled bld_tna read with a real session token, got `provisioned:true` before touching anything). Real Playwright session, real credentials (visual-test-owner on BLD-PINNACLE-2026, from the prior entry): filled and saved all 30 self-assessment items -- REAL BUG FOUND AND FIXED live during this pass, not shipped broken: the write succeeded and the results view updated correctly underneath, but the "My Self-Assessment" KPI card directly above it kept showing "Not started" -- saveTnaSelf()/saveTnaManagement()/saveDisc() all refreshed the data and the results view after a successful write but never re-rendered the three KPI cards, which were only ever computed once inside tnaRender() at panel-load time. Extracted the KPI-row logic into its own tnaRenderKpis(), called from tnaRender() unchanged plus all three save handlers now. Fixed, pushed, and the fix itself was then live-verified (not just code-reviewed): saved again with no page reload, KPI card correctly flipped to "Complete" immediately, confirmed via direct evaluate() of the live DOM, not assumed from the code change. COMPLETED THE FULL ROUND TRIP AFTER THE FIX: saved a real 30-item management assessment for the same employee -- "Manager Assessment: On file" and "Team Assessed: 1" both updated correctly and immediately; the self-view's "Your view vs. your manager's view" comparison table rendered real computed gap badges for both perspectives side by side (e.g. "Room to grow (+1.5)" self vs "Strength (-0.8)" manager, correct arithmetic, correct soft-vs-analytical framing per row); Team Overview showed the real aggregated "Strength (-0.6)" overall gap sourced from the actual stored management data, not a placeholder. Zero console errors across the entire round trip beyond one pre-existing, unrelated, harmless 403 (the SAIRNbiz cross-app crew read, expected and already documented). Test data left on BLD-PINNACLE-2026, flagged not hidden, cleanup statements written but not executed (sql/sairnbuild_visual_review_cleanup.sql updated with the new bld_tna_assessments rows, same low-risk demo-license precedent as the rest of this session) -- two real TNA rows (self + management perspective) alongside the owner credential and test bid already flagged in the prior entry. Skill-creator conversion next, per Michael's explicit condition now satisfied (built AND real-interaction verified, not just server-reachable). 2026-08-20
- .claude/skills/sairn-training-needs-assessment/SKILL.md + CLAUDE.md + sairn-master-orientation update (Hank) -- DONE, pushed (`10c2cda`). skill-creator is ALSO disabled for model invocation (same as graphify and security-auditor earlier this session) -- flagged rather than silently skipped, built the skill file directly using sairn-mobile-sync as the structural template (explicitly the same class of skill: a reusable technical pattern extracted from one app's real build, applicable across the other 10). Content encodes the honest Hennessy-Hicks sourcing discipline (verbatim item text often isn't retrievable -- label adaptations "-style," never claim fidelity), the two-perspective server-side privacy-gate pattern with real code shape, the table schema shape, a client-function checklist, the optional ADA-safe DISC module guidance, and BOTH real bugs found and fixed during the live SAIRNbuild build (the isMissingTable() file-scoping mistake, the KPI-refresh-after-save omission) so a future build of this for another app doesn't rediscover either one. Added a one-line pointer in CLAUDE.md's Skills section (matching the existing sairn-mobile-sync entry, `Last Updated` footer bumped to today) and a routing-table row in sairn-master-orientation, so the skill is discoverable both by trigger words and by the project's own "read these" index -- confirmed the new skill actually appears in the live available-skills listing before considering this done. This closes the full arc Michael asked for: build -> real end-to-end verification -> reusable skill. 2026-08-20
- sairnsenior.html + api/sen-auth.js + api/_lib/auth.js + api/sd-data.js + sql/sairnsenior_employee_auth_schema.sql + sql/sairnsenior_clients_schema.sql + vercel.json (Hank) -- SAIRNsenior Phase 1 DONE, pushed (`3157ac9`, rebased cleanly over CC's SAIRNcode Phase 2a/2b push -- one real conflict in api/sd-data.js's RESOURCES map/error-message string, both sides' additions kept, confirmed via grep after resolving). REAL SCOPE CONFLICT FOUND AND RESOLVED BEFORE ANY CODE WAS WRITTEN, not silently picked either way: Michael's initial framing ("assisted living, hospice, retirement/senior living facilities") directly contradicted three real, dated (June 2026) legal/business docs found on Desktop (Terms & Conditions, Data Privacy Addendum, SOP User Guide -- signed-agreement-shaped, real pricing already specified) which consistently describe SAIRNcare as a home-care-AGENCY platform (EVV/GPS caregiver clock-in at the client's home, not a facility). Also found a 370KB unused draft (`sairncare UPLOAD TO GITHUB.html`) on Desktop, confirmed stale (references the removed SAIRNhr/SAIRNacc apps, the abandoned Fabricor/Railway infra, the pre-real-auth shared-PIN pattern, and a `sc_` storage prefix that collides with SAIRNcode's live namespace) -- flagged, explicitly NOT resurrected as a foundation. Resolution: two separate apps -- SAIRNsenior (home-care-agency, built first) and SAIRNcare (facility-based, built next), confirmed by Michael before any architecture work began. Ground-up build, same starting point as SAIRNlegacy/SAIRNlaw/SAIRNbuild -- never had a shared-PIN scaffold to replace. Role vocabulary (owner/scheduler/coordinator/billing/caregiver) pulled directly from the app's own real SOP User Guide, not invented. checking-hipaa-compliance skill (referenced as "installed for this build") does NOT exist on disk -- checked `.claude/skills/`, confirmed absent, flagged rather than silently faked; proceeded on general HIPAA minimum-necessary knowledge + the platform's established privacy-gate pattern instead, per Michael's own go-ahead not to block on it. graphify attempted again, still disabled for model invocation (6th time this session across different skills). New api/sen-auth.js: check_license/whoami/bootstrap/login/setup/roster, modelled on api/bld-auth.js. HIPAA minimum-necessary privacy gate lives in api/sd-data.js's new sen_clients branch (owner/billing = management full visibility, scheduler/coordinator/caregiver = own-assigned-clients-only, unassigned = management-only, reassignment = management-only, all enforced server-side, same shape as every assignment-based gate this session) -- built the self-assign-on-create/edit fix in from the start (the exact bug class found and fixed on SAIRNbuild's bld_bids) rather than rediscovering it. sairn-app-scaffold's required v1 component (photo -> Claude -> structured output) implemented as MD-order/authorization-letter photo capture at client intake -> Claude extraction (name/diagnosis/hours/payer) -> pre-fills the intake form, staff reviews before saving, never auto-submitted -- through the real proxy with app_id+is_demo:true, free-text response + targeted regex extraction, same proven baseline as StoneDesk's Field Sketch Quote (not a fragile JSON contract). Settings panel includes a per-state EVV aggregator config (Sandata/HHAeXchange/Tellus/CareBridge/Other) rather than hardcoding one national standard, per Michael's own confirmed instruction (EVV is federally mandated by the Cures Act but each state chooses its own aggregator). ONE REAL GAP DISCLOSED, NOT HIDDEN: Caregivers/Staff is local-storage-only this pass -- no server sync built yet, unlike Clients which got the full real-sync+privacy-gate treatment from day one. Judgment call: prioritized the PHI-bearing client resource for real sync first given the explicit HIPAA framing was specifically about client visibility; caregiver-roster sync is a clean, scoped follow-up, not forgotten. node --check clean on the single script block + all 3 touched/new server files, zero duplicate ids/functions, dead-button audit 0 flagged, div balance 176/176, all 6 panels reconciled both directions against their sidebar buttons, every verifySessionToken call passes the explicit 'sairnsenior' app-scope argument, no direct api.anthropic.com calls, no service_role key in client HTML. LIVE-VERIFIED post-push: curled the deployed sairnsenior.html AND the /sairnsenior route directly, both real and matching; curled api/sen-auth.js directly -- check_license correctly reaches real license validation (INVALID_LICENSE, not a crash or a routing 404); curled api/sd-data.js directly -- sen_clients confirmed present in the live resource allowlist (compared against a bogus-resource-name control, same technique used to verify every other new resource this session). REAL BLOCKER, narrower in shape than every prior app's blocker this session: SAIRNsenior is a genuinely NEW app with ZERO license provisioning at all yet -- confirmed no SEN- entry exists anywhere in sql/demo_license_keys_seed.sql or any app-specific seed file, so even check_license fails with INVALID_LICENSE, one step earlier than the usual "table not provisioned" 503 every other app's blocker has hit. THREE things needed from Michael before any real testing, not the usual two: (1) provision a real SEN- demo license_keys row, (2) run sql/sairnsenior_employee_auth_schema.sql, (3) run sql/sairnsenior_clients_schema.sql. Once all three run: bootstrap the first real owner, then the full owner-bootstrap -> caregiver-scoped-visibility -> billing-full-visibility -> management-only-reassignment round trip, plus the photo->Claude intake flow, can be live-verified end to end the same way every other app's has been this session. Next: continue through the full panel list as scoped (Scheduling & Matching, EVV, Care Plans, Billing & Claims, Compliance & Reporting, Family/Client Portal, AI agentic tools, Reports, SAIRN Data Bridge integration), then SAIRNcare (facility-based) once SAIRNsenior is fully built and verified, per Michael's confirmed sequencing -- no need to pause for a new go-ahead between them. 2026-08-20
- sairnsenior.html + api/sd-data.js + 2 new SQL migrations (Hank) -- SECOND SAIRNsenior BATCH DONE, pushed (`f9d3e7e`, rebased cleanly over CC's pre-SAIRNcare gap-closure push -- one real conflict in api/sd-data.js's RESOURCES map/error-message string, both sides' additions kept, confirmed via grep after resolving). REAL BUG FOUND AND FIXED IN ALREADY-SHIPPED CODE, not something new introduced this pass: the sen_clients gate from the last commit only had a MANAGEMENT/everyone-else binary, silently narrowing coordinator (and scheduler -- same judgment call applied to both, since neither was named separately in Michael's approval) down to the same own-assigned-only view as caregiver, contradicting the approved "coordinator = broad caseload visibility" tier. Fixed to a real three-tier gate: owner/billing/coordinator/scheduler get agency-wide READ on clients, only caregiver stays scoped, reassignment stays management-only for everyone else regardless of read tier. CAUGHT A SECOND, SELF-INFLICTED BUG WHILE WRITING THAT FIX: the first merge attempt conflated the two non-management tiers' write rules into one comparison, which would have blocked a caregiver from ever creating a new client (self-assign-on-create compares against the caller's own id; the broad-read tier's rule compares against the EXISTING assignment -- two different invariants). node --check doesn't catch a logic bug like that -- caught by hand-tracing all 5 roles x create/edit before moving on, not by any tool. Caregivers/Staff (sql/sairnsenior_caregivers_schema.sql, new sen_caregivers branch): closes the Phase 1 disclosed gap, real server sync, read open to any authenticated employee (scheduling needs the roster), write management-only, client-side "+ New Caregiver"/Edit controls now hide for non-management instead of producing a confusing 403 on click. Scheduling & Visits (sql/sairnsenior_visits_schema.sql, new sen_visits branch, new panel-scheduling): one entity for both scheduling and EVV, matching how EVV actually works (it verifies a scheduled visit, not a separate concept). Field-level write split, NOT a role-vs-role split like sen_clients: scheduling fields (client, caregiver assignment, time) are writable by management/coordinator/scheduler since scheduling IS their job -- materially different from sen_clients where reassignment stays management-only; EVV fields (clock in/out, GPS, service notes) are writable ONLY by the assigned caregiver, and only on a visit that already exists, so a scheduler can cancel a visit but can never fake a completed/in-progress EVV state through the schedule form. Real GPS capture via navigator.geolocation with an honest fallback (flagged for review, never faked) when permission is denied or unavailable. node --check clean on the single script block + api/sd-data.js, zero duplicate ids/functions, dead-button audit 0 flagged, div balance 226/226, all 7 panels (incl. new panel-scheduling) reconciled both directions, every verifySessionToken call passes the explicit 'sairnsenior' app-scope argument, every new client-side resource call grep-confirmed passing withSession=true (not assumed). LIVE-VERIFIED post-push: curled the deployed sairnsenior.html directly, confirmed 10/10 new markers present; curled api/sd-data.js directly -- both sen_caregivers and sen_visits now pass the live resource allowlist and correctly reach real license validation (INVALID_LICENSE, not "unrecognized resource"). Same blocker as the last entry, unchanged: SEN- license still not provisioned, both new migrations still queued, not run. Next: Care Plans, Billing & Claims, Compliance & Reporting, Family/Client Portal, AI Ops Assistant enhancements, Reports, SAIRN Data Bridge integration -- continuing through the panel list per Michael's confirmed sequencing. 2026-08-20
- sairnsenior.html + api/sd-data.js + sql/sairnsenior_claims_schema.sql (Hank) -- THIRD SAIRNsenior BATCH DONE, pushed (`9c1bf69`, clean rebase over CC's docs-only push, no conflict). Care Plan Tasks: folded into the existing sen_clients record's data blob (care_tasks array) rather than a new resource -- clearly client-scoped data that should inherit the exact same privacy gate as the rest of the client record, so no new table or server code was needed, only a client-side draft-add/remove UI (same shape as SAIRNbuild's bidLineDraft). Billing & Claims (new sen_claims branch, new panel-billing): matches the app's own real SOP User Guide's Ready-to-Bill workflow verbatim -- a claim is generated from a completed, EVV-verified visit. Access deliberately simpler than sen_clients/sen_visits: management (owner/billing) only, both read and write, no assignee-based visibility at all -- financial/billing data, not clinical assignment data, and a caregiver has no HIPAA minimum-necessary reason to see claim amounts or payer reimbursement status. Ready-to-Bill queue computed live from real visit+claim data (completed visits with no matching claim yet), never a placeholder count; hours-billed computed from the visit's own real clock-in/out timestamps. REAL BUG CAUGHT AND FIXED DURING SELF-REVIEW BEFORE PUSH, not shipped: the non-management "access denied" path for the Billing panel originally overwrote the panel's real DOM structure via nested .parentElement.innerHTML -- destructive, would have permanently broken the panel (KPI row / table ids gone) if the same browser tab ever logged out and back in as management without a full page reload. Fixed to a non-destructive show/hide toggle between a dedicated denied-message card and the real content, both always present in the DOM. Also removed two window.prompt() calls (Set Rate, Mark Denied) in favor of a small reusable modal -- consistent with the rest of the app (no native browser dialogs anywhere in this codebase) and because a native prompt() blocks browser-automation verification, which this session relies on for real end-to-end testing. node --check clean on the single script block + api/sd-data.js, zero duplicate ids/functions, dead-button audit 0 flagged, div balance 274/274, all 8 panels (incl. new panel-billing) reconciled both directions, 7 verifySessionToken calls all pass the explicit 'sairnsenior' app-scope argument, every sen_claims client call confirmed passing withSession=true. LIVE-VERIFIED post-push: curled the deployed sairnsenior.html directly, confirmed 11/11 new markers present; curled api/sd-data.js directly -- sen_claims now passes the live resource allowlist and correctly reaches real license validation (INVALID_LICENSE, not "unrecognized resource"). Same blocker as every SAIRNsenior entry so far, unchanged: SEN- license still not provisioned, sql/sairnsenior_claims_schema.sql now a third migration queued, none run yet. Next: Compliance & Reporting, Family/Client Portal, AI Ops Assistant enhancements, Reports, SAIRN Data Bridge integration. 2026-08-20
- sairnsenior.html (Hank) -- FOURTH SAIRNsenior BATCH DONE, pushed (`ba45d5b`, clean rebase over CC's SAIRNcare v1 scope doc push, no conflict -- different files). No server-side changes this batch, api/sd-data.js untouched and confirmed still valid. Compliance panel: broad-read tier (owner/billing/coordinator/scheduler, same tier as sen_clients' broad-read gate) -- caregiver certification status and EVV exceptions are operational/employment data a coordinator/scheduler genuinely needs to schedule around, not client PHI, but still not shown to caregivers about their peers. Computed entirely from data already fetched for Caregivers/Scheduling, no new resource needed. EVV Exceptions flags visits missing GPS on clock-in/out or clocked-in with no clock-out yet -- same "disclose, never silently treat as verified" discipline the whole EVV build has followed since the first SAIRNsenior commit. Reports panel: real CSV export for Clients/Caregivers/Visits (any role) and Claims (management only, double-gated -- button hidden AND the export function itself re-checks). Exports whatever the session's already-scoped local cache holds, so a caregiver exporting "visits" only ever gets their own -- rides the same privacy boundary the read endpoints already enforce, not a new one. SAIRN Data Bridge: Dashboard now reads the employee roster from SAIRNbiz via the existing generic 'employees' resource -- same shared endpoint every other app this session reads it through (StoneDesk, SAIRNbuild), zero new server code, same honest degrade if the agency isn't also licensed for SAIRNbiz (null read means exactly that, never seeded/faked). node --check clean, zero duplicate ids/functions, dead-button audit 0 flagged, div balance 324/324, all 10 panels (incl. new panel-compliance and panel-reports) reconciled both directions. LIVE-VERIFIED post-push: curled the deployed sairnsenior.html directly, confirmed 12/12 new markers present. NOTED, NOT ACTED ON: CC independently scoped SAIRNcare v1 in parallel this session and reports finding the same real-docs-vs-verbal-framing conflict I found and reported earlier -- his scope doc is a separate file, no overlap with this work, and SAIRNcare stays queued behind SAIRNsenior's full build per Michael's original sequencing. Remaining for SAIRNsenior: Family/Client Portal (flagged as needing its own design decision before building, not a silent extension of the employee-auth model -- clients/family are a materially different actor class, likely needs its own lighter auth shape such as a scoped read-only link rather than a PIN account) and deeper AI Ops Assistant enhancements (the agentic insufficient-hours/denial-pattern tools described in the original scope, beyond the current scoped-context chat already built). Same SEN- license blocker as every SAIRNsenior entry so far, unchanged. 2026-08-20
- sairnsenior.html + api/sen-portal.js + sql/sairnsenior_portal_links_schema.sql (Hank) -- FIFTH SAIRNsenior BATCH DONE, Family/Client Portal, pushed (`973e247`) after a session cut mid-push last time -- resumed and closed out the three unconfirmed steps rather than assuming any of them had completed. New sen_portal_links table (scoped, revocable links, not a PIN account). api/sen-portal.js: create/revoke/list run under a real authenticated employee session; the public 'view' action is token-only, no license/session/PIN required, matching the family/client actor class flagged as needing its own lighter auth shape in the prior entry. Deliberately minimal exposed data -- client name plus visit date/time-window/status only; no caregiver identity, no diagnosis, no authorized hours, no notes, no care plan tasks, no billing data. Client-modal link management UI plus a public portal render path (?portal=TOKEN). Guardian + adversarial review caught a real comment/code mismatch before commit: the comment claimed "caregiver first name only" was exposed, but the code actually omits caregiver identity entirely -- comment corrected to match the safer real behavior, not the other way around. RESUMED-SESSION VERIFICATION, done with real evidence rather than assumed from the prior session's summary: git showed the commit existed locally but had NOT reached origin/main (confirmed via `git log origin/main..HEAD` before fetch, then `git fetch origin main` + re-diff) -- pushed it now (`79e03e8..973e247`). node --check clean on api/sen-portal.js; extracted sairnsenior.html's single script block via tools/extract_scripts.py and node --check'd it clean; div balance 357/357. Post-push, polled the real deployed sairnsenior.html (not assumed from push success) until senPortal markers appeared -- confirmed live. Curled api/sen-portal directly with a bogus token: real INVALID_LINK 400, not a 404 or crash (an initial probe against the wrong path, api/sen-portal.js with a literal .js suffix, 404'd -- that was my own test error, not a routing bug; the real route is extensionless like every other SAIRN api file). SAIRN-ACTIVE-WORK.md itself was the third unconfirmed item -- this entry closes it. Remaining for SAIRNsenior per the prior entry, unchanged: deeper AI Ops Assistant enhancements (agentic insufficient-hours/denial-pattern tools beyond the current scoped-context chat). Same SEN- license blocker as every SAIRNsenior entry so far, unchanged. 2026-08-20
- sairnsenior.html + api/claude.js (Hank) -- SIXTH SAIRNsenior BATCH DONE, AI Ops Assistant agentic tool, pushed (`f52e7ec`). New get_ops_attention tool (Claude tool-calling, custom/client-executed per the platform's Anthropic-tools contract), following the same get_X_attention pattern already proven on SAIRNcash/SAIRNgrounds/SAIRNscape/SAIRNlegacy/SAIRNdesign -- one consolidated tool, SEN_TOOLS register/execute wrapper, thrown errors become honest {ok:false}, two-step tool_use/tool_result round trip. insufficient_hours compares real delivered hours (completed visits' clock_in/out) against each active client's Authorized Hours/Week over the most recently FULLY COMPLETED 7-day window, deliberately excluding the current in-progress week to avoid flagging a client "behind" on a week that isn't over -- scoped by the same sen_clients three-tier read gate as everywhere else (broad-read roles see the active roster, caregiver sees only their own assigned clients). denial_patterns groups denied claims by denial_reason with counts/dollar totals, management-only (matching Billing & Claims' own gate, returns null for caregivers), and withholds a breakdown below 2 real denied claims (DENIAL_PATTERN_MIN_CLAIMS) rather than call two data points a "pattern" -- same discipline as SAIRNcode's DENIAL_PATTERN_MIN_EVENTS. New shared visitHours(v) helper factors the clock_in/out arithmetic out of generateClaim() so a claim and an hours finding can never silently diverge on it. TWO REAL PRE-EXISTING BUGS FOUND AND FIXED WHILE WIRING THIS IN, neither introduced by this pass: (1) api/claude.js's KNOWN_APP_IDS never included 'sairnsenior' at all -- every AI Assistant call has been 400ing "unrecognized app_id" since Phase 1 (3157ac9), silently swallowed into the chat's generic "AI request failed -- try again," undetected because nobody had a real session to click-test it with; caught by directly curling the proxy before assuming the existing chat worked, same discipline as the 2026-07-26 correction that added the other 9 missing apps. (2) aiAsk()'s roster-context scoping used senIsManagement() alone, silently narrowing coordinator/scheduler down to own-assigned-only in AI answers even though the real sen_clients gate (fixed in batch 2, f9d3e7e) already grants them agency-wide read -- the exact bug class batch 2 fixed everywhere else, missed in this one function until now; now uses senIsBroadRead() like every other screen. VERIFICATION, same SEN- license blocker as every SAIRNsenior entry (no real session exists to click-test the chat UI end to end), so verified at two levels rather than skipped: (a) LIVE, SERVER-SIDE -- curled api/claude.js directly with app_id:'sairnsenior', got a real 200 (was a real 400 immediately pre-push, re-confirmed with the exact same request); separately curled it with a real custom tool definition and a system prompt forcing tool use -- got back a real tool_use block naming get_ops_attention, proving the proxy forwards custom tools correctly for this app_id end to end. (b) ISOLATED-LOGIC, CLIENT-SIDE -- computeInsufficientHoursFindings() and computeDenialPatternFindings() copied verbatim into a Node harness with mocked clients()/visits()/claims()/senIsBroadRead()/senIsManagement(), 7 scenarios: window-boundary correctness (a visit the day before the window and a visit today are both correctly excluded, only the 9 in-window hours count, gap math 20-9=11 correct), a client meeting/exceeding authorized hours correctly NOT flagged, an intake-status client correctly excluded (never flagged before care has started), caregiver role-scoping correctly hides another caregiver's client, below-threshold denial note fires at 1 denied claim, grouping/sort/amount math correct at 3 denied claims across 2 reasons (a non-denied claim correctly excluded from the count), and the management-only gate correctly returns null for a non-management role -- 7/7 passed on verbatim code, not a reimplementation. node --check clean on both files, zero duplicate ids/functions, dead-button audit 0 flagged, div balance 357/357, 10/10 nav targets/panels reconciled. NOT YET VERIFIED, STATED PLAINLY: an actual end-to-end browser click (ask a real question, see the tool fire, see the final answer render) -- blocked on the same missing SEN- license as every other SAIRNsenior click-through item, not skipped or assumed passing. Remaining for SAIRNsenior: none outstanding from the original panel list -- all panels (Dashboard, Clients, Caregivers, Scheduling & EVV, Care Plan Tasks, Billing & Claims, Compliance, Reports, SAIRN Data Bridge, Family/Client Portal, AI Assistant with agentic tooling) are now built. SEN- license provisioning is still Michael's to clear, not mine -- flagged, not chased. 2026-08-20
- sairncode.html + api/sd-data.js + sql/sairncode_coded_items_schema.sql (Hank) -- SAIRNCODE GAP-CLOSURE PHASE 1 DONE, pushed (`5fa7922`), live-verified. Items 1+2 built as ONE resource (sc_coded_items, 25th SC_RESOURCES entry) rather than two, because they describe one object -- a coded item carrying its citation, reasoning, and review state; splitting them would have been speculative structure. Rides the existing generic sc_* handler, so the server change is three registration points only, no new branch. ITEM 1 (citation/reasoning as REAL STORED DATA): the honest finding first -- suggestCodesFromNote() was ALREADY doing the hard part (requiring an exact quote from the note, then independently verifying that phrase is genuinely a substring of the pasted text, a real check not a model claim) and then THREW ALL OF IT AWAY. Confirmed by grep across the function's whole body before building: zero scData/localStorage writes, render-only. So a coder could see a verified citation on screen and never reproduce it afterwards -- backwards for a compliance tool where "why was this code assigned" is the question that matters at audit time. Now each captured suggestion stores code/type/encounter ref/quote/quote_verified/reasoning/source_rule/who/when, and the capture path reads the suggestion out of a held array rather than the DOM so the stored quote is byte-identical to the one the verification actually ran against. ITEM 2 (honest escalation, never a forced best guess): confidence is a DERIVED label ('high'/'low') computed by scDeriveCodedItemConfidence() from signals this app can genuinely check -- no citation, citation not confirmed present in the note, no supporting rule, no code -- and any of them forces review_status='needs_human_review' with a plain-language reason. THE LOAD-BEARING DECISION, grounded in this file's own history rather than preference: no model is ever asked for a confidence percentage, and no numeric score exists anywhere in the schema. sairncode.html:2454 carries a standing comment recording that the Fraud panel used to seed fabricated 82%/71% confidence scores until the 2026-08-18 fabrication audit removed them; a percentage on a CODE ASSIGNMENT would be the same bug with higher stakes, because a number like that reads as a measured accuracy rate to whoever sees it next. Nothing promotes a flagged item automatically -- it waits for a real human decision recorded with employee id and timestamp. THREE REAL BUGS CAUGHT DURING THE BUILD, none shipped: (1) I referenced a `scSession` global that DOES NOT EXIST in this file -- the employee id lives in localStorage('sc_employee_id'); found by checking rather than assuming, fixed with a single scCurrentEmployeeId() read path. (2) THE IMPORTANT ONE -- the verification gate was written `quote_verified === false`, which FAILS OPEN: an item whose verification result was missing or unknown got treated as verified and auto-assigned on no grounds, the exact outcome this whole feature exists to prevent. Now `!== true`, failing closed. Caught by an isolated-logic test, NOT by reading the code -- the test that exposed it was specifically probing the undefined case. (3) Used a `table-wrap` CSS class that does not exist in this file (grep returned 1 hit, my own); corrected to the real table-container/tw pattern every other panel uses. VERIFICATION: node --check clean on both script blocks and api/sd-data.js; div balance 788/788; 24 nav targets / 24 panels 1:1; zero duplicate ids; zero duplicate function names; dead-button audit 75 handlers / 0 missing. Escalation logic isolated-logic tested 12/12 against the REAL function extracted verbatim from the file (not a retype), including three fail-closed cases (undefined, truthy string, numeric 1) and an explicit assertion that no numeric confidence field exists in the output shape. LIVE-VERIFIED after push: polled the real deployed sairncode.html until markers appeared -- all 10 present including the `quote_verified !== true` fail-closed guard in the deployed source, and zero numeric-confidence matches; sc_coded_items now passes the live resource allowlist returning an honest {"ok":true,"data":[],"provisioned":false} (migration queued, not run), with a bogus resource name still 400ing as the contrast control. MERGE NOTE, disclosed rather than buried: CC pushed SAIRNcare twice mid-build (alf_clients, then alf_staff) touching the same api/sd-data.js RESOURCES map. First rebase attempt went wrong in two ways I caught and fixed before anything reached origin -- a Python rewrite stripped every CRLF from api/sd-data.js (core.autocrlf=true normalizes on add, but the repo's blob for that file is CRLF), turning a 3-line change into a 6147-line whole-file rewrite; and restoring from a MOVING origin/main absorbed CC's alf_staff work into my commit. Both fixed by rebasing onto CC's actual commit and re-applying only my own three additions with CRLF preserved via `git -c core.autocrlf=false add`. Final commit is 19 lines in sd-data.js, verified by diff to contain nothing but my additions, and CC's alf_clients/alf_staff confirmed intact both in-file and live (alf_staff returns a real NO_SESSION 401 post-deploy, not a regression). MIGRATION QUEUED, NOT RUN: sql/sairncode_coded_items_schema.sql -- degrades honestly to local-only, confirmed live via the provisioned:false read above, not assumed. PHASE 2 GATE CHECKED EARLY, REPORTING IT NOW: item 4 (payer-eligibility-informed suggestions) is explicitly conditioned on real-time eligibility being live. sc_eligibility is provisioned and api/sc-eligibility.js exists, but api/sc-credentials.js's status endpoint returns NO_SESSION without a real admin session, so whether a REAL Stedi credential is actually connected for this practice is NOT yet established -- I will verify that with a real session before building item 4, and will not build ahead of it. 2026-08-20
- sairncode.html (Hank) -- SAIRNCODE GAP-CLOSURE PHASE 2 DONE, pushed (`f9a3b38`), live-verified. ITEM 4 DELIBERATELY NOT BUILT, precondition PROVEN unmet rather than assumed: Michael's instruction was "only once real-time eligibility verification is actually live; check status first, don't build ahead of it." Logged in with a real admin session (owner on SC-PINNACLE-2026) and checked two independent ways -- api/sc-credentials 'status' returns stedi.configured:false, and a real 270/271 'check' against production returns a real 503 NOT_CONFIGURED ("No Stedi API key is configured for this practice yet"). Re-confirmed after the push to rule out a transient. Real-time eligibility is genuinely NOT live; nothing was built ahead of it. Unblocking it is Michael connecting a real Stedi API key in Settings (free account, 270/271 billed at $0 by Stedi, but it must be the practice's own account) -- not mine to do. ITEM 3 BUILT, two parts. (1) EXCEPTION-BASED ROUTING: the Coded Items panel now genuinely splits into an "Exceptions -- need a human coder" table (the real work queue) and an "Auto-assigned -- no action needed" table. The separation IS the feature -- the stated goal is that high-confidence codes stop consuming a coder's attention, which a single mixed list with a status badge does not actually achieve. Items a human has already decided on leave the queue in either direction (accepted or rejected), so the queue only ever holds genuinely undecided work. (2) TWO ADDITIONAL REAL SIGNALS, chosen specifically because both derive ONLY from data this practice itself created, so neither asserts any coding fact this app invented: (a) conflict with a rule the practice ITSELF verified -- reuses the existing findLocalScrubMatch() against sc_scrubrules, a table where every row required a human-entered Source and which is never seeded; a hit is deterministic and practice-verified, scoped to the same encounter_ref, and PROVEN BY TEST to stay silent when that table is empty, so an empty reference table can never manufacture a conflict; (b) a coder previously REJECTED this exact code -- pure history from this app's own recorded review decisions, and deliberately narrow: a prior ACCEPTANCE does not hold an item, and a merely-pending twin does not either, both tested. ON "CONFIDENCE-SCORED", the phase title says "scored" and this deliberately still produces NO numeric score anywhere -- the reasoning is now written into the code rather than left implicit, because that comment is what stops a future session from "fixing" its absence. A percentage implies a measured accuracy rate and nothing in this app measures one: no labelled outcome set, no base rate, nothing to calibrate against. This file already had fabricated 82%/71% confidence numbers removed from the Fraud panel in the 2026-08-18 audit; a number on a CODE ASSIGNMENT would be the same bug with higher stakes. What is real is which concrete checks passed or failed, so that is what is stored and displayed. BACKWARD-COMPATIBILITY DETAIL THAT MATTERED: scDeriveCodedItemConfidence gained an OPTIONAL context argument -- called without it, Phase 1 behaviour is byte-identical, so the four grounding checks never silently depend on context being available; 5 explicit Phase 1 regression tests cover this. VERIFIED: node --check clean on both script blocks; div balance 794/794; 24 nav / 24 panels 1:1; zero duplicate ids/functions; dead-button audit 75 handlers / 0 missing; and an explicit grep for leftover references to the loop variable the render split removed (node --check cannot catch an undefined-variable reference, so that was checked by hand, not assumed from a clean syntax pass). Isolated-logic tested 20/20 against all three real functions (scDeriveCodedItemConfidence, scRouteCodedItems, findLocalScrubMatch) extracted VERBATIM from the file rather than retyped -- including the 5 Phase 1 regressions, the empty-rule-table anti-fabrication case, same-vs-different-encounter scoping, self-conflict exclusion, case-insensitive rejection matching, and four routing invariants including "no item lost or duplicated." LIVE-VERIFIED after push: polled the real deployed sairncode.html until markers appeared -- all 6 present including both new escalation-reason strings, and zero numeric-confidence matches in the deployed source. MERGE NOTE: CC pushed SAIRNcare twice more during this phase (Staff, then Care Plans/ADL); the second touched only sairncare.html and SAIRN-ACTIVE-WORK.md, so the rebase was clean with no conflict -- my commit is 1 file, 127 insertions. STANDING RULE ACKNOWLEDGED FOR PHASES 4-5: every specific fact in those phases (dollar thresholds, code ranges, modifier percentages, the CMS-vs-CPT 99292 conflict) came from this session's web research, not primary sources, and must each be verified against an authoritative source (CMS.gov, AMA CPT, official Medicare manuals) before entering the tool, flagged per-item as reached. 2026-08-20
- sairncode.html + api/sc-ai.js (Hank) -- SAIRNCODE GAP-CLOSURE PHASE 3 DONE, pushed (`d51d352`), live-verified. ITEM 5 BUILT AS A RISK CHECK, NOT A PROBABILITY SCORE -- the data gate was checked live with a real admin session BEFORE writing any code, exactly as instructed, and it failed: sc_denial_events 0 rows, sc_denial 0, sc_ar 0, sc_prebill 0, and sc_claims held exactly 1 row which is the known CLM-MIGRATION-TEST residue (TestPayer, $1, status Pending -- a test artifact, not real and not even denied). There is no labelled outcome set, no base rate, and nothing to calibrate a predicted probability against, so no probability is produced anywhere. THE DISTINCTION THE WHOLE FEATURE IS BUILT ON, written into the code comments so it survives a future session that might try to "add the missing percentage": "34% chance this claim is denied" is a PREDICTION requiring a calibrated model that does not exist here and is never produced; "this payer denied this code 3 of your 12 logged denials" is a COUNT of events the practice itself recorded, which is real, checkable, genuinely actionable, and is what got built. Three risk factors, every one from the practice's own data: (a) codes on this encounter still sitting unresolved in the Phase 1/2 human-review queue; (b) a conflict between the claim's OWN codes per a rule THIS PRACTICE verified, reusing findLocalScrubMatch against the Source-required never-seeded sc_scrubrules table, and proven by test to stay silent when that table is empty so an empty reference can never manufacture a risk factor; (c) prior denials involving this payer plus a code on this claim, gated on the file's EXISTING DENIAL_PATTERN_MIN_EVENTS=3 rather than a second invented threshold -- below it the UI shows an honest "not enough logged denial history for a real rate (have N, need 3+)" and NO rate at all, rather than a softened or approximated number. LINKAGE DECISION MADE WITHOUT A SCHEMA CHANGE: claim records have no codes field, and rather than migrate that, the check uses the linkage that already exists -- a coded item's "Claim / encounter reference" matching the claim id. When nothing is linked, the UI says so plainly instead of appearing to have checked codes it never had. ITEM 6 BUILT, DRAFT-AND-HOLD, with two things stated SEPARATELY because conflating them would overclaim: (1) this app has NO send path of any kind -- no SMTP, no clearinghouse submit, no fax, verified by grep across sairncode.html before building (the only mailto: in the file is the trial-upgrade link) -- so "never auto-sends" is a property of the APPLICATION, not a safeguard this feature invented, and it is deliberately not claimed as one in either the code or the UI copy; (2) what this DOES genuinely add is grounding -- the draft is built strictly from the recorded denial row, and the prompt forbids inventing chart contents, dates, provider names, patient identifiers, or policy citations, requiring an explicit [BRACKETED] placeholder instead, and separately bars the model from asserting that documentation supports medical necessity since it has not seen the chart. Rides the existing audited/screened scAiFetch transport via a new appeal_draft KNOWN_FEATURES entry, inheriting the consent gate and injection screening automatically rather than opening a bypass. VERIFIED: node --check clean on both script blocks and api/sc-ai.js; div balance 814/814; 24 nav / 24 panels 1:1; zero duplicate ids; dead-button audit 78 handlers / 0 missing; and every new function confirmed to have a real caller (no dormant code -- scPreSubmissionRisk was deliberately wired into a real Risk Check button rather than left as an uncalled helper, which Guardian Check 0d would have flagged). Isolated-logic tested 18/18 against the real functions extracted VERBATIM, covering the below-threshold honest-empty path, an assertion that no probability/score/percent key EVER appears in the output shape, real payer scoping (a different payer correctly yields 0 matches), per-encounter isolation of unreviewed codes, the empty-rule-table anti-fabrication case, and single-code self-pair exclusion. LIVE-VERIFIED after push: all 8 markers present in the real deployed sairncode.html with zero numeric-probability leaks; and the appeal_draft feature exercised END TO END against production with a real admin session -- returned audited:true (server confirms the audit-log write succeeded, not merely attempted) and a real 2,113-character draft that correctly bracketed [DATE OF SERVICE], a fact deliberately NOT supplied. Ran explicit anti-fabrication checks on that real output: zero invented regulatory citations (no 42/45 CFR, no CMS-nnnn, no Pub 100, no section numbers), zero unsupported "the documentation supports/establishes..." assertions, zero invented provider or patient identifiers, and 20 placeholders left for the human. The grounding discipline is working against the real endpoint, not just asserted in the prompt. PHASES 4-5 STANDING RULE RESTATED, now imminent: every specific fact in those phases (dollar thresholds, code ranges, modifier percentages, the CMS-vs-CPT 99292 conflict, license-gated code restrictions) came from this session's web research rather than primary sources, and each must be verified against an authoritative source (CMS.gov, AMA CPT, official Medicare manuals) BEFORE entering the tool, flagged per-item, with any error found reported plainly as the verification step working rather than quietly corrected. 2026-08-20
- sairncode.html + api/sd-data.js + sql/sairncode_credential_scope_schema.sql (Hank) -- SAIRNCODE PHASE 4 DONE, pushed (`51eb15a` item 7, `a729878` item 8), live-verified. ITEM 7, CREDENTIAL-GATING LAYER. Reported the gap BEFORE building as instructed, and it was structural, not just emptiness: sc_providers is empty (0 rows, verified live) AND could not have supported this gate even if populated -- its `cred` field is a Yes/No dropdown (credentialed-at-all, not per-code), its `specialty` is FREE TEXT so "Cardiology"/"cardiology"/"Cards" are three different values nothing can match a code against, and no claim, coded item, or denial record linked to a provider at all. So item 7 split into two honestly-separable halves: the validation layer plus the structured data it needs (built), and any claim about WHICH specialty may bill WHICH code (deliberately NOT built and never seeded -- that varies by payer policy and state scope-of-practice law and was not verified against a primary source; the practice enters scopes THEY verified, source required, same discipline as sc_scrubrules). New sc_credential_scope resource, 26th SC_RESOURCES entry -- a credential grant is genuinely a different object from a provider (one provider holds many, each with its own scope and source), same reasoning that kept sc_auth_requests out of sc_auth. THE GATE FAILS CLOSED, decision confirmed with Michael after I raised it rather than picking silently: no credential data returns 'no_data' and routes the coded item to human review rather than passing it, because an empty table silently green-lighting every code would be worse than having no gate -- identical principle to the quote_verified fail-open bug caught in Phase 1. It is a disclosed SOFT block: the message tells the coder to add the data or accept the code on their own recorded judgment, so an empty table never freezes the workflow. Reusable contract for every Phase 5 module: scValidateCredentialForCode(providerId, code) -> {state:'covered'|'no_data'|'not_covered'|'no_provider'|'no_code', ok, detail}; callers branch on state, never parse detail; only 'covered' is ok. Scope matching is literal and documented -- exact, prefix ("992*"), or range ("99281-99285") -- nothing inferred. ALSO FIXED, a real Phase 1 gap found while wiring boot render: renderCodedItems() was only ever called from scSyncAllResources(), so on a page load the Coded Items panel stayed empty until a server sync landed. Tested 26/26 including every fail-closed state, that a bare "*" matches nothing rather than everything, and specifically that an alphanumeric HCPCS code (A0430) can never fall inside a numeric CPT range just because its digits sort between the endpoints, with a numeric control proving the range still works. ITEM 8, CRITICAL CARE TRANSPORT -- EVERY FACT VERIFIED AGAINST A PRIMARY SOURCE BEFORE IT ENTERED THE TOOL, per the standing rule, AND TWO REAL CORRECTIONS WERE FOUND. Both on the same axis and both would have caused real misbilling if built as briefed: (1) 99466/99467/99485/99486 are PEDIATRIC, 24 MONTHS OF AGE OR YOUNGER -- the brief described them as generic "physician face-to-face" and "physician supervisory" time-based codes, and using any of them for an adult or older child would be flatly wrong; this is the defining element of the code family, not a nuance. (2) All four require INTERFACILITY transport -- face-to-face time begins when the physician assumes primary responsibility at the REFERRING facility, so a scene response or in-hospital transfer does not qualify. Both are now hard gates that FAIL CLOSED: undocumented age or undocumented interfacility status blocks rather than passes. CONFIRMED AS BRIEFED, no change needed: A0430 fixed wing / A0431 rotary wing / A0435 fixed-wing mileage / A0436 rotary-wing mileage (CMS Medicare Claims Processing Manual Ch. 15, which also yielded a real bonus rule -- air mileage in whole loaded statute miles, air ambulance payable only to a hospital); ventilation-management bundling, 94002-94004 and 94660-94662 not separately reportable with E/M and only the E/M payable (CMS NCCI Policy Manual Ch. XI); and the No Surprises Act IDR distinction applying to AIR ambulance but NOT ground (CMS No Surprises Act) -- that last was the highest-stakes claim in the set and it checked out exactly as briefed. Michael chose Option 1 when I raised how the verified facts should land: every rule is real app logic AND displays its source inline in the UI, so an outside reviewer can independently check "99466 requires age <= 24 months, source AMA CPT descriptor" in seconds rather than trusting an opaque gate or hunting for where a hardcoded value came from -- same show-your-work pattern as Phase 1 citations and Phase 2 rule-conflict reasoning. A test asserts EVERY finding carries a non-empty source naming CMS/AMA/AAP/NCCI. The two billing streams are structurally separated and displayed separately (A-codes billed by the transport SUPPLIER, 994xx by the PHYSICIAN, often to different payers); mixing them on one claim blocks. DELIBERATELY ABSENT: the ground ALS/BLS/SCT A-code variants. The brief referenced them but only the four AIR codes were verified against CMS in this build, and an unverified ground code has no business in a billing tool -- their absence is stated in the panel rather than silently omitted, and a test asserts none leaked in. VERIFIED: node --check clean on both script blocks and api/sd-data.js; div balance 839/839; 25 nav / 25 panels 1:1; zero duplicate ids; dead-button audit 81 handlers / 0 missing. Isolated-logic tested 27/27 on the transport module extracted verbatim -- both age boundaries (24 exactly = inside, 25 = outside), both fail-closed paths, stream separation, vent bundling, wing-type mismatch between transport and mileage, time-vs-descriptor checks, and that NSA is NEVER asserted for a non-air claim. LIVE-VERIFIED after push: all markers present in the real deployed sairncode.html including the pediatric age language and the NCCI citation, zero unverified ground codes in the deployed source, sc_credential_scope passing the live resource allowlist with an honest provisioned:false, and CC's newest alf_billing work confirmed unaffected. MERGE NOTE: CC pushed SAIRNcare three more times during this phase (MAR, Staff, Billing); the api/sd-data.js conflict was resolved with the now-established CRLF-safe procedure (rebuild from origin's bytes, re-apply only my own additions, `git -c core.autocrlf=false add`) giving an 18-line diff with CC's 34 alf_ references intact. TWO MIGRATIONS QUEUED, NOT RUN: sairncode_coded_items_schema.sql and sairncode_credential_scope_schema.sql -- both degrade honestly to local-only. 2026-08-20
- sairncode.html (Hank) -- SAIRNCODE PHASE 5 IN PROGRESS, 3 of 6 specialty modules done and live: item 9 Emergency Department (`9d6f8cd`), item 11 Physical Therapy (`bf1b274`), item 14 Mental Health (`62b639d`). All three rest on Phase 4 item 7's credential layer and reuse a shared scRenderRuleFindings() so "every rule shows its source" holds by construction rather than by repetition. FIVE REAL ERRORS FOUND AND CORRECTED ACROSS THE THREE, plus one case where my own doubt was WRONG -- reporting that one because it matters as much as the corrections. ITEM 9 (ED), THREE ERRORS: (1) THE BRIEF WAS TOO RESTRICTIVE, the first error in this arc running that direction -- it called for a HARD BLOCK on 99291 with an ED E/M same day, same provider, "real CMS violation otherwise." That is wrong: CMS PERMITS both when critical care follows a COMPLETED ED E/M, with modifier -25; only the reverse order is barred. A hard block would have rejected legitimate billing and cost the practice real revenue. Now order-dependent, and it fails closed when the sequence is undocumented. (2) 99217-99220 were DELETED 1 Jan 2023 (-> 99221-99223 initial, 99231-99233 subsequent, 99238/99239 discharge) -- but 99234-99236 in the SAME briefed claim REMAIN VALID, so only the wrong half was corrected and tests assert all three of 99234/99235/99236 are never flagged. (3) 99293-99296 were DELETED (-> 99471/99472/99468/99469) AND those replacements are INPATIENT-only, so the range was wrong twice over for an ED module; pediatric critical care in the ED uses 99291/99292 like adults. Confirmed as briefed: MDM-only leveling for 99281-99285, the critical-care clock PAUSING for separately billable procedures (31500, 36555/36556, 92950, 93010), and the real CMS-104-minute vs CPT-75-minute split for 99292, implemented as genuinely payer-dependent rather than picking one. ITEM 11 (PT), ONE ERROR + ONE REVERSAL: the CQ reduction is 15% of the FULL fee schedule amount (paid at 85%), NOT of the practice-expense component as briefed -- the PE component belongs to MPPR, a different reduction, and keeping the brief's wording would have mis-stated what a PTA-delivered unit actually pays. THE REVERSAL, reported plainly: I flagged the "new 2026 RTM codes 98979/98984/98985" as probably invented because they looked wrong against the established 98975-98981 family. THEY ARE REAL -- genuine 2026 additions for shorter 2-15 day monitoring windows, with the pre-existing device-supply codes now specifying 16-30 days. I nearly "corrected" a correct fact; the verification step cut both ways and that is exactly why it runs on everything rather than only on claims that look suspicious. It also caught an OMISSION -- 98986 (CBT, 2-15 days) is real and was missing from the brief, so it is included. Confirmed as briefed: the 8-minute rule aggregating the session FIRST then allocating (8-22=1, 23=2, 38=3, 53=4), the $2,480 CY2026 KX threshold, CQ required when a PTA furnishes MORE THAN 10%, and MPPR ranking -- refined to highest PRACTICE-EXPENSE RVU specifically. Two real nuances the brief omitted are now encoded: PT and SLP share a COMBINED KX threshold (OT has its own separate $2,480), and there is a second targeted-medical-review threshold at $3,000. The KX check also blocks when KX is applied with no recorded justification, since KX is an attestation and boilerplate defeats its entire purpose. ITEM 14 (MENTAL HEALTH), ONE ERROR + TWO FIGURES DELIBERATELY NOT ENCODED: MFTs/MHCs are paid at 75% of what a CLINICAL PSYCHOLOGIST is paid under the fee schedule, NOT "75% of the physician fee schedule" as briefed -- different numbers, and the brief's version would materially overstate expected reimbursement; the module states the correct basis AND explicitly denies the physician-rate reading, since that is the misreading most likely to persist. NOT ENCODED, per the fail-closed rule: the "~$42/session" POS 10 vs POS 02 swing (the DIRECTION is verified and enforced -- POS 10 pays the non-facility rate, POS 02 the facility rate, lower -- but the dollar amount is code- and locality-specific and could not be verified, so no figure is asserted, and a test asserts "$42" appears nowhere in the module); and a definition for H0020 (the other four H-codes were verified, H0020 was not, so entering it returns an honest "not verified by this tool, confirm against your own source" rather than a guessed description). Confirmed as briefed: LPC/LMFT/LMHC barred from 90792 and from the E/M-paired add-ons 90833/90836/90838, Medicare billing blocked until CMS-855I/PECOS enrollment completes, psychotherapy vs E/M time never double-counted, psychotherapy notes needing their own more restrictive tier per 45 CFR 164.501, telehealth keeping the same CPT with only POS changing, and family-therapy necessity anchoring on the patient's OWN diagnosis -- implemented as a real checkable pattern that rejects Z63.x relational codes as the anchor. The license blocks say plainly that they are LICENSURE limits, not documentation ones, because "write a better note" is the wrong and expensive response to them. VERIFIED across all three: node --check clean on both script blocks throughout; div balance 861/861 at the end; 28 nav / 28 panels 1:1; zero duplicate ids; dead-button audit 84 handlers / 0 missing. Isolated-logic tested 31/31 (ED), 40/40 (PT), 34/34 (MH) against the real modules extracted VERBATIM -- including the full published 8-minute table with every boundary, the 8+8+8=24 case where per-code counting says 3 units but the aggregate supports only 2 (the exact over-bill that rule exists to prevent), both 99292 payer boundaries (Medicare exactly 104, other payer exactly 75), the PTA exactly-10% boundary (CQ NOT required at 10, required at 11), and an assertion in every module that EVERY finding carries a non-empty source. LIVE-VERIFIED after each push. TWO LIVE-CHECK FALSE POSITIVES INVESTIGATED RATHER THAN REPORTED AS LEAKS: a grep for "$42" hit 2 times and one for a H0020 definition hit once -- both turned out to be my own grep patterns misfiring (the "$42" hits are inside the comment EXPLAINING that the figure is deliberately not asserted, and "H0020: true" matched a lowercase-letter pattern via the word "true"), confirmed by pulling the actual surrounding text before drawing any conclusion. MINOR, DISCLOSED: commit 62b639d's message contains a stray non-English word ("могли") from a typing glitch -- cosmetic, in the message only, not in any code; not force-pushed to fix, since rewriting shared-branch history while CC is actively pushing SAIRNcare would be far more disruptive than the typo. REMAINING IN PHASE 5: item 10 Cosmetic/Reconstructive, item 12 Optometry/Ophthalmology, item 13 Urgent Care. 2026-08-20
- sairncode.html (Hank) -- SAIRNCODE PHASE 5 COMPLETE, all 6 specialty modules built and live: item 9 Emergency Department (`9d6f8cd`), item 11 Physical Therapy (`bf1b274`), item 14 Mental Health (`62b639d`), and now items 10/12/13 together (`f48d780`) -- Cosmetic/Reconstructive, Optometry/Ophthalmology, Urgent Care. All 6 rest on Phase 4 item 7's credential-gating layer and share scRenderRuleFindings(), so "every rule shows its source" holds by construction. Verified live that all 8 validators (6 specialty + credential gate + transport) are deployed together. ITEM 10, ONE CORRECTION: the brief listed modifiers "59/51/76" together for MIXED same-session cosmetic/reconstructive cases. Modifier 76 is Repeat Procedure by the Same Physician -- the SAME procedure repeated -- and does NOT distinguish a cosmetic component from a reconstructive one (59 is for a DIFFERENT procedure, 51 for multiple procedures in one session). Appending 76 to a mixed session would be real misbilling, so it is BLOCKED with an explanation rather than offered, and a test asserts 76 is absent from the valid-modifier map while 59 and 51 are present. Also surfaced during that verification and included: CMS prefers the more specific X{EPSU} modifiers (XE/XP/XS/XU) over 59 where one applies, so those are accepted and a 59 use prompts that note. Cosmetic cases are hard-routed to the direct-pay workflow -- routing one into the insurance A/R pipeline blocks; reconstructive claims require BOTH documented medical necessity and objective evidence, and a prior related procedure in the patient's own claims history (e.g. a mastectomy) is recognised as real supporting evidence rather than ignored. ITEM 12, VERIFIED WITH NO CORRECTIONS: 92015 determination of refractive state separately billable from any 92xxx or 99xxx visit; -54 surgical care only / -55 postoperative management only, with transfer-of-care documentation required and, for -55 specifically, the post-op DATE RANGE; 66984 cataract carrying a 90-day global. ONE RULE ADDED that the brief never mentioned but which was confirmed and is genuinely useful: a refraction is NEVER part of a global surgical package, so 92015 stays separately billable even inside the 90-day cataract global -- that is exactly the kind of thing a coder loses money on by assuming the opposite. ITEM 13, VERIFIED WITH NO CORRECTIONS -- every briefed claim checked out, which is worth recording as plainly as the errors: S9083 "urgent care center global" and S9088, both appearing on CMS Outpatient CPT/HCPCS EXCLUDED Services lists consistent with Medicare not paying S-codes; standard E/M 99202-99215 for Medicare; POS 20. The ED-code block turned out to be MECHANICAL rather than a judgement call, because CMS defines POS 20 as "a location, DISTINCT FROM A HOSPITAL EMERGENCY ROOM" -- so 99281-99285 block unless hospital-based ED status is affirmatively recorded, and an unrecorded payer type also blocks since the same visit codes differently by payer. VERIFIED: node --check clean on both script blocks; div balance 875/875; 31 nav / 31 panels 1:1; zero duplicate ids; dead-button audit 87 handlers / 0 missing. Isolated-logic tested 43/43 across all three modules extracted VERBATIM, on top of the earlier 31/31 (ED), 40/40 (PT), 34/34 (MH), 27/27 (transport) and 26/26 (credential gate) -- 201 isolated-logic assertions across the whole Phase 4+5 arc, every one against real code pulled from the file rather than retyped. PHASE 5 ERROR TALLY, FINAL: six real errors found and corrected across items 8, 9, 10 and 14 (pediatric age limit and interfacility requirement on the 994xx transport codes; the over-restrictive ED co-billing hard block; deleted observation codes 99217-99220; deleted pediatric codes 99293-99296 that were also inpatient-only; the CQ practice-expense mis-attribution; the 75%-of-physician-rate error; and the modifier-76 mis-assignment). Three modules had NO errors at all (12, 13, and the confirmed half of 11). One case went the other way entirely -- the "new 2026 RTM codes" I suspected were invented turned out to be real, and I nearly corrected a correct fact. A NOTE ON PROCESS worth keeping: the errors were not concentrated in the claims that looked shakiest. The single most dangerous one (the ED hard block, which would have silently destroyed legitimate revenue) looked entirely safe, and the one I was most suspicious of was correct. That is the argument for verifying everything rather than triaging by apparent risk. TWO SHELL-QUOTING FAILURES hit while building this batch (heredocs breaking on embedded quotes) -- both caught because the command errored rather than silently half-applying, and the file state was checked before retrying rather than assumed; worked around by writing the insert script and the test files to disk via the Write tool instead of piping through the shell. 2026-08-21
- api/_resources/*.js (13 new files) + api/sd-data.js (Hank) -- STRUCTURAL FIX for the recurring api/sd-data.js merge collisions between parallel sessions, pushed (`d6f8afc`), live-verified with a before/after production diff. SCOPE DECIDED FROM EVIDENCE, NOT ASSUMPTION, and narrowed from the original request after raising it: Michael asked for one resource file per app with sd-data.js as a thin loader. Checked where the collisions ACTUALLY happen before designing anything -- all three conflicts hit while building SAIRNcode this session were in the shared RESOURCES object literal and the separately hand-maintained "resource must be one of" string, two shared lines every app appends to, while the request-handler branches changed by 100-189 lines in those SAME commits and merged cleanly EVERY time because each app appends in its own region. So the registry was split and the ~70 handler branches were deliberately left in place. Raised that as an explicit fork rather than silently narrowing scope; Michael chose registry-only. THREE FINDINGS THAT SHAPED IT: (1) there are ZERO duplicate resource+action branches, so dispatch is order-independent and no split can change which branch wins -- that is the fact that makes a future handler split safe if ever wanted, and it is worth recording because the file's own sairnscape comment warns about exactly that hazard for scp_jobs; (2) every branch is a closure over ~15 handler-local bindings (licHash, lic, isPaid, headers, rest, enc, nowISO, payload, action, resource, SUPABASE_URL, SERVICE_KEY, isScResource, req, res), so moving them means threading a context object through ~3,000 lines serving 11 live apps -- large behavioural risk for no additional collision benefit; (3) resources do NOT partition cleanly by app, which breaks the one-file-per-app framing: shared_knowledge is used by 10 apps, employees by 4 real apps, and schedule/invoices/jobs/customers are cross-app, so those live in _resources/shared.js rather than being forced into one app's file. TWO REAL PRE-EXISTING BUGS FIXED, both found during verification rather than looked for: (a) DRIFT -- the map held 158 resources but the hand-written error string listed 157; employee_profile is a valid, working resource that was MISSING from it, so a caller who mistyped got a "must be one of" list omitting a resource that actually works. The string is now GENERATED from the map, making that class of drift structurally impossible. (b) SILENT DUPLICATE REGISTRATION -- two apps claiming the same resource name previously just overwrote each other in an object literal with no signal at all; assertNoDuplicates() now throws at load, and that guard was live-tested by deliberately registering sc_denial under sairnbuild and confirming it throws with a message naming both apps. THE ONE DISCLOSED BEHAVIOUR CHANGE, raised with Michael before building rather than after: the single 400 diagnostic now lists 158 names grouped by app instead of 157 in the old hand-maintained order. Nothing was removed from it. No gate, no success path, and no resource's handling changed -- the only non-comment edits to sd-data.js are the require line and the error line. VERIFICATION, done the way everything else this session has been: node --check clean on sd-data.js and all 13 new files; the merged registry loaded in Node and compared against the ORIGINAL sd-data.js recovered from git rather than against my own notes -- 158 vs 158, set identical, every original key truthy in the new map, zero duplicates, nothing invented; the error-text delta computed exactly (ADDED employee_profile, REMOVED nothing); CRLF preserved throughout (3190 lines, 0 bare LF) using the established autocrlf-safe procedure; the generated per-app files produced by SCRIPT rather than hand-transcribed so 158 names could not be mistyped, with the original comment blocks kept attached to the resources they document (including the scp_ collision story). THE REAL PROOF: an 18-resource production baseline spanning all 11 apps and the 200/401/403/400 paths was captured BEFORE the push, then the identical probe replayed after deploy -- diff is EMPTY, byte-identical across every one, including both provisioned:true and provisioned:false states and all three distinct auth-failure shapes. Blast radius was understood going in (a bundling failure would have 500'd every app's sync), which is why the endpoint health was checked first and the api/_lib precedent plus the absence of any vercel.json includeFiles restriction were confirmed before pushing. ADDING A RESOURCE NOW: add the name to api/_resources/<app>.js and add the handler branch in sd-data.js as before -- two files, neither shared with another app. NOT MOVED, disclosed: SC_RESOURCES stays in sd-data.js. It is SAIRNcode-only and therefore not a cross-app collision surface, though it does duplicate the sc_ names and remains a (single-app) drift risk worth folding in if SAIRNcode is next touched. 2026-08-21
- sairnlaw.html + api/_lib/wex.js + api/_lib/intl-caselaw.js + api/legal-reference.js + sql/sairnlaw_wex_intl_schema.sql (Hank) -- SAIRNLAW PHASES A-D DONE, pushed (`1779346`, `ff26b05`, `e4fbb91`), live-verified. THE FIRST STEP CHANGED THE BUILD: Michael asked me to check whether Wex/BAILII/CanLII/AustLII have real APIs before writing code, and to consider a Bright Data connector rather than custom scraping. Checking each against its OWN primary terms found that TWO OF THE FOUR REFUSE THIS USE OUTRIGHT. BAILII prohibits bulk downloading and scraping in its standard user agreement and restricts crawlers, with its stated concern being AI software built to predict case outcomes -- it names this product category. AustLII goes further: its usage policy prohibits "spidering, scraping, crawling, mirroring, page framing, API access, bulk querying, automated agents" and blocks automated access for AI-related uses across its ENTIRE collection, with even its educational-permission carve-out stated as excluding AI-related uses. NEITHER IS A BOT-DETECTION PROBLEM, so the Bright Data connector was the wrong tool regardless of availability (and it is not exposed in my toolset anyway) -- the permission is explicitly refused, and routing around a written prohibition with better scraping tooling makes it deliberate rather than incidental. Reported this and raised the fork rather than building around it; Michael chose to swap in a legitimate substitute and state the gaps. ONE MORE FACT THAT SHARPENED IT: CanLII sued an AI legal-research platform (Caseway AI, Nov 2024) over systematic scraping -- live enforcement against exactly this product category, and confirmation that CanLII's KEYED API is the sanctioned route while scraping is not. SOURCES ACTUALLY USED, each under terms that permit it: US via the existing CourtListener citator; UK England & Wales via Find Case Law (The National Archives) -- real public API, no auth, verified live returning real August 2026 judgments, content under the Open Justice Licence which expressly permits commercial use and product incorporation, BUT that licence does NOT cover "computational analysis" (bulk programmatic searching to extract or enrich), so the client is on-demand lookup only and never sweeps the corpus; Canada via CanLII's official keyed API, verified live as real (403 ACCESS_DENIED without a key), degrading honestly to NOT_CONFIGURED. Australia and Ireland/Scotland are NOT COVERED and the app says so with the reason, rather than implying worldwide reach. PHASE A: Wex chosen deliberately over Black's (commercial, no free API) and Bouvier's (1856, wrong for modern/statutory terms). robots.txt read directly -- /wex is NOT disallowed, only Drupal paths and /search/, and Crawl-delay: 10 applies and is really enforced via a Supabase-backed ledger. Parser verified against the real live negligence page (13 paragraphs, smart quotes decoded, cross-refs extracted). No success shape exists without a real source_url. A REAL BUG CAUGHT DURING LIVE VERIFICATION, not by reading: with the migration unrun, define and intl_search returned an opaque 502 "Upstream connection error," which reads as a flaky network when the real cause is an un-run migration. Isolated it by evidence -- coverage (no table) returned 200, the two ledger-backed paths 502, and canlii_browse 503 because its key gate precedes the ledger check -- and fixed both to return 503 NOT_PROVISIONED naming the SQL file, matching every other SAIRN resource. Still FAILS CLOSED: a missing ledger means the published crawl-delay cannot be honoured, so the lookup is refused rather than running unthrottled. PHASE C, MOCK TRIAL: the refusal of outcome prediction is a REAL MECHANICAL CHECK on the model's own output (mtAssertNoProbability), not a prompt instruction -- a response containing a percentage, odds, or likelihood-of-winning claim is WITHHELD ENTIRELY and the user told why. Same fabricated-confidence problem already stripped from SAIRNcode's Fraud panel, worse here because it would advise legal strategy with no labelled outcome corpus behind it. Every reporter citation the adversary produces is extracted and checked against the real citator; anything the citator cannot find is labelled "Not verified -- do not rely on this" rather than shown as authority. PHASE D, JURY: two deliberately separate pieces -- difficulty sliders labelled in the UI as TRAINING settings for a practice juror (not a claim about any real panel), and an attitude-and-experience instrument built that way because the jury-consulting field's own research, verified before designing, finds demographics are poor predictors while attitudes/beliefs/experience predict considerably better, demographics being "at best an initial guide" and "inadequate for predicting juror behavior." THE HARD GUARDRAIL IS ENFORCED IN CODE: juryCheckStrikeReason() refuses any strike reason resting on race or gender and names the controlling authority. Word-boundary matched on purpose -- "management" must not trip "man", "Whitewater" must not trip "white" -- because a guardrail that nags on legitimate reasons trains users to ignore it, which is worse than not having one. Strike records additionally require an anchor in what the juror actually said, since an untethered reason is exactly what fails at Batson step three. ALL FOUR AUTHORITIES VERIFIED AGAINST PRIMARY SOURCES BEFORE ENCODING, and one materially widened scope: Batson v. Kentucky 476 U.S. 79 (1986); EDMONSON v. LEESVILLE CONCRETE CO. 500 U.S. 614 (1991), which extends Batson to CIVIL cases and private litigants -- so this applies to SAIRNlaw's civil users, not only criminal, which the brief did not mention; Georgia v. McCollum 505 U.S. 42 (1992); J.E.B. v. Alabama ex rel. T.B. 511 U.S. 127 (1994). Each carries a real Cornell LII source URL in the app. VERIFIED: node --check clean throughout; div balance 900/900; 19 nav / 19 panels 1:1; zero duplicate function names; dead-button audit 115 handlers / 0 missing; no dormant code (refRenderCoverage, mtRenderMatterOptions, juryRenderAll all wired into nav()). Isolated-logic tested 57/57 against the Phase C/D core extracted VERBATIM from the DEPLOYED sairnlaw.html rather than a scratch copy -- six outcome-prediction phrasings blocked and five legitimate critiques allowed, eight discriminatory strike reasons blocked and six legitimate ones allowed, five word-boundary false-positive cases, the anchor requirement, and that no voir-dire prompt anywhere references race or gender. TWO FALSE ALARMS INVESTIGATED RATHER THAN REPORTED AS BUGS: a "duplicate mtRun" (my grep matched the longer pre-existing mtRunConflictCheck) and a duplicate id tr-explain-result (pre-existing, created dynamically, present before this change) -- both checked against the pre-change file before concluding. ONE REAL TEST BUG found and fixed: an unanchored /age/ assertion matched "damAGEs_attitude"; corrected to word-boundary matching and then proven to still catch a deliberately planted demographic dimension rather than passing vacuously. TWO BLOCKERS, NEITHER MINE: sql/sairnlaw_wex_intl_schema.sql needs running, and CANLII_API_KEY needs a free key from CanLII. Until then Wex and Find Case Law return an honest NOT_PROVISIONED and Canada is honestly reported unavailable rather than silently empty. 2026-08-21
- api/_lib/deadline-engine.js + api/legal-deadlines.js + api/_resources/sairnlaw.js + api/sd-data.js + sql/sairnlaw_deadline_rules_schema.sql + sql/sairnlaw_deadline_seed_us_federal.json + sairnlaw.html (Hank) -- SAIRNLAW DEADLINE RULES ENGINE PHASE 1 DONE, pushed (`f17bf99`, corrected by `92c4783`), Guardian v2 full scan clean, live-verified. THE 6(d) VERIFICATION RESOLVED AND CONFIRMED THE ASSUMPTION -- reporting because the instruction was to report either way: the 2005 Advisory Committee Note states "Three days are added after the prescribed period otherwise expires under Rule 6(a). Intermediate Saturdays, Sundays, and legal holidays are included in counting these added three days. If the third day is a Saturday, Sunday, or legal holiday, the last day to act is the next day that is not a Saturday, Sunday, or legal holiday." So the order is 6(a) period -> roll -> +3 calendar days -> roll AGAIN, a double rollover exactly as designed. EXTENSION_UNVERIFIED lifted, path enabled. I MADE A REAL DESIGN ERROR AND PRODUCTION CAUGHT IT, disclosed rather than quietly patched: my spec said "api/sd-data.js deliberately untouched -- the per-app registry split means new resources go in api/_resources/sairnlaw.js alone," and Michael approved that. It is WRONG. Registering the name passes the allowlist but a resource with no HANDLER BRANCH falls through to "Unsupported action/resource combination" -- so after the first push law_deadlines still returned 400, just with a different message. The registry split removed the shared-map MERGE-COLLISION surface; it never removed the handler requirement, and my own _resources/sairnlaw.js header already said "add the name here, and add its handler branch in api/sd-data.js as before" -- the spec line contradicted the file it described and I did not catch it until production said so. Corrected in `92c4783`: a law_deadlines read/write branch added to sd-data.js (pure addition, 39 lines, ZERO removed, CRLF preserved via the established autocrlf-safe procedure), and law_deadline_rules + law_holidays REMOVED from the registry because they are read/written by api/legal-deadlines.js talking to Supabase directly and never pass through sd-data.js -- registering them there made them look supported while actually producing the same "Unsupported" failure, which is worse than an honest unrecognised-resource error. Registry is 159, not 161. THE BLOCKER IS NOW GENUINELY FIXED, verified live: law_deadlines returns 200 {"ok":true,"data":[],"provisioned":false} -- an honest "table not created yet" -- where it returned 400 before. Every deadline in SAIRNlaw had been living on exactly one browser, never hydrated back, lost with the profile. ENGINE: api/_lib/deadline-engine.js is PURE (no I/O) so date arithmetic is testable against worked examples from the rule text without a database; all dates handled in UTC because local-time Date arithmetic is how off-by-one date bugs happen and this platform has already had one. FRAP 26(a) and Bankruptcy 9006(a) MAP TO the FRCP 6(a) implementation rather than getting copies that could drift. WHAT VERIFICATION CHANGED versus an implementation from memory: FRCP counts CALENDAR days (6(a)(1)(B) counts every intermediate weekend/holiday, only the LAST day rolls -- implementing it as business days errs LATER than reality, the dangerous direction, and the endpoint now REJECTS any rule pairing an FRCP-family standard with business_days); the pre-2009 sub-11-day weekend exclusion is OBSOLETE and not applied; 6(a)(6) is DIRECTION-DEPENDENT so state holidays count only for forward periods and each holiday carries a `kind` the engine consults against direction; 6(a)(5) backward periods roll BACKWARD. FAILS CLOSED with every refusal naming the missing thing -- NOT_PROVISIONED, NO_MATCHING_RULE (lists the triggers that ARE covered), NO_RULE_IN_FORCE (lists the windows), AMBIGUOUS_RULE (refuses rather than picking one of two overlapping rules), UNKNOWN_STANDARD, UNKNOWN_UNIT -- and no failure path returns a due_date, asserted by test. Nothing is computed from today's date and no trigger is ever inferred. The cross-year case works as specced: a 21-day period triggered 20 Dec crosses into the next year and the refusal names 2027, not the trigger year. RULES ARE VERSIONED DATA with required authority.citation + resolvable URL, verified_by stamped SERVER-SIDE from the real session, and required effective_from/effective_to so a matter triggered in 2023 computes against the 2023 rule. SEED is US Federal civil only, no scope expansion, every day count read from the primary source 2026-08-21 and QUOTED in the row. TWO DELIBERATE OMISSIONS WITH REASONS: Rule 12(a)(3) is NOT encoded because it runs from service on the officer or on the US attorney "whichever is later" -- a two-trigger rule the engine does not model, so encoding it would compute from whichever trigger was supplied and be too early roughly half the time; and Rule 6(d) is NOT attached to the waiver deadlines because 6(d) applies when a party must act "after being SERVED" while the waiver clock runs from when the request was SENT -- attaching it would extend a deadline the rule does not extend, verified by test (waiver + mail returns the same date). VERIFIED: Guardian v2 full scan clean -- 0 direct API calls, 0 service_role, 0 box chars in JS strings (28 in comment banners are pre-existing house style), 0 real duplicate ids, 0 undefined handlers, 0 duplicate function names, 20 nav / 20 panels 1:1, all localStorage keys law_ prefixed, 0 hardcoded keys, 0 console.log, div balance 945/945, dead-button audit 0 flagged across all 7 sections, session token scoped to 'sairnlaw' (Check 28). Engine isolated-logic tested 33/33 against worked examples from the rule text including a PLANTED-FAILURE check proving the holiday test can still fail; seed validated 5/5 against the real endpoint validator. A THIRD REGEX FALSE POSITIVE INVESTIGATED RATHER THAN REPORTED AS A BUG: the "duplicate id" tr-explain-result is two mutually-exclusive outerHTML branches inside JS string literals, and outerHTML REPLACES the node so only one ever exists -- confirmed by re-running the check against markup only (314 ids, zero real duplicates). BLOCKERS, NOT MINE: sql/sairnlaw_deadline_rules_schema.sql not yet run and the seed not yet loaded -- both degrade to an honest 503 NOT_PROVISIONED naming the SQL file. 2026-08-21
- docs/sairnlaw-international-coverage-scope.md + api/legal-reference.js + sairnlaw.html (Hank) -- SCOPE CORRECTION, no new build. Asked to build Phase 9 as two items from a 2026-08-20 commitment list: Wex definitions, and worldwide case grounding via BAILII/CanLII/AustLII. BOTH WERE ALREADY BUILT on 2026-08-21 (`1779346`, `ff26b05`, `e4fbb91`) and the second was built DIFFERENTLY ON PURPOSE, because checking each source against its own terms first found that BAILII and AustLII PROHIBIT this use -- AustLII across its entire collection for AI-related uses, BAILII naming outcome-prediction AI as its stated concern, and CanLII having sued an AI legal-research platform (Caseway AI, Nov 2024) over exactly this. I did NOT build the prohibited sources and reported the conflict instead; the Aug 20 list predates the Aug 21 finding that changed it. FINAL SCOPE, not a placeholder: US via CourtListener, UK England & Wales via Find Case Law under the Open Justice Licence (on-demand lookup only -- that licence excludes computational analysis), Canada via CanLII's keyed API. Australia and Ireland/Scotland are permanently excluded by their sources' own terms; no engineering closes a written prohibition, only a licence would. ONE REAL GAP FIXED WHILE RECORDING THIS: refRenderCoverage() fetched the server's top-level coverage note and discarded it, so the app showed 'Not covered' with a per-row reason but never the sentence saying the exclusion is by TERMS and is final -- which reads as a roadmap item. Now rendered, and the server note says so explicitly. Same permanent-vs-temporary distinction the deadline engine's Phase 8 gate caught. TWO BLOCKERS UNCHANGED AND STILL NOT MINE: sql/sairnlaw_wex_intl_schema.sql unrun (Wex + Find Case Law return honest NOT_PROVISIONED; fails closed because without the ledger the published crawl-delay cannot be honoured), and CANLII_API_KEY unset. Mock trial / AI-judge and jury modelling deliberately untouched -- flagged for their own scope pass. 2026-08-23
- .claude/skills/sairn-session-handoff/SKILL.md (Hank) — DONE. Handoff naming convention CHANGED platform-wide: app+counter (`SAIRNLAW-SESSION6-HANDOFF.md`) is superseded by app-date-subject (`SAIRNLAW-2026-08-23-lemaj-handoff.md`). Cause: two different real `SAIRNLAW-SESSION6-HANDOFF.md` files existed at once (trust-disbursement 2026-08-18 vs LeMAJ 2026-08-23) and a fresh session read the wrong one. The counter cannot stay unique across concurrent sessions. Also written into the skill: a handoff is not written until committed in the same action (the colliding file sat untracked 5 days in `C:\Users\marsh\` and would have blocked a pull there), and handoffs may only be written inside a real clone — never `C:\Users\marsh\`, which is itself a checkout of this repo and is the structural root cause. Retiring that checkout is FLAGGED for whoever manages clone setup, deliberately not actioned. Do not find the latest handoff by highest N any more. 2026-08-23
- sairnlaw.html + api/_lib/intl-caselaw.js + tools/vercel_config_check.py + tools/nav_panel_check.py + .claude/skills/sairn-guardian-v2/SKILL.md (Hank) — IN PROGRESS. Post-Guardian-pass fixes: CanLII coverage claim made key-aware, license-key setItem guarded, trust demo-seed labeled on screen, plus Guardian tool drift (vercel_config_check wildcard false-404s, nav_panel_check nav() support, dead Check 23 retired, handoff protocol renamed to date-stamped convention). 2026-08-23
- sairnlaw.html + docs/sairnlaw-ohio-efiling-scope.md (Hank) — DONE. Court e-filing, Ohio-first: the last open roadmap item. RESEARCH FINDING FIRST: there is no way to transmit a filing into an Ohio court from this app. Ohio has no statewide trial-court e-filing system (Civ.R. 5(E) + Sup.R. 27 = county-by-county local rules), counties run different vendors (Tyler/Odyssey, Henschen, county-built), and programmatic filing requires certification as an EFSP -- a contract, not an API key. Even the Supreme Court of Ohio publishes a web portal only. So NOT a filing transmitter. Built an E-Filing Readiness Check instead: inspects the actual PDF bytes and checks them against the real published rules of 2 courts (Supreme Court of Ohio; Franklin County C.P. General Division), every rule carrying its citation. Three-state results -- PASS / FAIL / CANNOT VERIFY -- because margins, line spacing and "exclusive of the table of contents" page counts cannot be read from PDF bytes and reporting them as passes would be the exact false-confidence failure this module exists to avoid. 49/49 isolation tests. Panel says on screen that it files nothing. 2026-08-23
- api/_lib/deadline-engine.js + api/legal-deadlines.js + sql/sairnlaw_deadline_{seed,calendars}_illinois.json (Hank) — DONE (code); LOADING next. Deadline engine jurisdiction #6: ILLINOIS, first of the market-size Batch 1 (IL -> FL -> CA). Every rule text read verbatim end to end before encoding. Illinois differs from all five existing jurisdictions in three ways that would silently produce wrong dates if copied: (1) 5 ILCS 70/1.11 has NO short-period exclusion, so it follows MI/PA not OH/IN; (2) 205 ILCS 630/17(a) states NO weekend observance shift at all, unlike federal/IN/MI/PA (both-way) and OH (Sunday-only); (3) Good Friday is a statutory holiday requiring real Easter computation, and general election day is statutory in even years only. DISCLOSED NOT RECONCILED: the statutory holiday list and the list Illinois COURTS observe (S.Ct. order M.R. 5272) disagree in BOTH directions -- M.R. 5272 omits Pulaski Day and Good Friday, and adds the Day After Thanksgiving. Calendars encode the STATUTORY list because 1.11 keys on statute, not on whether the clerk is open. 39/39 isolation tests. 2026-08-23
- api/_lib/deadline-engine.js + api/legal-deadlines.js + sql/sairnlaw_deadline_{seed,calendars}_florida.json + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — DONE (code); loading next. Deadline jurisdiction #7: FLORIDA. SCOPE CORRECTION: I proposed Florida as "data-only, no engine change". WRONG. Fla. R. Gen. Prac. & Jud. Admin. 2.514(a)(1)(A) says "begin counting from the next day that is not a Saturday, Sunday, or legal holiday" -- Florida SHIFTS THE START to the next business day and counts it as day one, where FRCP 6(a)(1)(A) merely excludes the trigger day. Every Florida deadline triggered on a Friday or the day before a holiday differs. New data-driven shifted_start property on the standard (not a new impl string), so a future state with this shape needs no code. Also new: fl_rgpja_2514b service extension = FIVE days for mail only, not the FRCP three. Holiday list is the RULE (2.514(a)(6)(A), nine enumerated observances), NOT all of Fla. Stat. 110.117 -- no Columbus Day, Presidents Day, Juneteenth or Good Friday. Chief-justice hurricane-extension limb deliberately NOT modelled, flagged in 3 places. 44/44 Florida + 39/39 Illinois still pass. Also created docs/SAIRN-OPEN-WORK-INDEX.md, one live index of every open item platform-wide. 2026-08-23
- api/_lib/deadline-engine.js + api/legal-deadlines.js + sql/sairnlaw_deadline_{seed,calendars}_california.json + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — DONE (code); loading next. Deadline jurisdiction #8: CALIFORNIA. Batch 1 complete (IL, FL, CA). TWO computation standards, not one: ca_ccp_12_12a for statutory periods (CCP 12/12a/12b/12c) and ca_crc_1_10 for rules-of-court periods (Cal. R. Ct. 1.10) -- a blanket standard would cite the wrong authority on the appellate rows. No engine change needed, and that was VERIFIED not assumed after Florida disproved the same prediction. Calendar is Gov. Code 6700 FILTERED BY CCP 135: five state holidays carved out (Lunar New Year, Diwali, Genocide Remembrance, Admission Day, Columbus Day), Good Friday excluded because 6700(a)(19) designates it noon-3pm only and 135 takes FULL days, three added (Thanksgiving, every Saturday, day after Thanksgiving). NO automatic weekend shift -- 135 makes it discretionary on the Judicial Council. CCP 12b office-closed limb NOT modelled, flagged. 8.104(a)(1) 60/60/180 encoded as three rows with a prominent caveat that the engine does NOT take the earliest for you. 52/52 CA + 47/47 FL + 39/39 IL. 2026-08-24
- api/_lib/deadline-engine.js + sql/sairnlaw_deadline_seed_california.json + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — DONE. (a) CA service extensions CCP 1013/1010.6 read verbatim and encoded: per-method amounts (5/10/12/20 CALENDAR days for mail by location; 2 COURT days for overnight/fax/electronic). Engine gained court-day counting and per-method amounts on the standard -- two court days across a weekend is four calendar days, and counting them as calendar days gives a date EARLIER than the true deadline. A bare "mail" is deliberately REJECTED because the amount depends on facts the engine cannot see. Appellate rows carry NO extension: both sections exclude a notice of appeal in terms. 84/84 CA, 47/47 FL, 39/39 IL. (b) HANDOFF-READING PASS over all 46 handoff files (6500 lines): 28 new rows added to the index, every one re-verified against current code before listing, and 7 STALE CLAIMS caught and listed under a new Contradictions section rather than silently resolved -- including a main/master divergence claim repeated across three SAIRNvet handoffs that is INVERTED (main is 1142 ahead of master, not 47 behind). Two genuinely urgent finds: SAIRNdental dnt_appointments still has a 64KB CHECK while photo capture ships ~900KB, and api/sairndental/send-reminder.js still reads the non-existent RESEND_FROM_ADDRESS and has never sent a reminder. 2026-08-24
- .claude/skills/sairn-app-scaffold/SKILL.md + .claude/skills/sairn-guardian-v2/SKILL.md + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — DONE. Two platform-wide patterns each found 3x/2x independently are now checked, not rediscovered. (1) SCAFFOLD: credential-deactivation lifecycle (set_active) is now a REQUIRED v1 component for any app with per-employee credentials -- four guards, each traced to a real incident: last-active-admin refusal, no self-deactivation, deactivated-caller re-check (a session token stays valid 12h AFTER its credential is deactivated, found live on SAIRNcode), audit on every outcome including refusals. CORRECTION TO THE BRIEF: the third app is SAIRNroofing (api/rf-auth.js, shipped in v1 because of the two retrofits), NOT SAIRNcash -- api/sairncash/ has no auth lifecycle at all, verified. (2) GUARDIAN CHECK 30: RESEND_FROM_ADDRESS vs RESEND_FROM_EMAIL name drift. Both known instances are already FIXED (alf-alerts.js and send-reminder.js, each with regression tests) so the check is PREVENTIVE -- and it needs to be, because the SAIRNdental email-reminders design doc still specifies the wrong name throughout, so anyone building from it writes the bug again. Ran the check against the real repo: 0 code hits, all 10 process.env.RESEND reads correct. Guardian now 30 checks. Also moved the send-reminder index row to Recently closed -- another session fixed it hours after the handoff sweep flagged it. 2026-08-24
- api/_lib/deadline-engine.js + api/legal-deadlines.js + sql/sairnlaw_deadline_{seed,calendars}_westvirginia.json + api/_lib/deadline-westvirginia.test.js + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — WEST VIRGINIA DONE and committed (`ad45643`), 123 -> 140 rules, 11 -> 12 jurisdictions. KENTUCKY BLOCKED, written up in docs/sairnlaw-kentucky-deadline-seed-blockers.md. WV NEEDED TWO COMPUTATION STANDARDS, the first jurisdiction here that does: R. Civ. P. 6(a) and R. App. P. 39(a) are different rules, and the appellate one KEPT a short-period exclusion ("less than seven days") that the civil one DROPPED in the 2025 restyling. THE 2025 RESTYLING IS THE STORY -- adopted 2024-01-31, effective 2025-01-01, first substantial revision since 1998, and the FORMER rules were read verbatim specifically to find out what changed rather than assuming the new text had always said it. Three changes, each of which the old rule would have encoded wrong: the short-period exclusion is gone (and it was "fewer than 11 days", not the 7 Ohio/Indiana/Georgia use, so it could not have been carried across from them); the service-extension SEQUENCING FLIPPED from "added to the prescribed period" to "added after the period would otherwise expire" -- FIRST JURISDICTION HERE WHOSE SEQUENCING CHANGED OVER TIME, so the standing habit has to be read per DATE not just per state; and the same state sequences its two extensions OPPOSITE WAYS AT ONCE, civil 6(e) after-expiry vs appellate 39(c) period-lengthening, so it also has to be read per BODY OF RULES. RULE 6(e) POINTS AT THE WRONG SUBPARAGRAPH AND THE ENGINE NOW REFUSES RATHER THAN PICKING A SIDE: WV inserted a state-specific 5(b)(2)(E) (WV E-Filing/fax) that pushed the federal lettering down one, so 6(e)'s pointer "(F)" now names "other ELECTRONIC means consented in writing" while its parenthetical "(other means consented to)" describes (G). Read by the pointer WV extends for consented e-service, which the federal rule stopped doing in 2016; read by the parenthetical it does not. New engine state refused_contested_standard reports both contested methods visibly -- the usual "prefer the earlier date" tie-break would mean adding nothing, and a silent no-extension is indistinguishable from "none requested", the exact defect this engine already fixed once. Verified against a SECOND independent rendering on the court's own site before building code on it. ONE CALENDAR CANNOT SERVE BOTH HOLIDAY LISTS: R. App. P. 39(a) names Juneteenth, R. Civ. P. 6(a)(6) does not and CANNOT reach it because 6(a)(6)(C) omits CONGRESS from the bodies whose declarations count. Calendars key on jurisdiction+year only, so the wv calendar encodes the CIVIL list and a WV appellate deadline landing on 19 June computes ONE DAY EARLY -- chosen because the opposite rolls civil deadlines LATE, per the Pennsylvania rule already in the engine header. "LINCOLN'S DAY" IS THE DAY AFTER THANKSGIVING, not 12 February: 6(a)(6)(A) says "Lincoln's Birthday" but counts "the day set aside BY STATUTE", and 2-2-1(a)(11) puts it after Thanksgiving. Weekend shift runs BOTH WAYS (Saturday -> preceding FRIDAY), which no other jurisdiction here does, and it puts a New Year holiday in the PREVIOUS year's calendar -- the generator's own self-check caught that entry filed under both years, where it would have been silently unreachable. 51/51 in a new isolated suite run against the real engine and the real JSON on disk, every expected date worked by hand from the rule text FIRST; all 16 other api/_lib suites still pass; the 17 rules and 6 calendars were also run through the REAL endpoint validators, not eyeballed. FOUND AND DELIBERATELY NOT FIXED (own index row): the us-federal answer-to-complaint row grants an FRCP 6(d) extension for service of PROCESS, which 6(d) does not reach -- it extends only service "under Rule 5(b)(2)" and a summons goes out under Rule 4. Error direction is LATE. Left alone because it is another jurisdiction's data. KENTUCKY, TWO INDEPENDENT BLOCKERS, NEITHER A FAILURE TO TRY: (1) kycourts.gov DOES NOT PUBLISH the Rules of Civil Procedure -- its rules page carries one link and it points to Thomson Reuters Westlaw. Not scraped, deliberately, same call as BAILII/AustLII and with CanLII's suit against Caseway AI in mind. So CR 6.01/6.05/5.02/12.01/33.01/34.02/36.01 were never read verbatim and every period computes off those numbers. (2) "LEGAL HOLIDAY" IS UNDEFINED FOR KY COURTS: RAP 6(A) and KRS 446.030 both use the phrase and neither defines it, and KRS 2.110 is titled "Public holidays", never says "legal holiday", LISTS FOUR DAYS KY COURTS DO NOT CLOSE FOR (Robert E. Lee Day, F.D. Roosevelt Day, Lincoln's Birthday, Confederate Memorial Day) and OMITS THANKSGIVING, which reaches KY only by annual presidential proclamation. Encoding it rolls deadlines LATER than any real court -- the INVERSE of the Pennsylvania case, so "encode the statute" produces the dangerous direction here and cannot be applied. Blocker 2 alone is fatal: no calendar means every KY row refuses. It is a lawyer's question and belongs with the open Illinois holiday row. BANKED AND REUSABLE: KY replaced its appellate rules 2023-01-01 (Order 2022-49) and CR 73.01-73.08 ARE REPEALED, so every summary still citing CR 73.02 for the notice of appeal cites a dead rule; RAP 3, RAP 6 and amended CR 77.04 read verbatim; KY ADDS 3 DAYS FOR ELECTRONIC SERVICE (eFiling Rules 13(6)), the opposite of FRCP 6(d); and KRS 446.030(1)(b) ROLLS A BACKWARD PERIOD FORWARD, which rollOff cannot express today. 2026-08-25
- api/_lib/deadline-engine.js + api/legal-deadlines.js + sql/sairnlaw_deadline_{seed,calendars}_northcarolina.json + api/_lib/deadline-northcarolina.test.js + tools/load_deadline_seed.py + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — WV AND NC BOTH LIVE-LOADED AND LIVE-VERIFIED, 140 -> 153 rules, 12 -> 13 jurisdictions. WV load: 6 calendars + 17 rules, then 8/8 real computes against the deployed endpoint including the new refused_contested_standard state and both backward rows rolling EARLIER off an observed holiday. NC (`eb548f3`): 13 rules, 9/9 live computes. NEW SOURCE-AVAILABILITY GATE, added because Kentucky's effort was spent before hitting a wall a five-minute check would have found -- a jurisdiction is not started until its primary sources are confirmed readable and permitted. NC was chosen partly BECAUSE its civil rules are statutes (G.S. 1A-1) published free and in full on ncleg.gov. NC REWRITES A RULE THIS ENGINE ALREADY HAD: Rule 6(a) rolls off "a legal holiday WHEN THE COURTHOUSE IS CLOSED FOR TRANSACTIONS" and never cites G.S. 103-4, so the calendar must be the NCAOC closure schedule, not the statute -- G.S. 103-4 declares ~9 days the Judicial Branch does not close for (Robert E. Lee's Birthday, Greek Independence Day, Halifax Resolves, Confederate Memorial Day, Mecklenburg Declaration, Washington's Birthday, First Responders Day, Columbus Day, Yom Kippur, general election day) and each would roll a deadline LATER. The engine header said flatly to encode the statute; that note was written for Pennsylvania, whose rule asks a STATUTORY question, and is now amended to say the choice depends on the rule's own test, with "prefer the source that fails EARLY" as the invariant. A live compute proves it: Columbus Day 2026 does NOT roll. COVERAGE IS 2026-2027 ONLY because NCAOC publishes ~2 years forward; crossing into 2028 refuses, pinned by two tests including a late-2027 period that crosses the boundary. Christmas is THREE days and Thanksgiving TWO, allocated administratively -- no rule produces those dates, so the calendar must never be generated. THREE DISCOVERY RULES, THREE DIFFERENT DEFENDANT PERIODS, each read off its own rule: Rule 33 30/45, Rule 34 30/45, Rule 36 30/60. All three are resolve_periods later_of. Rule 36 is the dangerous one -- SELF-EXECUTING, so a row built on 45 would concede facts 15 days before the defendant had to answer; a live compute proves 33 and 36 diverge on identical facts (2026-07-16 vs 2026-07-31). No later-of row carries a service extension, and that is a reason: limb one is Rule 5 service which 6(e) reaches, limb two is Rule 4 service which it does not, and the engine extends the winning RESULT without knowing which limb won. Rule 6(e) is MAIL ONLY and LENGTHENS THE PERIOD -- live-verified at 2026-06-03 where the federal order gives 2026-06-04. NOT SEEDED: all backward periods (Rule 6(a) defines the rollover only forward, and WV/Florida roll backward while KRS 446.030(1)(b) rolls FORWARD, so there is no default), NC appellate (needs its own source check -- the Rules of Appellate Procedure are court rules, not statutes), and Rule 6(f)'s 5-day ACP extension (its amount is conditioned on the BASE PERIOD LENGTH, which amount(method) cannot see). 54/54 NC + 51/51 WV + all 17 other api/_lib suites pass; every rule and calendar run through the REAL endpoint validators. TWO PROBLEMS FOUND AND REPORTED, NOT HIDDEN: (1) THE RULES ARE SPLIT ACROSS TWO LICENSES -- LAW-TEST-2026 holds the 8 older jurisdictions / 87 rules and every "live-loaded" claim in the index went there, LAW-PINNACLE-2026 (the production key) held NOTHING until today, and NEITHER has Texas, New York or Georgia, so Batch 2 was never loaded anywhere. No wrong dates result (an absent jurisdiction refuses), which is why it went unnoticed. Michael's call which tenant is canonical. (2) SELF-INFLICTED: I probed which computation standards were deployed by POSTing add_rule -- a WRITE endpoint -- and the probe rules were stored. A junk zz/__probe__ row is now live and this endpoint implements no delete. Lesson recorded in the loader's own docstring: never probe a deployment with a write endpoint; use rules_status. Also learned and recorded there: push and wait for the deploy before loading a jurisdiction that needs a new computation standard -- NC's first load rejected all 13 rules against a deployment that predated nc_rcp_6a while both calendars loaded, the exact half-load the loader orders calendars-first to minimise. 2026-08-25
- api/_lib/deadline-engine.js + api/legal-deadlines.js + sql/sairnlaw_deadline_{seed,calendars}_washington.json + api/_lib/deadline-washington.test.js + docs/sairnlaw-arizona-deadline-seed-blockers.md + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — WASHINGTON DONE AND LIVE (153 -> 167 rules, 13 -> 14 jurisdictions); ARIZONA GATED OUT AND BANKED. GATE RUN ON BOTH BEFORE ANY CODE, which is the whole point of adding it after Kentucky. WASHINGTON PASSED OUTRIGHT: its Superior Court Civil Rules are published free and in full, one PDF per rule, at courts.wa.gov. WASHINGTON IS THE ONE STATE THAT SETTLES THE HOLIDAY QUESTION BY TEXT -- CR 6(a) says, mid-rule, "Legal holidays are prescribed in RCW 1.16.050", an EXPRESS cross-reference no other seeded jurisdiction provides (Texas says "legal holiday" and never cites 662.021; Kentucky says it twice and defines it nowhere; WV defines it two ways in one state; NC asks whether the COURTHOUSE IS CLOSED). AND THE STATUTE EXCLUDES ITS OWN DECOYS: 1.16.050(1) is the legal holidays, 1.16.050(7) declares Columbus Day, Pearl Harbor Day, Marcus Whitman Day and a dozen more "recognized ... but may not be considered legal holidays FOR ANY PURPOSE". Only (1) encoded; two tests assert (7) days are absent, and a live compute proves Columbus Day 2026 does not roll. SUNDAY IS A LEGAL HOLIDAY under 1.16.050(1)(a) and is deliberately unlisted -- the engine excludes weekends before consulting a calendar, so 52 entries/year would change no date; it also explains why 1.16.050(5)(a) shifts a Sunday holiday "OTHER THAN SUNDAY". Both-way shift; the 2028 New Year lands Fri 2027-12-31 and is filed under 2027 only, so 2028 has ten entries and the others eleven or twelve. CR 12(a) HAS FIVE LIMBS AND THEY ARE NOT INTERCHANGEABLE: 20 days ordinary CR 4 service, 60 from FIRST PUBLICATION, 60 out-of-state personal, 60 in a jail/prison, and an open-ended fifth. Three seeded as separate rows; live-verified spread is 2026-05-21 vs 2026-06-30 on identical dates. The jail limb is NOT seeded -- it ends "unless a different time ... is specifically stated in statute" and those statutes were not researched, so a flat 60 would be silently wrong whenever one exists. ALL THREE DISCOVERY RULES USE 30/40, and that is a fact about Washington rather than a pattern: NORTH CAROLINA'S THREE DIVERGE AT 45/45/60. Tests assert WA's limbs, assert NC still reads 30/60, and run the SAME facts through both states -- live: WA 2026-07-13 vs NC 2026-07-31. CR 36 is where an error hurts most (self-executing), so importing NC's 60 would concede facts twenty days late. CR 6(e) IS MAIL ONLY AND LENGTHENS THE PERIOD, read up front; live-verified 2026-06-03 where the federal order gives 06-04. FIVE STATES NOW ANSWER E-SERVICE FIVE DIFFERENT WAYS (KY expressly adds 3, WV contested and refused in code, CA adds 2 COURT days, NC and WA don't cover it). effective_from is each RULE'S OWN last amendment date off its published footer -- better provenance than NC's blanket 1970 -- and CR 12 was amended as recently as 2025-09-01. NOT SEEDED: all backward periods (CR 6(d)'s 5-day notice and 1-day affidavit are backward AND both under the 7-day threshold; CR 6(a) defines the rollover only forward and the three jurisdictions that do define it disagree), and appellate pending its own gate. 48/48 new suite + 54/54 NC + 51/51 WV + every other api/_lib suite; 14 rules and 6 calendars through the real validators; loaded and 9/9 live computes. ARIZONA IS A NEAR MISS, NOT A KENTUCKY: azcourts.gov's own rules page links out to azrules.westgroup.com -> govt.westlaw.com for the maintained text, but what IS free there is the complete 2017 restyled rules PLUS a maintained per-rule amendment index -- and that index PROVES the snapshot is stale for four of the six rules a seed needs (R-17-0010 amended Rules 33-37, R-20-0028 amended Rule 12 eff. 2021-01-01). The index itself stops at 2024, so even reconstruction carries an unknown 2025-26 gap. Failed anyway because a reconstruction cannot prove its own completeness and a missed amendment yields wrong dates with NO refusal. BANKED VERBATIM: AZ Rule 6's short-period exclusion is ELEVEN days -- higher than any state seeded, all of which are 7, so encoding 7 by analogy would be wrong on every 7-10 day AZ period; Rule 6(a)(4) DEFINES THE BACKWARD DIRECTION expressly, so AZ backward rows would be seedable where NC's and WA's were not; Rule 6(c) adds FIVE days on the FEDERAL after-expiry order, matching only Florida. ONE DECISION -- reading Westlaw's terms once -- UNBLOCKS ARIZONA AND KENTUCKY TOGETHER. Also added AZ to the bundled holiday question, which now spans five jurisdictions and has a sharper framing: WA and NC have each answered it themselves and answered it DIFFERENTLY (express statute vs courthouse-closure test), so the real question is which source a rule's own words point at and what to do when they point at nothing. 2026-08-25
- api/_lib/deadline-engine.js + api/legal-deadlines.js + api/_lib/deadline-virginia.test.js + sql/sairnlaw_deadline_{seed,calendars}_virginia.json + tools/gen_va_{seed,calendar}.py + tools/va_rule_currency.py + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — VIRGINIA DONE, committed (`73735ab`), LOADED and LIVE-VERIFIED. 167 -> 190 rules, 15 -> 16 jurisdictions. TWO FIRSTS, AND THEY ARE THE STORY. (1) THE COMPUTATION AUTHORITY IS A STATUTE THAT REACHES STATUTES: Va. Code 1-210 computes time fixed by "an act of the General Assembly OR rule of court" -- the EXACT INVERSE of New Jersey's R. 1:3-1, which reaches time fixed "by rule or court order" and NOT by statute. The two were read independently and neither assumed from the other; a VA deadline fixed by statute is INSIDE this standard where the equivalent NJ one is outside. NO SHORT-PERIOD EXCLUSION ANYWHERE, checked against the full published Rules rather than inherited from the six seeded states that have one. (2) FIRST TIME-OF-DAY SERVICE EXTENSION: Va. Sup. Ct. R. 1:7 adds 0 days at/before 5:00 p.m. and 1 day after, FOR THE SAME METHOD -- manual delivery, fax, email and same-day commercial all swing on the clock alone. amount(method) could not express it, so amount() now takes a CONTEXT: amount(method, ctx) with ctx.service_time, ctx.base_period_count, ctx.base_period_unit. A MISSING service_time IS REFUSED, NOT GUESSED (new refused_missing_context state) -- defaulting to 0 runs EARLY and to 1 runs LATE, the rule allows both, and this is the only refusal in the file the CALLER can fix, so the message says exactly how. COMMERCIAL DELIVERY IS TWO METHODS because the rule splits by SERVICE BOUGHT not by clock: same-day follows the 5pm cutoff, next-day adds 1 whenever handed over before midnight; a bare 'commercial_delivery' deliberately does not qualify. ONE ADDITION SERVED BOTH BLOCKED RULES -- N.C. R. Civ. P. 6(f) was documented as blocked on this same missing shape and the shape was built general on purpose. 6(f) IS STILL NOT IMPLEMENTED AND ITS BLOCKER MOVED RATHER THAN VANISHED: it also needs to know whether the served party is an Address Confidentiality Program participant, a fact about the PARTY that this engine accepts no input for, and ACP status is confidential by the program's own design so trusting a caller to assert it is a real decision, not a default. Given its OWN index row so the next session does not re-derive it, and the stale clause in the NC gate row that still called it an amount() problem was corrected in place. THE 1-210(F) GAP IS DISCLOSED, NOT REFUSED, AND THE DIRECTION IS WHY: 1-210(F) makes any day the Governor authorizes closing state government a legal holiday, and 17.1-207 lets each clerk close for locality holidays, Christmas Eve and judge-authorized health/safety threats -- none knowable in advance, all per-locality. Omitting them can only report a date EARLIER than the true deadline, never later, so a new top-level JURISDICTION_COVERAGE block rides on every successful VA result instead of blocking it. That is the OPPOSITE call from Kentucky, whose KRS 2.110 gap runs LATE and therefore refuses -- the new table's header states the refuse-vs-disclose test explicitly so the next jurisdiction is decided, not guessed. GUARDIAN CHECK 29 CAUGHT A REAL BUG BEFORE IT SHIPPED, the exact California class: gen_va_calendar.py emitted kind:"derived", which is NOT in validateHolidayPayload's enum (federal/declared/state) -- all six VA calendar years would have been rejected by add_holidays while every engine unit test stayed green, because the engine and its validator are separate files and unit tests never touch the validator. Every sibling jurisdiction uses "declared". Fixed, then all 12 rule payloads and 6 holiday-year payloads were run through the REAL endpoint validators extracted from api/legal-deadlines.js, 0 rejected. EFFECTIVE_FROM IS REAL ON EVERY ROW, which NJ and NC could not manage (both carry a blanket 1969-09-08 and disclose it): the published Rules print a per-rule "Last amended by Order dated ...; effective ..." line and tools/va_rule_currency.py scopes STRICTLY from one rule heading to the next so no rule inherits its neighbour's date. CALENDAR IS SIX YEARS not one or two, because VA holidays are fixed by STATUTE in derivable terms (2.2-3300) rather than by an annual court order (NJ) or a published administrative schedule (NC) -- every date DERIVED, each carrying its own derivation string, generator asserting no duplicate and no weekend date per year. ELECTION DAY IS ANNUAL NOT EVEN-YEAR (VA elects its House of Delegates in odd years, same trap as NJ) and the DAY AFTER THANKSGIVING IS IN THE STATUTE ITSELF, unlike NC's administrative blocks. 47/47 in a new isolated suite against the real engine and real JSON on disk, every date worked by hand from the rule text first, including the 17:00 boundary minute and a North Carolina cross-check proving the new ctx argument does not regress a pre-existing standard; all other api/_lib suites still pass. LIVE-VERIFIED against the deployed endpoint, not assumed from the push: rules_status shows va = 12 rules / 2026-2031, the coverage block appears on a successful compute with NC returning coverage:null as control, and 17:00 -> +0 / 17:01 -> +1 confirmed live. 2026-08-25
- api/_lib/deadline-engine.js + api/legal-deadlines.js + api/_lib/deadline-massachusetts.test.js + sql/sairnlaw_deadline_{seed,calendars}_massachusetts.json + tools/gen_ma_{seed,calendar}.py + api/_lib/deadline-newjersey.test.js + sql/sairnlaw_deadline_calendars_newjersey.json + tools/gen_nj_calendar.py + docs/sairnlaw-massachusetts-deadline-seed-gate.md + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) — NEW JERSEY 2027 GAP CLOSED (`4e97e00`) then MASSACHUSETTS SEEDED (`b7d8150`). 190 -> 201 rules, 16 -> 17 jurisdictions, both LOADED and LIVE-VERIFIED. NEW JERSEY FIRST, BECAUSE A LIVE REFUSING GAP BEATS NEW BREADTH: every NJ deadline crossing into 2027 was returning HTTP 503 NOT_PROVISIONED in shipped coverage. THE PRE-FIX REFUSAL WAS CAPTURED LIVE BEFORE TOUCHING ANYTHING so the fix is proven by a changed outcome on IDENTICAL inputs, not asserted: 2026-12-01/12-20/12-31 went 503 -> 2027-01-05/01-25/02-04, with the 2026-05-01 control at 2026-06-05 both sides. RE-RUNNING THE GATE CHANGED THE ANSWER, which is the whole argument for re-running one: the prior session's note that the 2027-2028 order "has not been issued" was correct when written and is now false -- it issued 2026-06-01, /s/ Stuart Rabner C.J., published 06-08. Both covering orders re-read VERBATIM from the court's own PDFs via pypdf, NOT from the HTML notice pages (which are renderings of the orders, not the orders) and NOT assuming last year's format held. THE COVERAGE RULE IS NOW ENFORCED IN CODE: gen_nj_calendar.py holds each order with the court-year span its own operative language states and emits a calendar year only when BOTH covering orders have issued, so 2028 is WITHHELD despite its Jan-Jun half being known -- the engine refuses on a missing YEAR not a missing DAY, so a half-filled 2028 would look complete and silently under-report its second half. Adding next July's order makes 2028 emit with no other change. Generator reproduces the hand-built 2026 calendar BYTE-IDENTICALLY, so 2027 is provably additive. Live-verified the calendar is READ not merely present: Columbus Day 2027-10-11 rolls, Juneteenth rolls on Fri 2027-06-18 proving the THIRD-FRIDAY rule (2026 is the year the coincidence hides it), and a Court Recess day does NOT roll because R. 1:3-1 rolls off legal holidays only. The three NJ tests that asserted the gap now assert the fix and were KEPT not deleted -- the property they guard is unchanged. 56 -> 74. MASSACHUSETTS: gate PASSED (docs/sairnlaw-massachusetts-deadline-seed-gate.md) and seeded same session. R. 6(a) ANSWERS THE HOLIDAY QUESTION IN ITS OWN TEXT -- "'legal holiday' includes those days specified in Mass. G.L. c. 4, s. 7" -- an EXPRESS cross-reference like Washington's CR 6(a) -> RCW 1.16.050 and the opposite of TX/AZ/KY, so MA does NOT join the bundled lawyer's question. THE FIRST GEOGRAPHIC COVERAGE GAP IN THE ENGINE: Cl. 18 makes Evacuation Day (17 Mar) and Bunker Hill Day (17 Jun) legal holidays "with respect to SUFFOLK COUNTY ONLY", and holidayFor() keys on jurisdiction+year with no notion of place -- WV's two lists split by BODY OF RULES, this one by PLACE. Encoded statewide and DISCLOSED: including them rolls thirteen counties LATE, omitting them can only run EARLY in Suffolk, AND the same clause requires Suffolk offices to stay "open for business and appropriately staffed" on both days, so the omission may simply be CORRECT -- safe on both readings. Disclosure names BOSTON out loud so nobody reads it as an edge case. THE GENERATOR'S OWN ASSERTION CAUGHT A CARRIED-OVER ASSUMPTION AND THE ASSERTION WAS THE THING THAT WAS WRONG: the weekend-shift check copied from the VA/WV generators fired on 2026-07-04, because Cl. 18 shifts only "when any of said days occurs on Sunday" and is SILENT ON SATURDAY -- so 4 July 2026 really is a Saturday legal holiday in MA. Harmless downstream (R. 6(a) rolls off a Saturday in its own right) but a both-ways shift carried across would have INVENTED A FRIDAY HOLIDAY and rolled deadlines LATE. Check now asserts no SUNDAY can appear, which is what the statute actually guarantees. SECOND ENGINE FIRST: R. 33(a)(4) ABSORBS ITS OWN SERVICE EXTENSION -- "The period of time set forth in the previous sentence shall be deemed to include the three day period allowed pursuant to Rule 6(d)" -- so its 40-day row carries NO service_extension deliberately, and adding one would extend a deadline the rule does not. Live-proved: 40 days by mail == 40 days with no method == 2026-06-10, while R. 33(a)(3) on the same date goes 06-15 -> 06-18. DO NOT "FIX" IT. R. 6(d) EXTENDS FOR ELECTRONIC SERVICE (mail, email, electronic, efiling_service_provider), the opposite of FRCP 6(d) since 2016 and wider than NC's mail-only 6(e). R. 33 HAS NO DEFENDANT FLOOR where R. 34 and R. 36 both give 45 days, so TWO later_of rows not three -- MA is NOT internally consistent the way VA is (21/21/21). Answer is 20 days. R. 12(a)(2)'s two limbs differ on whether the extension applies (notice of the COURT'S action vs SERVICE of a paper) and are separate rows for exactly that. PATRIOTS' DAY (third Monday in April) exists in no other jurisdiction here and is what the live verification used to prove the calendar is actually read: 20-day answer from 2026-03-31 rolls off Mon 2026-04-20 to 04-21. 56/56 in a new isolated suite, all 28 api/_lib suites pass, all 11 rule payloads and 6 holiday-year payloads run through the REAL endpoint validators. mass.gov 403s EVERY automated fetch including its own PDF download links while Playwright gets 200 -- a 403 there means "not a browser", never "page absent"; same shape as NC's nccourts.gov. 2026-08-25

- docs/SAIRN-OPEN-WORK-INDEX.md + LIVE DATA on LAW-PINNACLE-2026 (Hank) -- **THE 87/48 "DIVERGENCE" WAS A FALSE POSITIVE, AND CHASING IT FOUND A REAL LIVE DEFECT UNDERNEATH.** `sql/platform_reference_rules_divergence_2026-08-28.sql` reported 87 diverged rule ids and 48 diverged holiday ids between `LAW-PINNACLE-2026` and `LAW-TEST-2026`. Cause, found by diffing actual blobs: **`authority.verified_by`**, which `api/legal-deadlines.js` writes INTO `data` on every add_rule/add_holidays (`caller ? caller.employee_id : null`). TEST was loaded through an authenticated employee session -> `"hank-verify"` on every row; PINNACLE was backfilled by `tools/load_deadline_seed.py`, bearer-key only, no session -> `null` on every row. One field, present on 100% of rows by construction, so `md5(data::text)` differs on 100% of shared ids. The counts confirm it exactly: TEST holds 87 rules (live `rules_status`) and 8 jurisdictions x 6 years = 48 calendars. 87/87 and 48/48. This is precisely the false-positive class that file warns about at its own line 159 -- the warning was right and should stay.
  **NOT a customer-facing divergence either way:** `sql/sairnlaw_test_license_seed.sql` provisions LAW-TEST-2026 as `test@sairnlaw-verification.example`, an internal verification tenant deliberately left stale when PINNACLE became canonical 2026-08-25. Only two SAIRNlaw keys exist in `sql/`.
  **THE REAL FINDING: two committed fixes had never been loaded, and were live-wrong on the canonical customer licence.** `e1aa3f8` (Rule 6(d) does not reach Rule 4) and `a9daad1` (Florida exclusivity) changed SEED FILES. A seed-file change is inert until the loader runs, and nobody ran it. Live PINNACLE was computing FRCP 12(a)(1)(A)(i) on mailed service as **2027-06-25 (+3)** and 12(a)(2) as **2027-08-05 (+3)** -- the exact three-days-late defect `e1aa3f8`'s own message says it fixed. Reloaded us_federal + florida + illinois into PINNACLE on Michael's go-ahead: 23 rules + 2 calendars, zero failures. **Verified by changed OUTCOME on identical inputs, not the loader's exit code:** 12(a)(1)(A)(i) -> **2027-06-22**, 12(a)(2) -> **2027-08-02**, both `service_extension.state:"not_requested"`; Florida `applied` -> **`applied_exclusivity_assumed`** (date unchanged by design). **Mirror-image control in the same pass:** `frcp-12a1B-counterclaim-crossclaim` KEEPS its +3 -> 2027-06-25, so Rule 5 rows were not stripped along with the Rule 4 ones.
  **CAVEAT CLOSED, not hand-waved.** `verified_by` explains why all 87 flagged, not that the other 84 blobs agreed. Ran a read-only compute-diff of all 89 repo rules in TEST's 8 jurisdictions on BOTH licences, 178 probes, comparing whole normalised responses with `verified_by` stripped: **160 identical with a real date, 4 identical refusals, 4 probes for the 2 rule ids TEST never had (`frcp-15a3-response-to-amended-pleading`, `oh-appr-4B1-cross-appeal`), 10 divergent across exactly 7 rule ids -- every one of them a rule touched by the two 2026-08-27 commits, with TEST the stale side.** Only TWO produce a different DATE: `frcp-12a1Ai` and `frcp-12a2`, both +3 on mail. The other five differ in note/label text only. So: 80 of 87 shared ids are byte-equivalent modulo `verified_by`, 7 are stale-vs-corrected, 0 are unexplained. **Re-ran the diff before trusting it** -- the first pass showed an 11th divergence on `ca-ccp-41220a3-response-to-summons` that was a transient `Upstream connection error` on one side, not a data difference; a single-pass result would have reported a phantom California defect.
  **SECOND STALE-LABEL PROBLEM, corrected in the index:** it claimed 24 computing jurisdictions / 269 computing rules live. Live is **19 / 226** (`rules_status`, re-verified after the reload). The five "so the live count is now ..." updates (UT, NV, OR, OK, SC) each asserted a LIVE number for a jurisdiction written to a seed file and loaded to no licence -- 43 computing rules exist only in `sql/`, plus `ct` which is unprovisioned on purpose. There is no third tenant they could have landed in. The header cell of that row said 19/226 all along and was the only thing in it that was right. **Loading those five was NOT done -- out of scope for tonight's authorisation, and it is new coverage rather than a defect fix. Queued.**
  **Standing lesson worth more than the fix: "fix committed" != "fix live" for anything whose payload is a seed file.** The push protocol's live-verify step covers deployed CODE; rule DATA has no equivalent gate, which is exactly how this survived a day on the canonical licence.

- api/legal-deadlines.js + tools/sairn_load_state_check.py + LIVE DATA (Hank) -- **THREE QUEUED ITEMS CLOSED, AND THE GATE FOUND A SIXTH THING ON ITS FIRST RUN.**
  **(1) UT/NV/OR/OK/SC LOADED into LAW-PINNACLE-2026** on Michael's authorisation -- 43 rules + 25 calendars, zero failures, so live coverage is now **24 computing jurisdictions / 269 computing rules**, which is what the index had been claiming since 2026-08-27 and is now actually true. Verified in TWO passes, because "every rule returns a date" and "the calendar is actually read" are different claims and only the second fails quietly. Pass 1: 43/43 compute a real date. Pass 2, with the trigger date derived from each rule's own count so the base period lands exactly on the target holiday: **Pioneer Day (ut, 2026-07-24) rolls, Nevada Day (nv, 2026-10-30) rolls, Confederate Memorial Day (sc, 2026-05-11) rolls** -- none of the three exists in any other jurisdiction, so nothing else could produce those dates. **Two NEGATIVE probes, which are the ones a copied allowlist would fail:** Oklahoma OMITS Juneteenth and Oregon OMITS Columbus Day, so 2026-06-19 (ok) and 2026-10-12 (or) must NOT roll, and they do not. Utah's 2027 cap still refuses `NOT_PROVISIONED` -- correct, its calendar is 2026 only.
  **(2) DIVERGENCE QUERY FIXED** -- all three hash sites now `md5((data::jsonb #- '{authority,verified_by}')::text)`. jsonb cast is deliberate: `#-` has no json counterpart. Expected on re-run: 7 diverged rules, 0 calendars. **NOT RUN by me -- no DB access from this clone; it needs a Supabase-editor run to confirm the 7/0.**
  **(3) THE LOAD-STATE GATE, built and PROVEN AGAINST THE DEFECT IT EXISTS FOR.** New read-only `rules_fingerprint` action returns `{entry_id, version, hash}` per row -- a hash, not the blobs, so it detects drift without turning a licence-gated endpoint into a bulk export of every rule's content. `tools/sairn_load_state_check.py` diffs seeds vs licence: MISSING / STALE / EXTRA, exit 1 on drift, **exit 2 when it could not tell, so "unreachable" can never be misread as "pass"**. It does NOT offer to fix what it finds -- reloading a customer licence is a decision with a person's name on it.
  `version` is REPORTED, never trusted: where a row is stale while its version matches the seed's, it says so under its own heading, which is the version discipline failing out loud.
  **Retroactive proof, not a claim:** run against LAW-TEST-2026 (still holding the pre-fix rows) it names **exactly the same 7 stale rule ids** the compute-diff found earlier tonight -- by content hash, a completely different method from live computes -- and fires the version-silent warning on all 7. Against PINNACLE it now reports **0 missing / 0 stale / 0 extra**. `--expect-stale` exits 0 for a knowingly-behind tenant so a known-stale licence never trains anyone to ignore red.
  **THE TWO HASH IMPLEMENTATIONS WERE PROVEN EQUAL, NOT ASSUMED** -- JS extracted from the endpoint source and run against the Python over all 401 seed entries: 401/401 identical. A checker whose digest disagrees with the server's invents drift and gets switched off within a week.
  **THAT CROSS-CHECK FAILED FIRST TIME, AND THE FAILURE WAS REAL: FIVE 2027 CALENDARS ARE DEFINED TWICE WITH DIFFERENT CONTENT.** `in:2027 mi:2027 oh:2027 pa:2027 us-federal:2027` each appear in BOTH `calendars_2027_2031.json` and their own `seed_*.json`. The 2027_2031 copies carry **2027-12-31 "New Year's Day (observed)"** -- 1 Jan 2028 is a Saturday -- and the per-state copies omit it. Whichever the loader is handed LAST wins, decided by argument order, not by anything in the repo. **CONFIRMED LIVE: PINNACLE holds the copies WITHOUT it** -- federal 21-day answer triggered 2027-12-10 returns **2027-12-31 with no rollover step**. Error direction is EARLY (conservative), not late, which is why this is a question and not an emergency. **NOT resolved by me: which copy is canonical is a sourcing question about FRCP 6(a)(6), and picking one silently is exactly what this gate exists to prevent. Michael's call.**
  **CONNECTICUT no longer reads as MISSING forever.** Its seed now carries a top-level `_hold_reason`; the checker holds those entries out of the comparison and lists them separately. The loader reads only `rules` and `holiday_calendars`, so the key records intent without changing what a load does. A check that is always red is a check nobody reads.
  779/779 deadline tests pass, unchanged -- this adds an action and a tool and touches no computation.

- sql/sairnlaw_deadline_calendars_2027_2031.json + five seed files + tools/ (Hank) -- **2027-12-31 RESOLVED FROM PRIMARY SOURCE, AND IT WAS NOT "TAKE THE MORE COMPLETE FILE".** Five 2027 calendars were defined twice with different content. Reading each jurisdiction's own authority rather than picking a winner produced **four yeses and one no**:
  **us-federal YES.** FRCP 6(a)(6)(A) defines a legal holiday as "the day set aside **by statute** for observing New Year's Day"; 5 U.S.C. 6103(b)(1) provides that "instead of a holiday that occurs on a Saturday, the Friday immediately before is a legal public holiday". 1 Jan 2028 is a Saturday, so Friday 2027-12-31 is that day. **The deciding evidence was internal consistency, not completeness:** the seed copy already applied the SAME 6103(b) mechanism to Christmas (25 Dec 2027 is a Saturday; it carried 2027-12-24 "Christmas Day (observed)") and omitted the identical shift four weeks later. No reading of 6(a)(6) makes 12-24 a holiday and 12-31 not. **Limit recorded, not argued away:** 6103(b)(1) is written "for employees whose basic workweek is Monday through Friday" and for 5 U.S.C. 6309, not as a redesignation for all purposes.
  **in YES** -- IC 1-1-9-1(b)'s two-way shift, already applied by the same file to 2027-07-05 and 2027-12-24. **mi YES, and it is the subtle one** -- MCR 8.110(D)(2) shifts Saturday holidays back to Friday, so 12-31 is New Year's Day observed and sits ALONGSIDE New Year's Eve, which itself falls on Friday 12-31 and shifts back to Thursday 12-30. Two adjacent holidays is correct, and is the exact shape the file already carried for 12-23/12-24. **pa YES by the FEDERAL half** of Pa.R.J.A. 107(b)'s two-sovereign construction, mirroring its existing federal-observed 12-24 alongside statutory 12-25.
  **oh NO.** Ohio Rev. Code 1.14 does not shift Saturday holidays -- which is why oh has no 2027-06-18 and no 2027-12-24 where federal has both. **The 2027_2031 file's DATES were right for Ohio and its NOTE was wrong:** a generic amendment note applied to all five jurisdictions asserted "this jurisdiction observes it on FRIDAY 2027-12-31", contradicting its own date list. That is the dangerous direction -- a reader checking the reasoning would have "corrected" the data to match the prose.
  **FIXED STRUCTURALLY, not just for this round:** the duplicate 2027 blocks were removed from all five seed files by TEXT SPLICE (219 pure deletions, zero reformatting -- three of the five do not round-trip through json.dumps and a re-dump would have buried the change), leaving `calendars_2027_2031.json` as the single definition. Its five generic notes were replaced with per-jurisdiction reasoning. 131 calendar entries, 131 distinct, **0 duplicates**.
  **LIVE-VERIFIED BY CHANGED OUTCOME on identical inputs, with the negative control in the same pass.** Before: all five returned 2027-12-31 with `steps:['base_period']`. After: **us-federal / in / mi / pa -> 2028-01-03 with a `rollover` step**, and **oh -> 2027-12-31, still no rollover** -- which proves the reload did not blanket-apply the holiday and that Ohio's non-shift decision actually landed.
  **GATES CONSOLIDATED to one.** `tools/sairn_load_state_check.py` is canonical; `tools/sairnlaw_build_load_gate.py` + `sql/sairnlaw_load_gate_generated.sql` (`ca83d1f`, the parallel session's DB-side SQL gate) are removed. Reasons in the tool's own docstring so the deleted work is not silently erased: mine needs only a licence key (runs from any clone, can gate a pre-push step), reads the seeds at run time so it cannot go stale where a GENERATED gate must be regenerated after every seed edit, covers CALENDARS as well as rules -- theirs globbed `seed_*.json` only and **would have missed tonight's five-calendar finding entirely** -- and reports MISSING/EXTRA, not just STALE. What theirs had that mine does not: Postgres compares jsonb canonically, so it never had to make two languages agree on a digest; that risk here was closed by proof, 401/401 entries identical. **A defect in the deleted gate, recorded so it is not reintroduced: its INERT_KEYS listed `computation` as unable to change a date.** It selects the counting standard, and a rule whose standard drifted would have passed that gate clean.
  Gate now exits 0 on PINNACLE: 269 rules, 131 calendars, 0 missing / 0 stale / 0 extra. 779/779 deadline tests pass.

- api/reference-fingerprint.js + tools/sairn_load_state_check.py (Hank) -- **ONE GATE STYLE PLATFORM-WIDE, and it found a real gap on its first run against a new app.** New read-only `api/reference-fingerprint.js` covers every app's per-licence reference tables; `tools/deadline_load_state_check.py` renamed to `tools/sairn_load_state_check.py` and given `--app`. **Hashes the WHOLE ROW, not `data`** -- only SAIRNlaw keeps everything in one blob; `rf_contingency_rules` keeps `count`/`unit` as real columns, so a `data`-only fingerprint would miss a wrong count entirely (CC's observation from the superseded gate, carried over rather than rediscovered). **Null-valued keys dropped on BOTH sides**, which is load-bearing: `effective_to`/`role`/`facility_class`/`business_day_basis` come back from Postgres as explicit nulls and every such row would otherwise read as drifted forever. Also carried over: the two write-path normalisations read out of `api/sd-data.js` (`state` uppercased, `status` defaults to `active`) -- without them every `alf_payer_rules` row would have reported false STALE on the first run.
  **LIVE RESULTS, all four tables verified in one pass:** sairncare `alf_compliance_rules` 16/16 and `alf_payer_rules` 6/6 clean (ALF-TEST-2026); sairndental `dnt_cred_rules` 6/6 clean (DNT-PINNACLE-2026); sairnroofing `rf_cert_rules` 3/3 clean. **Zero false positives anywhere** -- the normalisations are right.
  **THE FINDING: `rf_contingency_rules` holds 0 rows on RF-PINNACLE-2026 while the seed has 2** (`RFCON-OH-HSSA-CANCEL`, `RFCON-OH-HSSA-SCOPE`). Same class as tonight's SAIRNlaw defect -- seeded in the repo, never loaded live. **It does not fail loudly:** `api/sd-data.js:4033` returns `{ok:true, data:[], provisioned:true}`, so a foreman asking for the Ohio HSSA cancellation window gets an empty list with no signal that anything is missing. **NOT LOADED BY ME** -- writes to this table require a real management session token for RF-PINNACLE-2026, which is a materially different operation from the SAIRNlaw bearer-key loader, and it is a different app than tonight's authorisation covered. Raised for Michael. I did NOT trace what the roofing UI renders on an empty list, so no claim is made about what a contractor actually sees.
  **CC FLAGGED IN THE FILE THEY WOULD REACH FOR:** `tools/sairn_build_load_gates.py` now opens with a superseded header addressed to them, explaining that a GENERATED gate must be regenerated after every seed edit and a forgotten regeneration is the same silent-failure shape the gate exists to catch. The file is KEPT, not deleted, because three of its findings are load-bearing and are now carried in the replacement -- promoted columns, the two normalisations, and that `sc_anesthesia_base_units` has NO seed file anywhere in the repo, so no gate can be built for it and that absence is itself the finding. Its five generated `.sql` gates are removed; regenerating them would put two gates back.
  **NEW REPORT the old gate could not produce:** seed keys that NO column can store. The write paths persist an explicit column list and discard anything else, so a seed field nobody stores is invisible from both ends. Underscore-prefixed keys are excluded by convention (`_note` is documentation and is meant not to load).
  SAIRNlaw unchanged after the rename: 269 rules, 131 calendars, 0/0/0.

- tools/seed_load_gate_hook.py + .claude/settings.json + CLAUDE.md (Hank) -- **ITEM 4 DONE: the gate runs on every push instead of from memory.** PreToolUse Bash hook, same shape as `git_push_master_guard.py`. Reads the commits ACTUALLY being pushed and returns immediately if none touch a seed file, so an ordinary push costs nothing. Live matches repo -> allow silently (the normal case when you loaded first, which is why a correct workflow feels no friction). Drift -> **DENY**, naming app, rule id and reload command. Could-not-tell (no key, endpoint down) -> allow with a loud note saying **it is not a pass** -- blocking someone with no licence key would get the hook deleted within a day, and a deleted hook checks nothing.
  **PROVEN TO BLOCK BEFORE BEING TRUSTED**, same standard as the federal service-extension test: a temp commit changing one Florida citation string was made, the hook denied it naming `fl-rcp-1140a1-answer-to-complaint seed 553d2ae906e3903a != live c9abcfec7547239e`, `SAIRN_SEED_GATE=off` was confirmed to allow, the temp commit was reset, and the hook confirmed silent again on the clean tree. A gate nobody has seen fire is a gate whose behaviour nobody knows.
  **Clone-agnostic by construction** -- repo comes from `git rev-parse --show-toplevel` against the CWD, never a fixed path; with four clones a hardcoded path checks the wrong tree three times out of four. Fails OPEN on internal error, same standard as the other hooks. The one thing that CAN go stale is named in its own header: `SEED_PATTERNS` matches path shapes so new seed files are covered automatically, but a new APP must be added by hand. CLAUDE.md gains step 3 of the Push Protocol.
  **DIVERGENCE SWEEP CONFIRMED by Michael 2026-08-29: 7 rules, 5 calendars, everything else consistent. I predicted 7 and 0.** The 5 are `in/mi/oh/pa/us-federal :2027`, corrected on PINNACLE *later the same day* the 0 was written, with TEST still holding the pre-correction copies. Recorded in the SQL file WITH the reason rather than editing the note to match the result -- a prediction written before a change and read after it is a prediction about a different database.
  **ITEM 2 RESOLVED BY NOT DOING IT, which is the correct answer.** `sc_anesthesia_base_units` was carried in CC's gate as "no seed file anywhere in the repo -- the absence is the finding", and **I repeated that twice before checking it. It is wrong in both halves.** SAIRNcode's own build record (`SAIRN-ACTIVE-WORK-cc.md`, 2026-08-20) says the table is **empty-by-default and Source-required**: the coder enters base units with their own citation, the case form auto-fills only from a verified match, and says "not in your reference table yet" rather than inventing a number. It is **customer-owned data, correctly seedless** -- and its migration is queued and unrun, so there is no live table to compare against either. **Authoring a seed would have pushed unverified values into a table built to hold only what the practice verified -- the fabricated-data class, committed by the tool meant to prevent it.** Corrected in both files. General shape worth keeping: before calling a missing seed a defect, ask whether the table is platform reference content or the customer's own; only the first kind can drift, because only the first kind has a repo to drift from.
  **ITEM 1 BLOCKED, and not probed.** `rf_contingency_rules` writes need a MANAGEMENT session on RF-PINNACLE-2026 (`api/sd-data.js:4043`); there is no bearer-key-only path. `check_license` confirms the licence is live and active; `whoami` returns `NO_SESSION`. **I did not probe with `bootstrap`** -- it is a WRITE that creates a real owner credential when a licence is unprovisioned, which is exactly the mistake `tools/load_deadline_seed.py`'s own header records ("NEVER PROBE A DEPLOYMENT WITH A WRITE ENDPOINT" -- a junk `zz`/`__probe__` row is still in the live deadline store). So either the licence already has credentials and I would be guessing a PIN against a real lockout threshold, or it does not and bootstrapping is a provisioning decision rather than a data load. Both need Michael. Same discipline that produced `sql/sairnlaw_test_license_seed.sql`.

- LIVE DATA on RF-PINNACLE-2026 + docs/SAIRN-OPEN-WORK-INDEX.md (Hank) -- **ITEM 1 CLOSED, and reading the roster first changed the answer twice.** Michael's recollection was that `rf-verify-admin` had been seeded and later CLEANED UP. **It has not been.** `sql/sairnroofing_access_panel_verify_cleanup_2026-08-28.sql` deletes all five disposable accounts and **has not been run** -- every one is still live. Checked before touching anything, exactly as instructed.
  **THE ROSTER CONTRADICTED THE OPEN-WORK INDEX TOO.** Row 186 was HIGH PRIORITY on the basis that RF-PINNACLE-2026 had **zero active owners** and was API-unrecoverable. Live `roster`, read with a real signed session: 5 rows and **TWO active owners** -- `rf-verify-admin` is **role `owner`, not the `admin` its own seed file still says** (someone applied precisely the promotion that row proposed as recovery action 1), and `rf-verify-owner` is **`active: true`** where the row recorded it false. `rf-verify-ui` is still owner/inactive; both foremen active. So the licence recovered at some point and nothing said so. Row corrected; **the four-app trapdoor ANALYSIS underneath it is untouched and still open** -- that is about the missing recovery PATH, not this licence's state.
  **NOTED, because it is the next trapdoor rather than a tidy-up:** the recovery now rests on a credential that a queued cleanup script is written to delete. Running that file as-is would recreate the exact zero-owner state row 186 describes. It should not be run until an owner that survives it exists.
  **No bootstrap decision was needed and none was made.** `MANAGEMENT_ROLES = {owner, admin}` (`api/rf-auth.js:95`) is what gates `rf_contingency_rules` writes, so the existing owner session sufficed. I logged in with the PIN already documented in `sql/sairnroofing_verify_admin_seed.sql` -- a recorded credential, not a guess -- and still did **not** probe with `bootstrap`, which is a WRITE.
  **LOADED AND VERIFIED THROUGH A SECOND PATH.** Before: `rf_contingency_rules` 0 rows, `provisioned: true` (so it was failing silently, not refusing). Wrote both Ohio HSSA rules -- `RFCON-OH-HSSA-CANCEL` and `RFCON-OH-HSSA-SCOPE`, both `OH / execution / 3 business_days / basis oh_hssa`. After: 2 rows, read back correct. **Then confirmed independently by the load-state gate, which recomputes the content hash from the seed rather than trusting the write's own echo: `rf_contingency_rules` 2/2, `rf_cert_rules` 3/3, 0 missing / 0 stale / 0 extra, exit 0.** SAIRNroofing is now clean on both tables.
  Provenance note: `verified_by` on both rows is `rf-verify-admin`, a disposable account, because the server stamps it from the session. Acceptable on a demo licence, wrong on a real one -- worth remembering before this pattern is used to seed a paying customer.

- sql/sairnroofing_access_panel_verify_cleanup_2026-08-28.sql + docs/2026-08-29-single-provisioning-role-trapdoor-analysis.md + SAIRN-BACKLOG.md (Hank) -- **CLEANUP REWRITTEN AND GUARDED; TRAPDOOR ANALYSED; `verified_by` LOGGED.**
  **The cleanup file: narrowed to the two foremen per Michael, and made structurally safe.** It now deletes only `rf-verify-fmA`/`fmB` -- neither is an owner, so it cannot change the active-owner count at all. Added a `do $$` guard that asserts the END STATE is one of the two safe shapes (zero rows, or >=1 active provisioner) and **rolls the whole transaction back otherwise**, so a future pass that widens the delete list cannot leave the licence unrecoverable whether or not it read the reasoning.
  **ONE CORRECTION RAISED BEFORE ACTING, and it is worth keeping.** The concern was that deleting `rf-verify-owner` recreates the zero-owner lockout. **Deleting ALL FIVE would not have** -- the trapdoor is "rows exist AND none is an active provisioner", because `bootstrap` refuses 409 while ANY row exists and does not filter on `active`. Zero rows RE-ARMS bootstrap and is recovery, which is what the original file's reasoning was built on. **The only dangerous shape is deleting SOME owners and leaving others**, which is exactly how this licence got stuck. Proceeded with the narrower instruction anyway -- it is safe under either reading -- and wrote the distinction into the file so nobody has to hold it in their head again. **Cost of the conservative option, stated in the file rather than hidden: all three owner rows survive and two are loggable-in with PINs committed to this repo.**
  **NOT RUN -- no DB access from this clone** (re-verified: no Supabase env vars, `.env.local` is 0 bytes, no `psql`), and no roofing table grants DELETE to `service_role`, so the API cannot perform it by design. Needs a Supabase-editor run.
  **THE TRAPDOOR ANALYSIS, and the finding that changes its recommendation.** Read from source, not carried from the index row. `PROVISIONING_ROLES`: dnt `['owner']`, mech `['owner']`, rf `['owner']`, sc `['admin']`, sd `['owner','admin']`. **SAIRNdental is the worst-placed, not SAIRNcode** -- the only app where PROVISIONING and MANAGEMENT are both a single role AND the same role, so there is not even an `admin` to promote. The SAIRNcode `admin`-not-`owner` trap is real but applies to NEW code only: all five existing guards read `PROVISIONING_ROLES` rather than a literal, which I checked rather than assumed.
  **THE API CANNOT REACH THE STATE.** `set_active` requires an active provisioning caller, re-reads the roster to confirm the caller's OWN row is still active, refuses self-deactivation, and refuses when the target is the last active provisioner. The count cannot cross 1 -> 0 by any API path. So RF-PINNACLE-2026 got stuck through **seed files and direct SQL**, which is the only door and the one with nothing on it. **That kills the obvious fix and also the usual objection to it:** softening `bootstrap` to "no ACTIVE provisioner" is normally rejected because someone with the licence key could deactivate their way to a fresh owner -- **that attack is not reachable**. It is still the wrong fix, for a different reason: a licence key is shared with every employee and is not a secret, so an org legitimately sitting at zero active provisioners (owner on leave) would let anyone holding the key seize a live tenant. Recommendation: **guard the SQL door (urgent), add detection reading each app's own PROVISIONING_ROLES (RF sat in the state, recovered, and nothing noticed either transition -- the index row still asserted zero active owners when the live roster had two), and treat widening the role pool as a per-app authorisation decision rather than a safety patch** -- StoneDesk's two roles are a reduction in exposure, not immunity, so the real question is "how many provisioners must remain active".
  **`verified_by` logged in SAIRN-BACKLOG.md as a pattern, not a design.** It records who was SIGNED IN when a row was written, not who verified the content, while its schema comments claim the latter -- and the real provenance is already in `data.authority`. It has cost once already in the other direction: being inside the SAIRNlaw `data` blob is what produced the false 87/48 divergence, and two tools now subtract it explicitly. Seven tables affected. Acceptable on demo tenants; the line not to cross is a paying customer, and nothing currently stops it.

- tools/employee_auth_guard_check.py + api/provisioner-health.js + tools/licence_recoverability_check.py + docs/2026-08-29-sairndental-provisioning-role-decision.md (Hank) -- **THE SQL DOOR IS GUARDED, THE STATE IS DETECTED, AND THE SAIRNDENTAL DECISION IS WRITTEN UP.**
  **GUARD (top priority).** Five 2026-08-28 Access-panel cleanups now end in a `do $$` block asserting the END STATE and rolling the whole transaction back otherwise: **safe shape A = zero rows** (this RE-ARMS `bootstrap`, it is recovery not lockout), **safe shape B = >=1 active provisioner**. Each file uses **its own app's roles read from source** -- sc `['admin']`, dnt/mech/rf `['owner']`, sd `['owner','admin']`; a guard hardcoding `owner` would pass SAIRNcode clean forever while checking nothing. `tools/employee_auth_guard_check.py` requires BOTH a transaction and the guard on any `sql/` file writing an `*_employee_auth` table -- a guard with no transaction cannot roll back, which is why the older files could not just have one pasted in. **19 pre-existing writers are GRANDFATHERED in an explicit list, not silently excluded; they are not fixed, only visible.** 24 writers / 5 guarded / 19 grandfathered / 0 unguarded. Wired as check 2 of the push hook (renamed `seed_load_gate_hook.py` -> `sairn_push_gate_hook.py`), and **proven to block before being trusted**: a temp unguarded delete against `sairndental_employee_auth` was committed and the hook denied the push naming the file and both reasons.
  **DETECTION.** `api/provisioner-health.js` -- read-only, licence-key only, returns COUNTS and never an employee id. **Deliberately not a `roster` call: roster needs a management session, and a licence in the trapdoor has nobody who can sign in, so that check would be unavailable exactly when it matters.** The five auth modules now **export their own `PROVISIONING_ROLES` and table name** (one additive line each, `node --check` clean, verified by loading all five and printing them) so the detector cannot drift from the rule it checks. Three states, and `NO_CREDENTIALS` is deliberately NOT folded into the failure count; an app with no `set_active` returns `NOT_APPLICABLE` rather than `HEALTHY`, so a future app that adds it cannot inherit a clean answer it never earned. **LIVE SWEEP OF ALL SEVEN LICENCES ACROSS FIVE APPS: every one HEALTHY.** Two things that sweep surfaced: **SD-AUDIT-2026 is HEALTHY (4 rows / 2 active provisioners)** despite `api/sd-auth.js:304-308` naming it as the licence that was LOST to this exact trapdoor -- it has recovered and the comment is stale; and **dnt, mech and sc each sit at exactly ONE active provisioner**, which the API cannot reduce but any SQL touching those rows can.
  **SAIRNDENTAL DECISION DOC written, nothing built.** Five options with costs and reversibility. **The finding that shapes it: SAIRNdental has NO `admin` role.** Its vocabulary is `owner, frontdesk, provider`, yet `api/dnt-auth.js:146` says "'admin' runs the office but does not mint identities" -- reasoning carried over from an app that HAS one, making today's design look like a deliberate narrowing from two roles to one when it was never two. That comment should be fixed under any option. **Recommendation: Option A (leave it) plus the comment fix, with Option D (a new `manager` role, matching mech/roofing's `admin`) as the thing to build when a customer actually asks.** The reasoning that decides it: **widening was proposed as a lockout mitigation and is not one** -- the API could never reach the trapdoor, so the real problem widening solves is "owner unavailable, nobody can offboard staff", which no customer has reported and which B/C guess at. B and C are cheap to do and expensive to undo; D is the reverse and matches the platform's existing shape. **Option E (break-glass) cannot be built safely today** -- a licence key is shared with every employee and is not a secret, so recovery gated on it alone lets anyone holding it seize a tenant, and SAIRNdental has no verified-email flow.
  **A MISTAKE, recorded because it cost real work:** I ran `git reset --hard HEAD~1` to remove a deliberate proof commit while unrelated UNCOMMITTED work sat in the same tree, and it destroyed the guard insertions and the hook edits. Redone from the scratchpad script. Earlier the same night I stashed before rebasing for exactly this reason. **Commit or stash before any hard reset, including one that looks like it only touches a commit you just made.**
  Also noted: the post-push deploy hook flagged `sairn.vercel.app/stonedesk` as not matching HEAD. **I have not touched `stonedesk.html` this session** and both new endpoints (`reference-fingerprint`, `provisioner-health`) deployed and answered within one poll, so I treated it as a false positive rather than pushing a re-trigger commit for a file I never changed.

- sql/sairnlaw_deadline_seed_arkansas.json + calendars + api/_lib/deadline-engine.js + docs (Hank) -- **ARKANSAS SEEDED. The gate had it BLOCKED, and the thing that cleared it was a channel the gate itself had named and never tried.**
  **THE BLOCK WAS REAL AND IS STILL TRUE.** Ark. Code Ann. 1-5-101 -- specifically subsection (b), the observance shift -- has never been read on a primary source. Four secondary publishers agree on the sentence and the gate refused to seed on agreement. **What changed:** every earlier attempt went to `media.ark.org/artreasury/` (2026 and 2027 both 403). The **SECRETARY OF STATE** publishes the same sheet -- `sos.arkansas.gov/uploads/holidays_2026.pdf`, HTTP 200, 204,182 B, real text via pypdf, headed *"2026 State Holidays. Legal holidays by authority of Act 304 of 2001 and Amended by Act 561 of 2017."* Sec. 1b of the gate had listed exactly this as the third of three routes that would clear it: *"a court or agency document that QUOTES (b) rather than citing 1-5-101 -- the Treasurer's sheet was the near miss."*
  **THE DISTINCTION THAT MAKES THE SEED HONEST, and it is narrow: the calendar is TRANSCRIBED, not DERIVED.** A calendar in this engine is an enumerated list of dates, not a shift rule, so every 2026 date now comes off a primary state publication and no unread statute is relied on. The gate blocked a *generated* calendar spanning years; it does not block a *transcribed* one for a year the State has published. **2027 REFUSES `NOT_PROVISIONED`** -- that sheet 404s and deriving it would need subsection (b). Utah's cap shape, and the safe direction. Verified live.
  **THE SHEET CONFIRMED BOTH TRAPS THE GATE PREDICTED:** Christmas Eve IS on the state list (no other seeded state except South Carolina has 24 December) and **Juneteenth and Columbus Day are NOT** -- so the state-federal union ARCP 6(a) requires is mandatory, and a state-only calendar computes EARLY two days a year. It also SHOWS the Saturday limb firing in the one 2026 instance where it does: *"July 4 . . . (Observed on Friday, July 3, 2026)"*. No 2026 holiday falls on a Sunday, so the unproven half of (b) never fires in what was seeded.
  **13 RULES, all read verbatim from the court's own PDF** (`opinions.arcourts.gov`, 1,311,020 B, 195pp, *"current through June 4, 2026"*). New engine standards: **`ar_rcp_6` with a FOURTEEN-DAY short-period exclusion -- the longest on the platform** (six states use 7, three use 11, three have none; copying any of them computes EARLY on the 10-day and 5-day rows), and **`ar_rcp_6_d`, three BUSINESS days with E-SERVICE EXPRESSLY INCLUDED** -- the opposite of the federal rule, Nevada, WV and NY. **Do not reason from FRCP 6(a) here:** the federal rule abolished its exclusion in 2009 and Arkansas went the other way.
  **ARKANSAS SETTLES THE RULE 4 / RULE 5 QUESTION IN ITS OWN TEXT.** ARCP 6(d)'s proviso names Rule 4 outright, which is direct textual confirmation of the reading two federal rows had to be corrected to on 2026-08-27 by inference. Both halves are encoded and asserted: the answer rows take NO extension, the cross-claim row KEEPS three business days.
  **ENGINE PUSHED FIRST, ON ITS OWN, THEN LOADED** -- the loader's standing warning, and the reason NC's 13 rules were once rejected wholesale. 13 rules + 1 calendar loaded, zero failures. **20/20 LIVE ASSERTIONS PASSED** on LAW-PINNACLE-2026, including the carve-out both ways, the 14-day exclusion as arithmetic (5-day reply skipping Labor Day), e-mail extending where personal service does not, Juneteenth excluded as an intermediate day, Christmas Eve rolling through Christmas and the weekend, a 60-day incarcerated-defendant period no other jurisdiction has, the first BACKWARD row outside the federal seed, and the 2027 refusal.
  **A NEAR-MISS WORTH KEEPING: `effective_from` is PER RULE, not the collection's currency date.** The first draft used 2026-06-04 for all 13 rows and every Rule 12 row then refused `NO_RULE_IN_FORCE` for triggers before June 2026 -- against rules that were in force. Each rule's own HISTORY line has the real answer: Rules 6/33/34/36 were *"amended and effective June 4, 2026"*, but **Rule 12's current text dates from *"amended June 21, 2018, effective January 1, 2019"***. A currency date and an effective date are different things; the engine computes against the law as it stood at the TRIGGER date and made the difference visible immediately. Asserted in the test file both ways.
  **NINE EXISTING TEST FILES BROKE AND THAT WAS THE POINT** -- each carries a standing invariant asserting the EXACT set of jurisdictions declaring a coverage entry, so adding `ar` forced the question rather than letting it slide. Massachusetts needed a second edit its siblings did not: it also checks each summary NAMES ITS OWN jurisdiction, and the regex map had no `ar` key, so it threw instead of failing an assertion.
  Live coverage is now **25 computing jurisdictions / 282 computing rules**, verified by the load-state gate (282/282 rules, 132/132 calendars, 0/0/0) rather than by counting seed files. **816/816 deadline tests pass** (779 + 37 new).

- sql/sairnlaw_deadline_seed_alabama.json + calendars + engine + docs (Hank) -- **ALABAMA SEEDED (11 rules, 16/16 live), AND WISCONSIN IS BLOCKED ON A DECISION I FOUND THE EVIDENCE FOR BUT DID NOT MAKE.**
  **ALABAMA.** Live is now **26 computing jurisdictions / 293 computing rules**, verified by the load-state gate (293/293 rules, 133/133 calendars, 0/0/0). All four of the gate's "would be wrong if inherited" items encoded AND asserted: **(1) the service order is FEDERAL after-expiry** -- "3 days are added AFTER THE PERIOD WOULD OTHERWISE EXPIRE" -- and the test asserts against `2026-06-03`, the date period-lengthening would have produced, since nearly every recently seeded state uses that other order; **(2) the exclusion is ELEVEN**, not 7 and not Arkansas's 14; **(3) the holidays are a THREE-SOURCE union** because Ala. R. Civ. P. 6(a)(4) is the only seeded rule that NAMES ITS OWN eleven days and then sweeps in both the federal list and Ala. Code 1-3-8; **(4) Thanksgiving has NO statutory date** ("as designated by the Governor") so it comes from the federal limb, recorded rather than assumed.
  **TWO DATES EXIST ON NO OTHER CALENDAR IN THIS PLATFORM** -- Confederate Memorial Day (4th Mon April) and Jefferson Davis' Birthday (1st Mon June). Neither is federal and neither is in the rule's own eleven; both reach deadlines only through 6(a)(4)(B), and both are asserted as arithmetic. The answer row from 2026-05-01 rolls TWICE -- Sunday, then Jefferson Davis' Birthday -- which nothing else here can produce.
  **A FOURTH TRAP CAUGHT IN THE RULE TEXT, not in the gate: "MAY" vs "SHALL NOT BE REQUIRED".** Rules 33 and 34 say a defendant MAY serve within 45 days -- an ELECTION -- while Rule 36 says a defendant SHALL NOT BE REQUIRED to serve before 45 days, a mandatory FLOOR. Only 36 is a `resolve_periods later_of`; encoding the elections as floors would report dates the rules do not guarantee. Same distinction South Carolina's Rule 36 row records, and asserted here by showing 33 and 34 land on the plain 30-day date on identical facts.
  **`effective_from` IS PER RULE AGAIN, and Alabama makes the point better than Arkansas did:** five different dates across 11 rows, from Rule 36's 1995-10-01 to Rule 6's **2026-04-09** -- the freshest rule text on the platform, amended four months ago. A single collection date would have been wrong for four of the five.
  **ONE ALABAMA ITEM IS DELIBERATELY UNRESOLVED.** Ala. Code 1-3-8(f)(1) lets an office STAY OPEN on a state holiday on 60 days' notice; if a court did and the engine rolls anyway, that is **LATE**. It is discretionary, per-office and published nowhere machine-readable, so it is **disclosed in the coverage summary rather than buried in the detail** -- and the gate had already said it should go to the bundled holiday question rather than be waved through. It is still there.
  **WISCONSIN: I ANSWERED THE GATE'S OPEN QUESTION AND IT REFUTES THE GATE'S OWN PREFERRED OPTION.** Sec. 6 listed as *not determined* whether Wisconsin's clerks publish closure schedules -- "which is what option 1 turns on". **They do:** `wicourts.gov/courts/circuit/docs/holidayschedule26.pdf` (200, 86,005 B, 3pp; the 2027 sheet 404s). **It is PER-COUNTY and the counties genuinely diverge** -- the sheet's own key is `X = Closed all day; PM = Closed in the afternoon; No Court = Courts are down but offices are OPEN`, and at least **12 of 72 counties** carry a non-closure marker on a listed holiday: **Dane is OPEN on Juneteenth, Barron is OPEN on Memorial Day**, six counties are PM-only on MLK Day. Since Wis. Stat. 801.15(1)(b) rolls on *"a day the clerk of courts office is closed"* and NOT on the 801.15(1)(a) list, **encoding the list as a rollover proxy would roll LATE in real counties.** Option 1 is now refuted by the court system's own publication rather than merely unproven.
  **PROPOSED, NOT CHOSEN -- option 4: seed the statewide INTERSECTION of actual closures** (only days every county is marked closed all day). Derived from a primary court-system publication, it can never roll LATE because any day some county is open is simply absent, and where it is wrong it is EARLY. Caps at 2026 like Arkansas and Utah. **This is a design decision with a legal consequence and it is Michael's, not the session's. Wisconsin stays unseeded pending that call**, and it is the same question Alabama's 1-3-8(f)(1) raises in a milder form -- a statutory holiday list is not the same fact as whether the courthouse was open.
  **844/844 deadline tests pass.** The nine-file coverage-set invariant has now caught two jurisdiction additions in a row, which is the argument for keeping it.
